---
description: 极简 + 编码铁律 + 工作流 + Git/沟通约束（全项目通用）
alwaysApply: true
---

# AI 编码代理完整规则

## 0. 优先级（冲突时）

1. **安全与数据**：信任边界校验、防数据丢失、安全、无障碍——不可为极简省略
2. **用户明确要求**：点名要的功能/文档/测试/完整实现 → 照做
3. **编码铁律**：可读性底线、内联禁止项——原则性约束，非机械指标
4. **极简**：最短路径、最少代码
5. **Git/PR**：仅用户明确要求时执行
6. **沟通格式**：代码引用 `startLine:endLine:filepath`；实现任务代码优先

**规则冲突时的总原则**（不得为满足某一条规则而降低整体代码质量）：

正确性 > 可维护性 > 可读性 > 极简

**优先保持项目一致性**。修改已有项目时，优先遵循项目已有的目录结构、编码风格、命名规范、技术选型；除非用户明确要求调整。**项目约定优先于通用最佳实践**。

---

## 一、决策阶梯

写代码前，按下面顺序思考：

0. 先理解（修改已有项目时）
   → 先阅读相关代码，理解当前实现、调用关系、数据流、命名风格
   → **禁止**未理解上下文直接重写

1. 不写可以吗？
   → 不是当前需求，直接跳过（YAGNI）

2. 已有实现可以复用吗？
   → 优先复用，不重复造轮子（DRY）

3. 项目 / 平台能解决吗？
   → 项目已有方案 > 平台能力（标准库 / CSS / 数据库）

4. 现有依赖能解决吗？
   → 不为几行代码增加新依赖

5. 能再简单一点吗？
   → 选择最简单、最容易维护的方案（KISS）

6. 最后才写代码
   → 只写满足需求的最少代码（MVP）

7. 已经满足需求了吗？
   → **满足就停止**，不继续优化、抽象、封装

**MVP ≠ Demo**。必须满足：

- 用户需求完整
- 可直接运行
- 可维护

无需为了未来扩展增加设计；仅避免过度设计；禁止用 `TODO`、「以后补」代替应交付的功能。

**禁令**：删优于增；最少文件、最短 diff。
复杂需求：默认完成用户要求；仅当需求存在明显扩展空间时，可额外说明还可以继续完善。
刻意简化标 `// ponytail: …`；有性能/并发上限时写明天花板与升级路径。

**实现输出**：代码优先；其后最多三行 `skipped: X, add when Y`（用户要的报告/文档除外）。

**不可懒**：信任边界校验、防数据丢失、安全、无障碍、用户点名内容；硬件留校准旋钮。
仅用户要求、或项目已有测试体系、或确有验证价值时，补充最小验证。

---

## 二、核心工作流

### 需求澄清

- 模糊任务先确认需求、边界、交互；动手前明确验收标准

### 方案设计（复杂任务）

默认完成用户要求并编码。

存在以下情况时，**先确认再编码**：

- 重大架构分歧（多种方案代价相当）
- 需求歧义、验收标准不清
- 不可逆修改（数据迁移、破坏性 API 变更等）

多状态/复杂交互：可用流程图或组件图辅助说明；非必须等待确认。

### 实施与交付

- 分步进行；结束说明**做了什么**及**为什么**
- 若当前环境支持文件修改且存在项目目录，有代码改动时更新 `.ai-coding-md/迭代-YYYYMMDD.md`（改了哪些文件、核心逻辑、注意事项）

### 输出原则

- 默认**先改代码**、直接输出最终代码；不要分析半天、不要讨论理论
- 不要：长篇解释、教学式介绍、重复需求
- 仅在用户询问设计原因，或设计取舍非显而易见时，简短说明

---

## 三、编码铁律

### 体积与拆分

- 函数应保持足够短，**优先保证可读性**；超过约 30～50 行时，评估是否存在可独立理解的业务动作
- **禁止**仅为满足行数限制，拆成多个只调用一次的步骤函数（如 `ensureLogin()` → `buildPayload()` → `requestLogin()` 链式碎函数）
- 完整业务动作、或多处复用的逻辑，允许且应独立成函数；命名：动词 + 业务

`.vue` 页面过大时，**首先评估**是否存在：

- 独立 UI 区块
- 独立业务区块
- 跨页复用逻辑

可拆组件时优先拆组件；多页复用或跨页业务编排再进 `composables/`。**禁止**仅为减少行数把单页逻辑拆成跳读文件或碎函数。保留在同页更清楚时，可用区块注释整理顺序。

### 常量与配置

**不要为了抽象而抽象。**

- 相同值仅使用一次时保持内联
- 仅当提升可读性、避免重复、跨处复用时再提取常量
- `import.meta.env` / `pages.json` / `manifest` / 多文件 `locales` 等框架级配置除外

### 分层

**优先保持就近**；类型判断、单步、短逻辑优先内联。

仅当以下情况再拆入 `composables/` 或 `utils/`：

- 已复用
- 明显会复用（如通用格式化、跨域工具）
- 存在独立领域边界

禁止仅为降行数拆出单消费者文件；禁止机械计数「是不是第二次」再决定是否提取。

### 类型文件位置

- 可复用业务类型统一放 `src/types`，按域组织：接口契约放 `src/types/modules/<domain>.ts`；页面/业务共享类型放 `src/types/<domain>.ts` 或同域 `modules` 文件
- 禁止在 `pages/`、`components/`、`composables/` 下长期维护共享 `*-types.ts`
- 仅当前文件私有、且不导出的类型，可留在当前 `.vue` / `.ts` 内

### 极简与风格

No Dead Code；**Platform First**（项目已有方案 > 平台能力 > 新增依赖）；Directness。平铺 `if/else`；可读性 > 错误抽象。

### 命名可读性底线

- 禁止业务参数/变量使用无语义短名：`v`、`n`、`p`、`s`、`d`、`e`、`ft`、`mn`、`ec`、`res2` 等
- 允许例外：`i`/`j` 极短循环下标；数学/坐标公式通用名；1 行回调且语义从 API 完全明确
- 业务名必须表达含义：如 `rawValue`、`parsedCount`、`phone`、`price`、`firstTicket`、`magicNo`、`eventChannel`
- 方法名动词 + 业务名；禁止 `handleData`、`doAction`、`processItem` 等泛名

### Vue 专项

**组合式 API 优先**；`<script setup>` 内推荐顺序：

1. `import`
2. 本页私有类型（可复用类型必须放 `src/types`）
3. 常量
4. `defineOptions`（如有）
5. `defineProps` → `defineEmits`
6. composable / store / i18n 初始化
7. 页面状态：`ref` / `reactive`
8. 派生状态：`computed`（**优先 computed，其次 watch**）
9. 公共方法：格式化、转换、校验、提示等
10. 页面业务方法：按用户流程或业务流程顺序排列
11. 触发代码：`watch` / `watchEffect` / `onLoad` / `onShow` / `onMounted` / 事件通道监听

- **能 derived 就 derived**：避免用 `watch` 同步本可由 `computed` 派生的状态
- 模板保持声明式，避免复杂表达式；复杂逻辑放 `computed` 或方法
- 禁止把生命周期、watch、业务方法穿插摆放；除非依赖顺序必须提前，需用一行注释说明

### 修改已有代码

默认保持：

- API、类型、调用方式、对外行为不变
- 代码风格、目录结构、命名方式、异常处理方式、日志风格与项目一致

不要引入新的编码风格；除非用户明确要求重构。精准打击；优化滞后；任务结束删无用变量 / import / 注释代码。

### 异常处理

保持与项目一致。不要每个函数都 `try/catch`；能够向上抛出就不要吞异常；日志只记录真正需要的信息。

### 性能

默认优先代码正确性；只有存在真实性能问题时再优化；不要提前优化（memo、cache、Map 等）。

### 注释

端差异、Mock、对外 composable/utils、接口契约、非显而易见业务规则须注释；禁密钥、无归属 TODO、注释保留废弃代码。

### 测试

仅用户要求或有真实覆盖价值时加；不写 trivial 测试。

---

## 四、Git 与 PR（仅用户明确要求）

- 格式：`type(scope): subject`（feat/fix/refactor/style/docs/test/chore）
- 未要求不 commit/push；不提交含密钥文件；不清不楚先问
- PR 用 `gh`；摘要覆盖全部将纳入的 commit

---

## 五、环境与执行

- 真实环境：自己跑命令、查代码、验证；不编造数据
- 不确定依赖版本时询问
- 完整遵循 user/tool/skill 指令

### AI 禁止行为

- 无请求抽象、无样板、无「以后用」脚手架
- 虚构项目结构、目录、文件
- 虚构依赖、包名、版本
- 虚构接口、API 路径、请求/响应字段
- 虚构数据库表、字段、配置项
- 猜测未明确的需求并擅自实现
- 修改用户未要求的业务逻辑
- 无文件系统时假装已写入文件；应直接给出代码块供用户复制
- **为了遵守规则**而故意增加复杂度、拆分代码、删除必要逻辑、降低可读性（如为 DRY 碎拆、为 KISS 删必要逻辑、为 Platform 弃用项目已有成熟方案）

---

## 六、任务结束检查

- [ ] 需求已满足，未继续无关优化、抽象或封装
- [ ] 无冗余代码、死代码、无用 import；未为满足规则而机械拆分或优化
- [ ] 命名与风格符合项目已有习惯
- [ ] 若环境支持且存在项目目录，已更新迭代文档（有改动时）
