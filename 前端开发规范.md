# 前端开发规范

本文档基于 `前端架构方案总览.md` 制定，作为前端团队日常研发的统一执行规范。适用于 `admin-web`、`customer-web`、`customer-mobile` 以及与之配套的业务组件库 `uni-lib`。

---

## 一、适用范围

- `admin-web`：管理后台项目
- `customer-web`：C 端站点项目
- `customer-mobile`：移动端跨端项目
- `uni-lib`：业务组件库

原生应用 `native-app` 以 Flutter 规范为主，不完全适用本文，但接口契约、命名一致性、提交规范等通用规则可参考执行。

---

## 二、技术栈规范

### 2.1 项目技术栈

| 项目类型   | 技术栈要求                                                           |
| ---------- | -------------------------------------------------------------------- |
| 管理后台   | Vue 3 + Vite + Vue Router + Element Plus + Pinia + TypeScript + Sass |
| C 端站点   | Nuxt 3 + Vue 3 + Pinia + TypeScript + Sass                           |
| 移动端跨端 | Taro + Vue 3 + TypeScript + Sass                                     |
| 业务组件库 | Vue 3 + Element Plus + TypeScript + Sass                             |

### 2.2 基础要求

- 新项目必须基于统一模板目录创建，不允许随意自建一套工程结构
- 默认启用 TypeScript，禁止新增纯 JavaScript 业务模块
- 默认使用 Sass，禁止新增 Less 或 Stylus 作为主样式方案
- 状态管理统一使用 Pinia
- 管理后台统一使用 Element Plus 作为基础 UI 组件库
- 跨项目通用组件统一沉淀至 `uni-lib`

---

## 三、目录结构规范

### 3.1 管理后台标准目录

```bash
src/
├── api/                        # 接口层，只放请求方法、参数类型、响应类型
│   ├── modules/                # 按业务域拆分，如 user.ts、order.ts
│   └── index.ts                # 统一导出
├── assets/                     # 静态资源层，不放业务逻辑
│   ├── images/                 # 图片、图标、字体
│   └── styles/                 # 全局样式、变量、mixin、reset
├── components/                 # 当前项目复用组件层
│   ├── common/                 # 通用展示组件
│   └── business/               # 当前项目业务复用组件，跨项目组件应进入 uni-lib
├── composables/                # use-xxx 逻辑复用层，不承担页面渲染职责
├── directives/                 # 全局或局部通用指令
├── layouts/                    # 后台布局、默认布局、空白布局
│   ├── index.vue               # 后台主布局入口
│   └── components/
│       ├── content.vue
│       ├── header.vue
│       ├── menu-tree.vue
│       ├── sidebar.vue
│       └── tags-view.vue
├── plugins/                    # 第三方库注册、全局能力初始化
├── router/                     # 路由层，不写接口逻辑
│   ├── guards/                 # 路由守卫
│   ├── modules/                # 路由模块拆分
│   └── index.ts                # 路由统一聚合
├── stores/                     # Pinia 状态层，不放 DOM 类逻辑
│   ├── modules/                # 按业务域拆分状态
│   └── index.ts                # 统一注册导出
├── types/                      # 全局类型、接口契约、模块类型，不写运行时代码
├── utils/                      # 纯函数、格式化工具、存储工具、请求薄封装
│   ├── request/                # 请求实例、拦截器封装
│   ├── storage/                # 本地存储相关工具
│   └── index.ts                # 通用工具导出
├── views/                      # 页面编排层，不长期沉淀跨页面公共能力
│   └── order/                  # 业务页面目录
│       ├── components/         # 页面私有组件
│       ├── list.config.ts      # 搜索、表格、表单等页面配置
│       └── list.vue            # 列表页入口（含 <script setup> 业务逻辑）
├── App.vue                     # 应用根组件
└── main.ts                     # 应用启动入口
```

### 3.2 C 端站点标准目录

```bash
src/
├── app.vue                     # 应用根入口，只承载全局壳层能力
├── assets/                     # 图片、字体、全局样式、主题变量
│   ├── images/                 # 静态图片资源
│   └── styles/                 # 全局样式、主题样式
├── components/                 # 当前站点复用组件
│   ├── common/                 # 通用展示组件
│   └── business/               # 当前站点业务复用组件
├── composables/                # 可跨页面复用的组合逻辑
├── layouts/                    # 站点布局壳，如默认布局、活动布局
├── pages/                      # 页面路由层，等价于后台项目的 views
│   ├── index/                  # 首页目录
│   └── user-center/            # 用户中心目录，页面私有组件可放同级 components
├── plugins/                    # Nuxt / Vue 插件初始化
├── stores/                     # Pinia 状态层
│   ├── modules/                # 按业务域拆分状态
│   └── index.ts                # 统一导出
├── types/                      # 页面模型、接口类型、全局类型声明
└── utils/                      # 纯工具函数与通用薄封装
```

### 3.3 移动端跨端目录建议

```bash
src/
├── app.config.ts               # 应用级路由、页面注册、全局配置
├── app.scss                    # 应用全局样式入口
├── pages/                      # 页面路由层，按小程序 / H5 页面维度拆分
│   ├── home/                   # 首页目录
│   └── mine/                   # 我的页面目录
├── components/                 # 跨页面复用的移动端组件
├── composables/                # 跨页面逻辑复用
├── stores/                     # Pinia 状态层
├── utils/                      # 纯工具函数
├── services/                   # 接口能力封装；若统一用 api/ 则不要重复维护
└── types/                      # 页面与接口相关类型
```

- 页面目录结构应贴近端侧路由结构，避免出现过深的业务目录嵌套
- 同一移动端项目中，接口目录命名必须统一为 `services/` 或 `api/` 其中一种

### 3.4 `uni-lib` 组件库目录建议

```bash
uni-lib/
├── packages/
│   └── ui/
│       ├── src/
│       │   ├── components/     # 标准业务组件，每个组件独立目录管理
│       │   │   └── uni-data-table/
│       │   │       ├── index.ts
│       │   │       ├── index.vue
│       │   │       ├── cell.vue
│       │   │       ├── toolbar.vue
│       │   │       ├── column-settings.vue
│       │   │       ├── use-columns.ts
│       │   │       ├── use-data.ts
│       │   │       └── use-export.ts
│       │   ├── directives/     # 可复用指令，如权限、复制、防抖点击
│       │   ├── composables/    # use-xxx 组合式能力，如认证、主题、字典
│       │   ├── plugins/        # install 入口、全局注册与配置注入
│       │   ├── services/       # 请求、认证、国际化、主题等协议层封装
│       │   ├── theme/          # 设计 token、样式变量、Element Plus 主题扩展
│       │   ├── types/          # 组件对外暴露类型与公共类型
│       │   ├── utils/          # 纯函数工具，如格式化、下载、校验
│       │   └── index.ts        # 组件库统一导出和安装入口
│       └── package.json        # 子包配置
├── docs/                       # 组件文档站、示例说明、使用文档
├── playground/                 # 联调验证工程，不放正式发布代码
├── scripts/                    # 构建、发版、校验脚本
└── package.json                # 仓库根配置
```

- `uni-lib` 中组件必须保持可复用、低耦合，不直接依赖业务项目 `store`、页面路由和私有接口地址
- `uni-lib` 可提供组件、指令、组合式函数、插件和请求/认证/国际化/主题等协议层能力，但必须通过配置、回调或依赖注入与业务工程解耦
- `uni-lib` 必须支持全量安装与按需导入；对外组件统一 `Uni` 前缀，目录统一 `uni-xxx`
- `uni-lib` 对外主组件使用 `index.vue`，入口使用 `index.ts`；组件目录内文件不重复目录语义，如使用 `cell.vue`、`toolbar.vue`、`use-columns.ts`，不使用 `uni-table-cell.vue`、`use-uni-table-columns.ts`
- 文档站、类型声明、组件源码应同步维护，不允许只改组件不更新文档

### 3.5 层级控制

- `src/` 下一级目录必须保持稳定，不允许随意新增平级目录
- 业务页面目录建议控制在 `3` 级以内，例如 `views/order-list/components/`
- 页面私有组件目录建议不超过 `2` 层
- 通用工具目录建议不超过 `2` 层，避免形成深层迷宫式结构
- 出现超过 `4` 层路径时，应评估是否需要重新抽象

### 3.6 放置原则

- 页面独有逻辑优先留在页面内部（管理后台：`views/<域>/use-*.ts`；C 端站点：`pages/<路由>/` 内 composable 或 `use-*.ts`；移动端：`pages/<页>/` 内逻辑）
- **多页面共用的业务逻辑**抽至 `composables/`（见 **3.7**）
- **全局共用的纯方法**（与具体页面流程无关）抽至 `utils/`（见 **3.7**）
- 多页面复用组件抽至 `components/`
- 多项目复用组件抽至 `uni-lib`
- 接口能力统一经 `api/` 或 `services/` 输出（移动端全项目二选一）

### 3.7 `composables/` 与 `utils/` 分工

二者都可能被多个页面引用，但**职责不同**，禁止混放。

| 目录 | 放什么 | 典型特征 | 命名 |
| ---- | ------ | -------- | ---- |
| **`composables/`** | **多页面共用的业务逻辑** | 组合式函数；可使用 `ref` / `computed` / `watch`；可调接口层、读 Pinia、依赖路由或 i18n；表达「怎么做业务」 | `use-xxx.ts`，导出 `useXxx()` |
| **`utils/`** | **全局共用的纯方法** | 无 Vue 组件生命周期；**不**持有 `ref`、路由、store；输入输出明确；表达「怎么算 / 怎么转」 | 如 `format-date.ts`，导出普通函数 |

**一句话：** `composables` = 跨页的 **use-业务**；`utils` = 跨页（乃至可跨项目）的 **工具函数**。

#### 判定流程

1. **是否只服务当前一个页面（或同一页面域）？** → 写在对应 `.vue` 的 `<script setup>`（**admin-web 不新建 `use-list.ts` / 页面专用 `use-*.ts`**），**不进** `composables/`。
2. **是否 2 个及以上页面 / 模块要用？** 继续 ↓
3. **能否写成「给定参数 → 返回值」、且不 `import 'vue'`、不调接口？**
   - **是** → `utils/`（如 `formatDateTime`、`downloadBlob`、`parseCsvLine`）
   - **否**（要拉接口、维护弹窗/列表状态、权限、字典选项等）→ `composables/`（如 `useListTableEmpty`、`useMembershipSchoolOptions`）
4. 业务域专用纯计算可放在 `utils/<domain>.ts`；若同一文件内出现 `ref`、调 api、路由，应拆到 `composables/use-<domain>-xxx.ts`，纯函数保留在 `utils/`。

#### 示例

| 放 `utils/` | 放 `composables/` |
| ----------- | ----------------- |
| `formatDateTime(iso: string): string` | `useListTableEmpty()`（空态 + i18n） |
| `buildQueryString(params)` | `useMembershipSchoolOptions()`（api + 选项 `ref`） |
| `utils/request` 拦截器、`storage` 封装 | `useDialogDetailLoading()`（详情 loading 编排） |

#### 各项目页面内落点对照

| 项目 | 仅当前页业务逻辑 | 多页业务逻辑 | 全局纯方法 |
| ---- | ---------------- | ------------ | ---------- |
| `admin-web` | `views/<域>/*.vue` `<script setup>` | `composables/use-xxx.ts` | `utils/` |
| `customer-web` | `pages/<路由>/` 内 `use-*.ts` 或 composable | `composables/` | `utils/` |
| `customer-mobile` | `pages/<页>/` 内函数或 `use-*.ts` | `composables/` | `utils/` |

#### 禁止事项

- 在 `utils/` 中使用 `import { ref } from 'vue'` 或 `useRoute()` / `useStore()` → 改为 `composables/` 或页面 `.vue` 的 `<script setup>`。
- 在 `composables/` 中堆积与业务无关的日期、字符串格式化 → 下沉 `utils/` 再由 composable 调用。
- 仅单页使用却提前放入 `composables/`「以备后用」。
- 在 `views/`、`pages/` 下长期放置 `*-utils.ts`；无 Vue 依赖的纯函数应迁入 `src/utils/`。
- 将多页共用的业务编排放进 `utils/<domain>.ts` 却混用 `ref` / 接口（应拆分 composable + utils）。

---

## 四、代码分层规范

### 4.1 分层要求

统一采用以下分层：

```text
API Client -> Service Layer -> Store / Component
```

### 4.2 分层职责

- API Client：统一处理请求实例、拦截器、鉴权、错误处理
- Service Layer：按业务域封装接口能力
- Store：组织状态、页面共享数据、业务动作
- Component / Page：负责界面展示与交互编排，不直接散落请求细节

### 4.3 禁止事项

- 在页面或组件中直接大量拼接请求逻辑
- 在 `utils/` 中编写依赖页面上下文的业务逻辑
- 在 `api/` 中写视图层逻辑
- 管理后台新建或恢复 `api/constants.ts`、`API_PATHS` 集中 REST 前缀（见 **4.4**；须内联在对应 `api/modules/*.ts`）
- 页面文件过大却不拆分，原则上应主动拆组件和 composable

### 4.4 管理后台 `api/modules` 路径约定（admin-web）

与 **uni-review-web §13** 一致，适用于 `admin-web` 及同类管理后台项目。

- 按业务域单文件封装请求；参数/响应类型显式声明（可放同文件或 `types/modules/`）。
- 后端 **REST 路径前缀**在对应 `src/api/modules/<domain>.ts` **文件顶部**用 `const path`、`const base`、`membershipBase` 等**字面量**声明，与接口方法同文件维护。
- **禁止** `api/constants.ts`、`API_PATHS` 等跨模块集中路径表；禁止为复用前缀抽到 `api/` 根目录汇总再 `import`。
- 单文件内确有多套资源域时，允许多个 `const`（如活动主数据与反馈导出各一条前缀）。
- 鉴权、错误提示、超时在 `utils/request` 拦截器统一处理；页面/`list.vue` 优先调 `api/modules`，禁止裸拼接 axios。

**原因：** 与权限码不抽 `permission-codes.ts` 同理——打开 module 即可对照后端路径，减少跳转与陈旧常量。

```ts
// 推荐
const base = '/isacommunity/busorder'
export default {
  page: { url: `${base}/getOrderPage`, /* … */ }
}

// 禁止
import { API_PATHS } from '@/api/constants'
```

**存量：** 可渐进把集中表内联到各 module 后删除；**新增与改动**不得再引用 `API_PATHS`。

---

## 五、组件开发规范

### 5.1 组件分类

| 类型           | 存放位置             | 说明             |
| -------------- | -------------------- | ---------------- |
| 页面私有组件   | 页面同级或页面子目录 | 仅当前页面使用   |
| 应用内复用组件 | `components/`        | 仅当前项目复用   |
| 跨项目标准组件 | `uni-lib`            | 可被多个项目复用 |

### 5.2 组件设计要求

- 组件职责单一，避免一个组件承担页面级业务编排
- Props、Emits、Slots 命名清晰，避免含糊语义
- 优先通过 `props`、`emits`、`slots` 实现解耦
- 不允许组件直接依赖具体页面路由名或页面私有 store
- 跨项目复用组件不得直接耦合业务项目接口地址

### 5.3 页面组件抽离规则

#### 判定原则

页面组件抽离按“先页面内，再项目内，最后组件库”的顺序判断：

1. 只在当前页面使用，保留为页面私有组件
2. 在当前项目多个页面复用，抽至 `components/`
3. 在多个项目复用，且接口稳定，进入 `uni-lib`

#### 页面私有组件

以下场景优先保留在页面目录内，如 `views/order-list/components/` 或 `pages/user-center/components/`：

- 仅当前页面使用
- 强依赖当前页面路由、页面 store、页面交互流程
- 与当前页面布局强绑定
- 短期内没有跨页面复用价值

**示例**：

- `order-list-filter-panel.vue`
- `user-center-header-card.vue`
- `activity-detail-banner.vue`

#### 抽到项目级 `components/` 的条件

满足以下任一情况，应评估抽到项目级组件目录：

- 同一组件在 `2` 个及以上页面重复出现
- 同一套 UI 结构、交互、样式被复制使用
- 页面目录中多个私有组件已经出现明显相似实现
- 某个组件已具备清晰输入输出边界，可通过 `props` 和 `emits` 解耦

**推荐存放方式**：

- 通用展示能力放 `components/common/`
- 当前项目业务复用能力放 `components/business/`

**示例**：

- `components/common/page-header/page-header.vue`
- `components/business/order-search-form/order-search-form.vue`
- `components/business/user-status-tag/user-status-tag.vue`

#### 进入 `uni-lib` 的条件

只有在满足以下条件时，组件才允许进入 `uni-lib`：

- 至少在 `2` 个独立项目中有明确复用需求
- Props、Emits、Slots 已相对稳定
- 不依赖业务项目私有路由、私有 store、私有接口地址
- 样式和行为可以抽象为标准能力
- 具备文档说明、示例和类型声明

**示例**：

- `UniDataTable`
- `UniForm`
- `UniSearchForm`
- `UniUpload`

#### 拆分触发条件

页面组件或页面文件出现以下情况时，必须评估拆分：

- 单页面文件超过 > 800 行
- 同一页面内存在 `2` 块及以上重复模板结构
- 页面中同时承载筛选区、表格区、弹窗区、详情区等多个独立区域
- 页面脚本中存在大段独立交互逻辑，可抽为 `composable`
- 页面样式出现多个独立模块，已不适合继续写在同一文件中

#### 推荐拆分方式

- 筛选区域拆成 `xxx-filter-form.vue`
- 表格区域拆成 `xxx-table.vue`
- 弹窗区域拆成 `xxx-dialog.vue`
- 详情卡片拆成 `xxx-detail-card.vue`
- 超长 `<script setup>` 优先拆页内子组件；**不**新建 `use-list.ts` 等页面级逻辑文件

#### 禁止事项

- 仅因“目录好看”就过度拆分微小组件
- 一个简单静态片段拆成多个无意义组件
- 页面私有组件尚未复用就提前放入 `components/`
- 尚未跨项目复用就提前放入 `uni-lib`
- 将页面组件抽离后仍保留对页面私有变量、页面 DOM 结构的强耦合

#### 评审检查点

- 该组件是否真的跨页面或跨项目复用
- 抽离后输入输出是否清晰
- 是否减少了页面复杂度
- 是否引入了额外理解成本
- 存放位置是否与复用范围一致

### 5.4 方法与子逻辑抽离规范

方法与组合逻辑的抽离，目标只有两个：**降低单处阅读成本**，或**消除真实复用**。不满足其一，默认保留在原处。

#### 判定原则（先问三个问题）

**以下三问适用于是否要抽离「任意一个」新函数（含 utils、composable、页面内私有函数）。** 抽离前依次确认：

1. **是否只调用一次？** 仅单处使用的逻辑，默认写在调用方（`.vue` 的 `<script setup>`、事件处理）内联，不单独建函数文件。
2. **是否带来复用？** 无跨页面、跨模块复用预期时，不提前抽到 `utils/` 或项目级 `composables/`。
3. **是否减少理解成本？** 若抽离后需要在多个小函数间跳转才能看懂一次用户操作（如「提交表单」），视为过度抽离。

与组件抽离一致，逻辑沉淀顺序为：**页面 `.vue` → 项目 `composables/` / `utils/` → `uni-lib`（极少）**。

#### 必须抽离（满足任一）

| 场景 | 建议落点 |
| ---- | -------- |
| 单函数（含箭头函数）有效逻辑超过 **33 行** | 在同 `.vue` 内拆成多个命名函数，或多页复用时上提 `composables/` |
| **同一套逻辑**在 **2 个及以上** 页面/组件重复出现 | 有接口/状态/编排 → `composables/`；纯计算无 Vue → `utils/`（见 **3.7**） |
| 页面 `.vue` 同时承担列表加载、弹窗、导入导出等多条独立流程，单文件难以维护 | 拆页内 `components/`（表格/弹窗/筛选），**不**拆 `use-list.ts` |
| 纯计算、格式化、校验等**可单测**且与 DOM/路由无关 | `utils/` |

> **Rule of Three**：完全相同或高度相似的逻辑，第 **3** 次出现时必须抽公共实现；第 2 次出现可抽，也可在评审中约定下次一并治理。

#### 禁止拆碎步骤函数（内联原则，必遵）

**适用范围：前端业务代码中的全部函数与方法，无场景例外。** 含页面组件、composable、`utils`、`api`、请求封装与拦截器、Pinia、路由守卫、指令回调、类/模块内私有函数等；**不区分**导出/非导出、同步/异步。

**目标：** 读一条链路（请求拦截、表单提交、列表查询、单行格式化）时，不为 2～4 行的「步骤」单独建函数，避免为理解流程多跳一层文件。

| 情形 | 典型反例 | 处理方式 |
| ---- | -------- | -------- |
| **仅调用 1 次** | 任意文件中仅被调用一次的短函数 | 写在**唯一调用处**（`handleXxx`、事件、`onMounted`、拦截器等） |
| **有效逻辑 < 5 行** | 去空行与纯注释后核心语句不足 5 行（如 `ensureHeaders`、`isHideLoad`） | 不单独建函数 |
| **语义是「步骤」不是「动作」** | `attach*`、`ensure*`、`normalize*` 仅赋值/合并，无第二消费者 | 并入父级回调或**一个**完整业务动作函数 |
| **拆碎同一用户路径** | 读「提交」需连跳多个各不足 5 行的子函数 | 保留在一个 `handleSubmit` 内平铺 |

**仍允许单独成函数：**

- 表达**完整业务动作**（如整段 401 处理、`fetchList` 拉取与落表），且有效逻辑 **≥5 行**
- **2 处及以上**真实调用（第 3 次出现须抽公共实现前，也需 ≥5 行有效逻辑才值得抽）
- **多处复用**的基建（如请求 loading 栈的 `push/pop`，被多个拦截分支调用）
- 大型静态映射表（如 HTTP 状态码表）可保留为常量，查找写在调用处

```ts
// ❌ 禁止（页面 / utils / 拦截器 同一标准）
function openFormAdd() {
  formDlg.value?.open("add");
}
function attachAuthHeaders(headers: Record<string, unknown>) {
  headers["x-access-token"] = uni.getStorageSync("token") || "";
}

// ✅ 页面：内联
function handleAdd() {
  formDlg.value?.open("add");
}

// ✅ 拦截器：内联；多处复用的 push/pop loading 可保留函数
uniRequest.interceptors.request.use((config) => {
  if (!config.headers) config.headers = {};
  config.headers["x-access-token"] = uni.getStorageSync("token") || "";
  return config;
});
```

#### 禁止无谓参数/常量抽离（内联原则，必遵）

与上节「禁止拆碎步骤函数」**同一套内联原则**，适用于**所有参数、常量、配置项的定义与抽取**（不仅是函数、不仅是路由 path）。

**涵盖：** 路由、接口前缀、权限码、Storage/Header 名、魔法数字、枚举、`as const` 对象、默认值、`config/`、`constants/`、单次请求用的 options 对象等。

| 情形 | 典型反例 | 处理方式 |
| ---- | -------- | -------- |
| **取值仅 1 处使用** | `const PAGE_SIZE = 10` 只在一处列表 | 直接写字面量 |
| **对象每个 key 各用 1 次** | `APP_ROUTES`、`CONFIG.xxx` 每键 1 处 | 禁止建表；值写在调用处 |
| **仅 1 个文件 import 的 constants** | `constants/routes.ts` 单消费者 | 写在消费文件内 |
| **仅为传参包一层** | `const opts = { hideLoad: true }` 只传一次 | 调用处内联 |

**仍允许：** ≥2 文件或同文件同值 ≥2 次 → 消费方文件顶部 `const`；多 module 共用的纯函数；大型映射表；环境变量与构建配置。

**禁止：** 无真实复用的 constants/config 汇总；每键只用一次的对象表；为整洁或 Lint 抽参。

#### 应保留在原处（不必要的抽离）

以下情况**禁止**仅为“整洁”而抽函数：

- **一次性包装**：函数体只有 1～2 行，且仅被调用 1 次（如 `setLoading(true)`、`formRef.value?.validate()` 再调接口）。
- **拆碎步骤函数**：见上表（含 `attach*` / `ensure*` 类仅调用一次的短函数）。
- **拆碎用户动作**：把 `handleSubmit` 拆成 `validate`、`request`、`onSuccess`、`onFail` 等多层，每段不足 5 行且仍须顺序阅读才能理解「提交」全流程。
- **语义空心命名**：`handleData`、`processItem`、`doAction` 等无法表达业务动作的函数名（命名规范见 **6.3**）。
- **配置与编排混淆**：`list.config.ts` 只放表格/表单/搜索等**静态配置**；不要把单次页面编排（如 `onMounted` 里拉字典再拉列表）塞进 config。
- **提前公共化**：仅当前页面使用、且无单函数超长问题，不要抽到 `utils/`「以备后用」。

#### 方法即动作

- 一个方法对应**一个完整业务动作**（如 `handleSubmit`、`fetchList`、`openEditDialog`），不在此动作内再拆仅一步的私有方法。
- 允许在动作内部用**局部变量、早 return、平铺 if/else** 表达分支；避免为减少嵌套而制造多个 3 行小函数。
- 事件处理优先 `handleXxx`；查询/副作用用 `fetchXxx` / `loadXxx`（与 **6.3** 一致）。

#### 推荐落点

| 范围 | 位置 | 示例 |
| ---- | ---- | ---- |
| 仅当前 `.vue` 使用 | 同文件 `<script setup>` 内函数 | `buildQueryParams()` |
| 当前页面多个子组件共用 | 父 `.vue` `<script setup>` | `normalizeRow()` |
| 项目内多页复用**业务逻辑** | `composables/use-xxx.ts` | `useListTableEmpty()` |
| 项目内多页/全局**纯方法** | `utils/` | `formatDateTime()`、`downloadBlob()` |

#### 禁止事项

- 为通过 Lint/行数检查而**机械拆函数**，导致调用链变长、收益为负。
- 将页面状态（`ref`、路由、`store`）传入深层 utils，使工具函数依赖页面上下文（应放在 `composables` 或留在页面）。
- 在 `api/` 中写 UI 反馈、路由跳转等视图层逻辑。

#### 评审检查点

- 抽离后，**单条用户路径**是否仍能在 1～2 个函数内读完？
- 新函数是否会被**第二处**调用；若不会，是否应内联回去？
- 放入 `utils/` 的函数是否**无**组件/路由/store 依赖？业务编排是否误放在 `utils/` 而非 `composables/`（见 **3.7**）？
- 与 **5.3 组件抽离** 是否重复劳动（大段模板应拆组件，而非拆一堆只服务模板的脚本函数）。

### 5.5 `uni-lib` 规范

- 业务组件库名称统一为 `uni-lib`
- 技术栈统一为 `Vue 3 + Element Plus`
- 对外组件统一使用 `Uni` 前缀，如 `UniDataTable`
- 目录名统一使用 `uni-xxx`
- 支持全量安装与按需导入，组件样式应能独立导入
- 可提供 `v-uni-*` 自定义指令、`useUni*` 组合式函数和 `createUni*` 协议层工厂，但不得写死业务项目私有接口、路由、菜单、store 路径
- 权限能力统一采用 `v-uni-permission` 指令 + `hasUniPermission` / `useUniPermission` JS 校验方式，不沉淀按钮专用组件
- `Vue`、`Element Plus`、`Vue Router`、`Pinia` 等宿主依赖应按组件库实际使用声明为 `peerDependencies`
- 文档站为必选交付物
- 发布前必须完成类型检查、构建校验、测试、文档构建与文档更新

---

## 六、命名规范

### 6.1 总则

- 代码标识符统一使用驼峰命名
- 目录和文件统一使用横杆命名
- 命名应语义清晰，原则上避免过长
- 单个标识符尽量控制在 `2` 到 `4` 个语义单词内

### 6.2 参数命名

- 使用 `camelCase`
- 布尔值使用 `is`、`has`、`can` 开头
- 集合使用复数形式
- 避免使用 `data`、`obj`、`info`、`temp` 等泛化命名

示例：

- 推荐：`userId`、`orderList`、`isDisabled`、`pageNo`
- 不推荐：`dataValue`、`obj`、`flag`、`tempInfo`

### 6.3 方法命名

- 使用 `camelCase`
- 方法名以动词开头
- 查询类使用 `get`、`fetch`、`load`
- 新增类使用 `create`、`add`
- 修改类使用 `update`、`edit`
- 删除类使用 `remove`、`delete`
- 提交类使用 `submit`、`save`

示例：

- 推荐：`fetchOrderList()`、`submitForm()`、`removeUser()`
- 不推荐：`handleData()`、`doSubmit()`、`dealInfo()`

### 6.4 目录与文件命名

- 目录统一使用 `kebab-case`
- 文件统一使用 `kebab-case`
- Vue 组件文件同样使用横杆命名
- 组合式函数（**多页复用**）使用 `use-xxx.ts`，放在 `composables/`
- 目录已经表达业务语义时，文件名只表达职责，不重复目录名前缀
- 目录已经表达业务语义时，页面内本地函数和配置函数也只表达职责，不重复业务名前缀

示例：

- 目录：`user-center`、`order`
- 组件文件：`user-table.vue`
- 页面文件：`list.vue`
- 配置文件：`list.config.ts`

### 6.5 代码注释规范

代码注释用于解释业务意图、边界条件和非显而易见的实现原因，不用于重复描述代码本身。

**必须添加注释的场景**：

- 复杂业务规则、权限判断、状态流转、金额计算等仅从代码难以直接理解的逻辑
- 与后端接口、第三方 SDK、浏览器兼容性、跨端差异相关的特殊处理
- 临时方案、历史兼容、灰度逻辑、降级逻辑等需要说明背景和移除条件的代码
- 暴露给其他模块使用的公共函数、组件 Props、指令、组合式函数和工具方法
- 关键类型、枚举、常量的业务含义不够直观时

**注释写法要求**：

- 注释应说明“为什么这样做”和“业务约束是什么”，避免只翻译代码
- 单行注释使用 `//`，放在被说明代码的上一行；避免与代码写在同一行
- 多行说明使用 `/** ... */`，适用于公共 API、复杂逻辑块或类型说明
- `TODO`、`FIXME` 必须写明原因、责任人或关联任务编号，不允许长期无归属保留
- 注释内容应随代码变更同步更新，禁止出现与实现不一致的过期注释

**推荐示例**：

```ts
// 后端分页从 1 开始，前端表格组件内部页码同样保持 1 基准，避免转换误差。
const pageNo = query.pageNo || 1

/**
 * 判断用户是否具备菜单访问权限。
 * 超级管理员直接放行，普通用户按后端返回的权限码校验。
 */
function hasMenuPermission(permissionCode: string) {
  // ...
}
```

**不推荐示例**：

```ts
// 设置 pageNo
const pageNo = query.pageNo || 1

// 调用接口
const result = await fetchOrderList(params)
```

**禁止事项**：

- 使用注释屏蔽废弃代码后长期保留
- 在代码中保留调试说明、临时排查记录、无效 TODO
- 为每一行简单赋值、简单调用添加低价值注释
- 注释中写入账号、密钥、Token、接口签名等敏感信息

---

## 七、样式开发规范

### 7.1 样式方案

- 统一使用 Sass
- 全局样式、变量、mixin、reset、utilities 按职责拆分
- 组件样式默认局部隔离
- 主题变量统一管理，禁止项目中多套变量体系长期并存

### 7.2 样式要求

- 优先使用设计变量，不直接写死大量魔法数值
- 颜色、间距、圆角、层级应使用统一 token
- 页面样式不应过度依赖深层嵌套
- 避免在多个页面复制大段相同样式

---

## 八、状态管理规范

### 8.1 使用原则

- 页面私有临时状态优先使用组件内部状态
- 跨组件共享、跨页面复用、需要持久化的状态再进入 Pinia
- Store 按业务域拆分，避免一个全局大仓库
- Store 中保留状态与动作，不堆积视图层处理代码

### 8.2 禁止事项

- 将所有数据不加区分地放入全局 Store
- 在 Store 中编写大量 DOM 或视图逻辑
- 页面可局部维护的简单状态强行上收至全局

---

## 九、接口开发规范

### 9.1 接口组织

- 接口按业务域拆分，如 `user.ts`、`order.ts`
- 请求参数与响应类型尽量显式声明
- 统一处理鉴权、错误提示、超时和异常兜底

### 9.2 接口调用要求

- 页面和组件优先调用 Service 或已封装接口方法
- 禁止在多个页面重复复制相同请求代码
- 接口方法命名应体现业务语义

---

## 十、质量与提交流程规范

### 10.1 Lint 与格式化

- 必须启用 ESLint、Prettier、Stylelint
- `pre-commit` 执行 `lint-staged`
- 提交前应确保本地无基础校验错误
- 提交代码中禁止保留调试性质的 `console`

`**console` 约束\*\*：

- 禁止提交 `console.log`、`console.dir`、`console.debug`、`console.time`
- 如确有错误上报或运行监控需要，应统一走日志封装，不直接散落在业务代码中
- 临时排查问题使用的 `console` 必须在提交前删除

**强制格式化范围**：

- `*.ts`
- `*.tsx`
- `*.js`
- `*.jsx`
- `*.vue`
- `*.scss`
- `*.css`
- `*.md`
- `*.json`

**强制格式化案例文件**：

- `src/views/order/list.vue`
- `src/components/user-table/user-table.vue`
- `src/api/modules/order.ts`
- `src/assets/styles/theme-vars.scss`
- `README.md`
- `package.json`

以上文件类型在提交前必须经过统一格式化，不允许以“仅改动少量内容”为由跳过格式化。

**建议项目脚本**：

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx,.vue --max-warnings=0",
    "lint:fix": "eslint . --ext .js,.jsx,.ts,.tsx,.vue --fix",
    "lint:style": "stylelint \"src/**/*.{vue,scss,css}\" --allow-empty-input",
    "lint:style:fix": "stylelint \"src/**/*.{vue,scss,css}\" --fix --allow-empty-input",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "type-check": "vue-tsc --noEmit"
  }
}
```

**ESLint 规则示例**：

```js
module.exports = {
  root: true,
  env: {
    browser: true,
    es2022: true,
    node: true,
  },
  extends: [
    "eslint:recommended",
    "plugin:vue/vue3-recommended",
    "plugin:@typescript-eslint/recommended",
    "prettier",
  ],
  parser: "vue-eslint-parser",
  parserOptions: {
    parser: "@typescript-eslint/parser",
    ecmaVersion: "latest",
    sourceType: "module",
  },
  rules: {
    "no-console": ["error", { allow: ["warn", "error"] }],
    "no-debugger": "error",
    "no-unused-vars": "off",
    "@typescript-eslint/no-unused-vars": [
      "error",
      { argsIgnorePattern: "^_", varsIgnorePattern: "^_" },
    ],
    "vue/multi-word-component-names": "off",
  },
};
```

**Prettier 规则示例**（与仓库内 `uni-lib/.prettierrc`、各子项目 `.prettierrc` / `.prettierrc.json`、根目录 `.prettierrc` 保持一致）：

```json
{
  "semi": false,
  "singleQuote": true,
  "trailingComma": "none",
  "printWidth": 100,
  "tabWidth": 2,
  "useTabs": false,
  "vueIndentScriptAndStyle": false,
  "bracketSameLine": true
}
```

**Stylelint 规则示例**：

```js
module.exports = {
  extends: ["stylelint-config-standard", "stylelint-config-recommended-vue"],
  rules: {
    "block-no-empty": true,
    "color-no-invalid-hex": true,
    "declaration-block-no-duplicate-properties": true,
    "selector-class-pattern": null,
  },
};
```

**lint-staged 规则示例**：

```json
{
  "*.{js,jsx,ts,tsx,vue}": ["eslint --fix", "prettier --write"],
  "*.{scss,css}": ["stylelint --fix", "prettier --write"],
  "*.{md,json}": ["prettier --write"]
}
```

**Commitlint 规则示例**：

```js
module.exports = {
  extends: ["@commitlint/config-conventional"],
  rules: {
    "type-enum": [
      2,
      "always",
      [
        "feat",
        "fix",
        "docs",
        "style",
        "refactor",
        "perf",
        "test",
        "build",
        "ci",
        "chore",
        "revert",
      ],
    ],
    "subject-empty": [2, "never"],
  },
};
```

**Husky Hook 示例**：

```sh
npx husky add .husky/pre-commit "npx lint-staged"
npx husky add .husky/commit-msg "npx --no -- commitlint --edit $1"
```

**建议校验顺序**：

1. 本地开发时执行 `lint`、`lint:style`、`type-check`
2. 提交前由 `lint-staged` 自动执行修复和格式化
3. 提交信息由 `commitlint` 校验
4. CI 中至少执行 `lint`、`lint:style`、`type-check`、`build`

**强制校验要求**：

- `eslint` 必须对 `ts`、`tsx`、`js`、`jsx`、`vue` 生效
- `stylelint` 必须对 `scss`、`css`、`vue` 生效
- `prettier` 必须覆盖代码、样式、文档、配置文件
- `no-console`、`no-debugger` 必须开启为错误级别
- CI 中不得跳过 Lint、格式化校验和类型检查
- 本节配置可直接作为项目初始化模板，允许按项目微调，但不得降低约束等级

### 10.2 Git 分支管理规范

#### 环境与分支映射

| 分支      | 环境              | 用途                         |
| --------- | ----------------- | ---------------------------- |
| `main`    | PROD（正式机）    | 与线上一致，仅接受已验收代码 |
| `dev`     | UAT（测试机）     | 集成测试、业务验收           |
| `feat-*`  | 本地 / 可选联调   | 单需求开发，不直接部署       |
| `fix-*`   | 本地 / 可选联调   | 单缺陷修复，不直接部署       |
| `hotfix-*`| 本地 / 可选预发   | 线上紧急修复，不直接部署     |

**原则：** 仅 `dev`、`main` 对应可部署环境；`dev` 用于多人 UAT 集成，`main` 仅接收**已验收的功能分支**（`feat-*` / `fix-*` / `hotfix-*`），**禁止**将 `dev` 合入 `main`。

#### 分支命名

| 类型     | 格式              | 示例                      |
| -------- | ----------------- | ------------------------- |
| 正式     | `main`            | —                         |
| 测试     | `dev`             | —                         |
| 需求     | `feat-<简述>`     | `feat-school-bus-order`   |
| 修复     | `fix-<简述>`      | `fix-login-token-expire`  |
| 紧急修复 | `hotfix-<简述>`   | `hotfix-payment-callback` |

命名要求：小写 + 连字符；可与需求/缺陷单号关联（如 `feat-1234-school-bus`）；分支名与提交 scope 尽量一致。

#### 分支来源与职责

- **`main`**：始终可发布 PROD；禁止直接 push 业务代码（通过 MR/PR 合并）；**只合入已在 UAT 验收通过的功能分支**，不合入 `dev`。
- **`dev`**：UAT 集成与验收；接收多个 `feat-*`、`fix-*` 合并，供并行开发与联调；**可领先 `main`，但不得作为上线来源**——`dev` 上常含多人、多需求、不同上线日期的代码；须**定期从 `main` 同步**已上线基线，避免与 PROD 偏离过久。
- **`feat-*`**：从 `main` 拉出；开发完成后合入 `dev` 做 UAT；UAT 通过后**由该 `feat-*` 分支**合入 `main` 再发 PROD（不是合 `dev`）。
- **`fix-*`**：从待修复目标分支拉出——UAT 问题从 `dev` 拉，线上问题从 `main` 拉（或走 `hotfix-*`）；在 `dev` 验收通过后，**由该 `fix-*` 分支**合入 `main`。
- **`hotfix-*`**：从 `main` 拉出；修复验证后**先合 `main` 发 PROD**，**再合 `dev` 同步**，避免后续发版覆盖修复。

#### 标准工作流

**需求开发（feat）：**

```text
main ──► feat-xxx ──► dev（UAT 集成验收）
              │
              └──────► main（PROD 上线，UAT 通过后，合 feat-xxx 而非 dev）
```

1. `git checkout -b feat-xxx main`
2. 在 `feat-xxx` 开发、自测、提交
3. 将 `feat-xxx` 合并到 `dev`，部署 UAT，业务验收（`dev` 上可能同时存在他人未上线需求）
4. 验收通过：将**同一 `feat-xxx` 分支**合并到 `main`，部署 PROD（**禁止**将 `dev` 合入 `main`）
5. 删除已合并的 `feat-xxx`

**缺陷修复（fix）：**

```text
dev ──► fix-xxx ──► dev（UAT 复测）
            │
            └──────► main（PROD 上线，复测通过后，合 fix-xxx 而非 dev）
```

1. `git checkout -b fix-xxx dev`（线上问题则从 `main` 拉）
2. 修复、自测、提交
3. 将 `fix-xxx` 合并到 `dev`，部署 UAT，复测
4. 通过后：将**同一 `fix-xxx` 分支**合并到 `main`，部署 PROD（**禁止**将 `dev` 合入 `main`）
5. 删除 `fix-xxx`

**线上紧急修复（hotfix）：**

```text
main ──► hotfix-xxx ──► main（PROD）──► dev（同步）
         修复            先发 PROD        避免 dev 丢修复
```

1. `git checkout -b hotfix-xxx main`
2. 修复后在预发或小流量验证（若有）
3. 先合并 `main` 并发布 PROD
4. 再合并回 `dev`
5. 删除 `hotfix-xxx`

#### 合并规则

| 场景       | 合并方向              | 说明                                       |
| ---------- | --------------------- | ------------------------------------------ |
| 需求 UAT   | `feat-*` → `dev`      | UAT 集成验收，不上 PROD                    |
| 需求上线   | `feat-*` → `main`     | 该需求 UAT 通过后，**只合对应 feat 分支**  |
| 修复 UAT   | `fix-*` → `dev`       | UAT 复测                                   |
| 修复上线   | `fix-*` → `main`      | 该修复 UAT 通过后，**只合对应 fix 分支**   |
| 紧急修复   | `hotfix-*` → `main` → `dev` | main 优先，dev 必须同步              |
| 基线同步   | `main` → `dev`        | 定期合入，使 dev 包含已上线代码（见下节）  |
| **禁止**   | `dev` → `main`        | dev 含多人并行需求，不得整支合入 main      |

合入 `main` 前，功能分支须 rebase/merge 最新 `main`、解决冲突，并通过 Code Review 与 CI（lint / type-check / test）。`main` 合并后建议打 tag（如 `v1.2.3`）便于 PROD 回滚。

#### main → dev 定期同步

`dev` 不对齐 `main` 是常态（dev 含未上线需求），但 **PROD 已发布的代码须定期回流到 `dev`**，否则 UAT 环境与线上基线差距过大，联调、修 bug、新 feat 拉分支时冲突会累积。

**触发时机（满足任一即同步）：**

- 每次有 `feat-*` / `fix-*` / `hotfix-*` 合入 `main` 并发布 PROD 后
- 固定节奏（建议每周至少 1 次，或 sprint 结束前）
- `dev` 与 `main` 差异过大、多人反馈 UAT 与线上行为不一致时

**操作方式：**

```bash
git checkout dev
git pull origin dev
git merge origin/main   # 或 rebase，团队择一统一；merge 保留 dev 上未上线提交
# 解决冲突 → CI 通过 → push dev → 按需重新部署 UAT
```

**说明：**

- 这是 **`main` → `dev` 单向同步**，与禁止的 **`dev` → `main`** 发版方向相反，二者不冲突
- 同步后 `dev` 仍可能领先 `main`（含未上线需求），但应 **至少包含** 当前 PROD 全部代码
- hotfix 合入 `main` 后须 **立即** 同步到 `dev`，不可只等定期同步
- 冲突以保留 `dev` 上未上线功能、同时吸收 `main` 已上线修复为原则，由当次值班或分支负责人解决

#### 发布流程

```text
                    ┌──► dev（UAT 集成验收，多人并行）
feat-xxx / fix-xxx ─┤
                    └──► main（PROD 发布，仅合已验收的功能分支）
```

1. 开发完成 → 将 **`feat-*` / `fix-*`** 合入 `dev` → 部署 **UAT** → 测试 / 业务验收
2. 该分支验收通过 → 将 **同一功能分支** 合入 `main` → 部署 **PROD**（或先灰度再全量）
3. **`dev` 不对齐 `main`**：`dev` 是集成测试场，可含未上线需求；**禁止**用 `dev` → `main` 做发版；**须定期 `main` → `dev` 同步**（见上节）
4. 若需上线前再验，应使用独立预发环境，由待上线的功能分支或 `main` 部署，而非整支 `dev`

#### 分支与提交类型对应

| 分支               | commit type | 示例                              |
| ------------------ | ----------- | --------------------------------- |
| `feat-*`           | `feat`      | `feat(bus): 新增校车订单列表`     |
| `fix-*` / `hotfix-*` | `fix`     | `fix(auth): 修复 token 过期未跳转` |
| 文档 / 配置        | `docs` / `chore` | `docs: 更新部署说明`         |

#### 禁止事项

- 不在 `main` 上直接开发或直接 push
- **禁止 `dev` 合入 `main`**（dev 含多人并行、不同上线日期的代码）
- 不跳过 `dev` UAT 就将 `feat-*` 合入 `main`（hotfix 除外）
- 不把长期未合并的分支堆在 `dev` 而不清理
- hotfix 合入 `main` 后不同步 `dev`
- 用 `fix-*` 夹带无关需求（应新开 `feat-*`）

### 10.3 Git 提交规范

提交格式：

```text
<type>(<scope>): <subject>
```

常用前缀：

| 前缀       | 含义           |
| ---------- | -------------- |
| `feat`     | 新功能         |
| `fix`      | 缺陷修复       |
| `docs`     | 文档更新       |
| `style`    | 样式或格式调整 |
| `refactor` | 重构           |
| `perf`     | 性能优化       |
| `test`     | 测试相关       |
| `build`    | 构建或依赖调整 |
| `ci`       | CI/CD 调整     |
| `chore`    | 杂项维护       |
| `revert`   | 回滚提交       |

示例：

```text
feat(user): add login page
fix(order): fix order status display
docs(architecture): update development standard
```

### 10.4 提交要求

- 一个提交尽量只表达一个核心意图
- 文档更新使用 `docs`
- 单纯格式调整使用 `style`
- 重构但不改功能使用 `refactor`
- 提交说明避免过长，直接描述核心变更
- 提交前必须移除无效 `console`、临时注释、调试断点和废弃代码片段

---

## 十一、测试与验收规范

### 11.1 测试要求

- 工具函数、纯逻辑模块优先补单元测试
- 核心交互组件应具备基础组件测试
- 关键业务流程应有 E2E 覆盖
- 修复线上问题时，优先补充针对性验证

### 11.2 验收清单

- 功能符合需求
- 无明显控制台报错
- 无基础 Lint / 类型错误
- 命名、目录、分层符合规范
- 复用能力判断合理，组件与方法抽离无明显「为拆而拆」
- 提交信息符合规范

---

## 十二、明确禁止事项

- 私自新增与规范无关的顶层目录
- 在 `utils/`、`api/` 中引用 `.vue` 文件
- 在 `utils/` 中编写依赖 `ref`、路由、store 或页面上下文的逻辑（应使用 `composables/` 或页面内 `use-*.ts`）
- 在 `composables/` 中堆积全局纯工具函数（应下沉 `utils/`）
- 复制 `uni-lib` 源码到业务项目中直接修改
- 业务应用中自建组件滥用 `Uni` 前缀
- 同一类命名同时混用驼峰、下划线、横杆
- 提交包含调试用 `console`、`debugger`、临时注释
- 页面或组件职责过重却长期不拆分
- 在多个页面复制粘贴相同逻辑而不抽离
- 无复用、无降复杂度收益的方法抽离（增加跳转阅读成本）
- 拆碎步骤函数（**所有方法**适用）：`attach*` / `ensure*` 等仅调用一次且有效逻辑 <5 行（见 **5.4**）
- 无谓参数/常量抽离：单处引用却建 constants/config/对象表（见 **5.4**，**所有参数定义抽取**）
- 管理后台使用 `api/constants.ts`、`API_PATHS` 集中维护 REST 前缀（见 **4.4** / **uni-review-web §13**）

---

## 十三、执行原则

- Agent 协作使用用户级 Skill（随 Cursor 账号）：`uni-review-web`（管理后台）、`uni-review-mobile`（H5/小程序）、`uni-review-pc`（官网）；见 `~/.cursor/skills/README.md` 与仓库 `.agents/skills/README.md`
- 规范默认强制执行，特殊情况需评审说明
- 新项目必须严格按模板落地
- 存量项目可渐进治理，但新增代码必须遵循本规范
- 代码评审、CI 校验、发布流程应共同保障规范落地
