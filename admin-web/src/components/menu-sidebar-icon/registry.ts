/**
 * 与 `uni-ui-lib` 内置 `uniIconMap` / `uniIconNames` 同源，
 * 保证后台图标选择器与 `UniLayout` 侧栏 `resolveIcon` 一致。
 */
export { uniIconMap as menuSidebarIconMap, uniIconNames as menuSidebarIconNames } from 'uni-ui-lib'
