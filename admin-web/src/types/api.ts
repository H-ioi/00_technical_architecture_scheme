/**
 * 列表分页与接口信封类型统一定义在 uni-ui-lib；此处仅为模板项目的汇聚导出，
 * 业务模块继续从 `@/types/api` 引入即可，避免在本文件重复声明形状。
 */
export type {
  UniApiEnvelope as ApiEnvelope,
  UniPageQuery as PageQuery,
  UniPageResult as PageResult
} from 'uni-ui-lib'
