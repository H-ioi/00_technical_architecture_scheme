# 前端架构方案总览

本文档用于汇总当前前端建设方案，统一说明多端技术架构、工程规范、目录边界、组件库联动方式与落地路线，作为团队在项目启动、方案评审和架构对齐时的总览依据。

---

## 一、建设目标

- 统一前端技术选型，减少项目间割裂与重复建设
- 明确 PC 端、移动端、原生端的职责边界与适用场景
- 统一工程化基础设施、状态管理、API 封装和样式体系
- 明确业务应用、组件库、文档站之间的协作关系
- 通过规范、脚手架和 CI 保障方案可执行、可复用、可演进

---

## 二、总体架构

### 2.1 端形态划分

前端整体采用“多端分治、基础能力统一”的建设思路，按业务场景拆分为以下几类：

| 端类型     | 子类型        | 推荐技术栈                                       | 适用场景                           | 模板目录                |
| ---------- | ------------- | ------------------------------------------------ | ---------------------------------- | ----------------------- |
| PC Web     | 管理后台      | Vue 3 + Vite + Vue Router + Element Plus + Pinia | 内部运营平台、管理控制台、数据看板 | `admin-web`             |
| PC Web     | C 端站点      | Nuxt 3 + Vue 3 + TypeScript + Sass（Pinia 按需） | 官网、商城、内容门户、会员中心     | `customer-web`          |
| 移动端跨端 | H5 / 小程序   | uni-app + Vue 3（Vite）                          | 营销页、活动页、简单业务功能页     | `mobile-cross-platform` |
| 原生应用   | iOS / Android | Flutter                                          | 高性能交互、深度原生能力场景       | `native-app`            |

业务组件库（`lg-lib`，`Vue 3 + Element Plus`）主要服务 **管理后台类**及需要表格/表单中台能力的场景；**PC C 端站点（如官网、门户）不强依赖 Element Plus / lg-lib**，可按品牌与体验自选 UI 方案（当前仓库 `customer-web` 案例模版未接入组件库）。

### 2.2 核心架构原则

- PC 端拆分为管理后台和 C 端站点，避免一套架构同时兼顾后台效率与前台体验
- 仅 C 端站点使用 Nuxt 3，以支持 SEO、首屏性能和混合渲染能力
- 管理后台采用 Vue 3 CSR 方案，优先保证开发效率、权限体系和表单能力
- 多端共享通用基础设施，包括构建规范、状态管理、API 封装、工具函数与样式规范
- UI 层按端独立实现，业务逻辑、类型定义、服务层优先复用

### 2.3 通用基础设施

统一基础设施包括：

- 统一构建体系：以 Vite 为核心
- 统一状态管理：Pinia
- 统一样式体系：Sass + 设计变量
- 统一 API 客户端封装与错误处理
- 统一工具函数、类型定义与通用业务抽象
- 可选微前端基座能力，用于大型平台渐进式演进

---

## 三、技术架构总览

### 3.1 PC 管理后台

**推荐技术栈**

- Vue 3
- Vite
- Vue Router
- Element Plus
- Pinia
- TypeScript
- Sass

**架构定位**

- 以 CSR 为主
- 强调权限控制、菜单路由、表单页、表格页和后台工作台效率
- 适合作为标准化脚手架模板长期复用

**推荐目录骨架**

```bash
src/
├── api/
├── assets/
├── components/
├── composables/
├── directives/
├── layouts/
├── plugins/
├── router/
├── stores/
├── types/
├── utils/
├── views/
├── App.vue
└── main.ts
```

### 3.2 PC C 端站点

**推荐技术栈**

- Nuxt 3
- Vue 3
- TypeScript（首推）
- Sass
- Pinia：**可选**；当前团队模版仓库 `customer-web` **暂不接入**，以页面 + `composables` + 服务端接口为主，后续有全局状态时再接入

**架构定位**

- 以 SEO、首屏加载和内容分发能力为重点
- 使用 Nuxt 约定式路由和混合渲染能力
- 适合官网、内容门户、商城与会员类站点

**目录骨架（以仓库 `customer-web/src` 落地为准）**

以下为当前案例模版实际结构；未出现的目录（如 `stores/`、`plugins/`）表示模版阶段未初始化，可按业务按需补充，**不以泛化骨架强行对齐空目录**。

```bash
src/
├── app.vue
├── assets/           # 全局样式与设计变量（如 styles/_vars.scss）
├── components/       # 应用内组件（文件命名 kebab-case，见 §7.2 补充）
├── composables/      # 组合式封装（如 use-xxx.ts）
├── layouts/
├── pages/
├── server/           # Nitro：api/、utils/（模拟或转发数据）
```

### 3.3 移动端跨端

**uni-app + Vue 3（Vite）**

- 用于微信小程序、H5 等多端场景（可按需扩展其它小程序平台）
- 适合营销页、活动页和轻量业务功能
- 强调快速上线与多端一致性；路由与页面配置遵循 `pages.json` 约定
- 工程侧与团队统一基础设施对齐：**构建以 Vite 为主**（与总体「以 Vite 为核心」一致）
- 在同功能对照样例下，**微信小程序打包产物体积通常显著小于 Taro**（团队曾在 `test/taro-mini` 与 `test/uni-mini` 做过首页+列表页对照构建，可参考该目录复测）

### 3.4 原生应用

**Flutter**

- 用于核心业务能力、高性能体验和原生深度集成
- 状态管理建议采用 BLoC / Cubit
- 必要时保留 Flutter Web 作为补充能力

---

## 四、工程化与基础规范

### 4.1 统一工程栈

| 类别     | 推荐选型                                                   |
| -------- | ---------------------------------------------------------- |
| 语言     | TypeScript                                                 |
| 构建     | Vite                                                       |
| 状态管理 | Pinia（管理后台必选；**C 端 Nuxt 按需**，当前 `customer-web` 未接入） |
| 路由     | Vue Router / Nuxt Pages / uni-app 页面路由（`pages.json`） |
| 样式     | Sass                                                       |
| 代码规范 | ESLint + Prettier + Stylelint                              |
| 提交规范 | Commitlint + Husky + lint-staged                           |
| 单元测试 | Vitest                                                     |
| 组件测试 | Testing Library                                            |
| E2E 测试 | Playwright                                                 |
| 监控     | Sentry + Lighthouse + RUM                                  |

**落地说明（`customer-web`）**：仓库已配置 **TypeScript**、**ESLint**、**Prettier**、**Stylelint**（当前脚本校验 `src/**/*.scss`；Vue 内联样式可后续扩展）。与本表其它条目（Vitest、Husky 等）一致的完整门禁可按项目阶段增量接入。

### 4.2 API 分层

**管理后台（Vue SPA）**推荐采用：

```text
API Client -> Service Layer -> Store / Component
```

约束如下：

- API Client 负责请求实例、拦截器、认证注入、统一错误处理
- Service Layer 负责业务域接口封装
- Store / Component 只消费服务能力，不直接散落请求实现

**PC C 端（Nuxt，`customer-web` 以当前代码为准）**：服务端路由由 **Nitro `server/api`** 提供数据或与后端通信；前端通过 **`useFetch` / `useAsyncData` + `composables`** 调用，**不强制**等同于上述三层目录（无独立 `src/api` Client 目录亦可）。若后续接入 Pinia，再由 composable/service 收敛请求实现即可。

### 4.3 状态管理策略

- PC **管理后台**统一使用 Pinia
- **C 端站点（Nuxt）**：需要跨页共享状态时可接入 Pinia；**当前仓库 `customer-web` 模版未接入**（见 §3.2），默认以 `composables` + 服务端数据请求为主
- uni-app 端采用适配后的 Pinia 方案
- Flutter 端独立使用 BLoC / Cubit，但接口契约与前端服务层保持一致

### 4.4 样式规范

- 全局样式采用 Sass
- 通过设计变量、mixins、reset、utilities 形成统一样式基础层
- 响应式断点统一定义，避免各项目零散维护
- 组件样式默认隔离，主题变量统一收敛管理

---

## 五、目录与代码分层原则

### 5.1 业务应用目录边界

业务应用目录必须围绕职责划分，重点原则如下：

- `api/` 仅承载接口层与契约相关类型
- `components/` 放应用内复用组件，不承载可发布 npm 的标准件
- `composables/` 放组合式逻辑复用，禁止再平行维护 `hooks/`
- `router/`、`stores/`、`utils/`、`types/` 各自保持单一职责
- `views/` 只作为页面编排层，避免沉淀大量可复用 UI

### 5.2 抽离判定顺序

遇到复用逻辑或 UI 时，按以下顺序判断：

1. 若含明显业务领域语义，优先留在业务页面或业务模块内部
2. 若在多个页面复用，抽到应用内 `components/` 或 `composables/`
3. 若将被多个独立项目复用，再进入 npm 业务组件库
4. 若为纯函数，则归入 `utils/`

### 5.3 明确禁止

- 在页面层堆积超大单文件而不拆分
- 在 `api/`、`utils/` 中引用 `.vue` 组件
- 复制组件库源码到业务应用中直接修改
- 将通用能力长期放在 `views/` 中跨页面引用
- 在业务应用自建组件上使用组件库专属命名空间

---

## 六、组件库与脚手架联动

### 6.1 基本关系

整体建议采用“三层协作”：

- 业务应用仓库：承载具体业务页面和业务流程
- 业务组件库仓库：沉淀跨项目复用组件，发布 npm 包
- 文档站：承载组件说明、示例、版本兼容与接入指南

### 6.2 组件库定位

业务组件库应独立仓库维护，并同时交付：

- npm 包
- 文档站
- 可选 playground

组件库适合沉淀权限按钮、表格、表单、搜索区、复合弹窗等跨项目标准件，不应直接耦合某个业务应用的 store、路由名或接口路径。

### 6.3 组件命名约定

- 对外发布的 Vue 组件统一使用 `Lg` 前缀
- 目录名使用 `lg-xxx`
- 业务应用 `src/components` 下自建组件不得使用 `Lg` 前缀

### 6.4 与脚手架的联动方式

- 脚手架负责生成标准目录结构、Vite 配置、别名、环境变量、Lint 规则和 Git 钩子
- 业务应用通过 `package.json` 依赖业务组件库
- 组件库应将 Vue、Vue Router、Pinia、Element Plus 等声明为 `peerDependencies`
- 主题变量统一由组件库输出，脚手架负责注入与覆盖
- 本地联调可使用 `link`、workspace 或 `file:` 依赖，但发版前必须以正式版本重新验证

---

## 七、规范治理与交付要求

### 7.1 规范治理

- 使用脚手架模板固化标准目录与工程配置
- 使用 Code Review 审查目录归属、抽离边界和组件命名
- 使用 CI 校验构建、Lint、类型检查、测试和文档构建
- 将文档更新纳入组件库发版门禁

### 7.2 命名约定

命名原则统一为：**语义清晰、长度适中、见名知意，原则上避免过长**。为避免混乱，统一采用以下口径：

- **代码标识符**统一使用驼峰命名
- **目录和文件名**统一使用横杆命名
- **组件名 / 类型名**作为代码符号时可使用大驼峰，但不作为目录和文件命名规则

#### 命名规则总表

| 类型     | 命名方式     | 基本要求                                            | 推荐示例                                              | 不推荐示例                                          |
| -------- | ------------ | --------------------------------------------------- | ----------------------------------------------------- | --------------------------------------------------- |
| 参数命名 | `camelCase`  | 优先业务语义；布尔值用 `is`/`has`/`can`；集合用复数 | `userId`、`orderList`、`isDisabled`                   | `dataValue`、`obj`、`flag`                          |
| 方法命名 | `camelCase`  | 动词开头，直接表达动作；避免空泛动词                | `fetchOrderList()`、`submitForm()`、`removeUser()`    | `handleData()`、`doSubmit()`、`dealInfo()`          |
| 目录命名 | `kebab-case` | 按职责或业务域命名，尽量短词组                      | `user-center`、`order-list`、`search-form`            | `userCenterModule`、`allOrderManageList`            |
| 文件命名 | `kebab-case` | 所有落盘文件统一横杆命名，按职责区分                | `user-table.vue`、`use-user-list.ts`、`auth-guard.ts` | `UserTable.vue`、`DoOrderList.ts`、`final-utils.ts` |

#### 统一控制原则

| 规则         | 说明                                                        |
| ------------ | ----------------------------------------------------------- |
| 避免过长     | 单个标识符尽量控制在 `2` 到 `4` 个语义单词内                |
| 避免泛化     | 不使用 `data`、`info`、`obj`、`temp` 这类弱语义命名         |
| 避免重复     | 名称中不重复表达同一含义，如 `userUserList`                 |
| 避免无效后缀 | 少用 `final`、`new`、`manager`、`util` 这类无实际区分度后缀 |
| 优先统一     | 同一类场景使用同一套动词和结构，不混用多个表达              |

#### 参数命名

- 使用 `camelCase`
- 优先使用业务语义名，避免 `data`、`item`、`obj` 这类泛化命名
- 布尔值使用 `is`、`has`、`can` 开头
- 集合使用复数形式
- 事件参数优先体现来源或含义，如 `userId`、`formData`、`pageNo`

**示例**：

- 推荐：`userId`、`orderList`、`isDisabled`、`pageSize`
- 不推荐：`idValue`、`listDataValue`、`flag`、`paramsObj`

#### 方法命名

- 使用 `camelCase`
- 方法名以动词开头，表达动作或结果
- 查询类用 `get`、`fetch`、`load`
- 新增类用 `create`、`add`
- 修改类用 `update`、`edit`
- 删除类用 `remove`、`delete`
- 提交类用 `submit`、`save`
- 初始化类用 `init`
- 布尔判断类用 `is`、`has`、`can`

**示例**：

- 推荐：`getUserInfo()`、`fetchOrderList()`、`submitForm()`、`canEditOrder()`
- 不推荐：`userInfoHandle()`、`doSubmitData()`、`check()`、`dealOrder()`

#### 目录命名

- 统一使用 `kebab-case` 或已有约定的固定复数目录名
- 目录名优先短词或词组，不叠加无意义修饰词
- 按职责命名，不按个人习惯命名
- 复用目录保持稳定，如 `components`、`composables`、`utils`、`stores`

**示例**：

- 推荐：`user-center`、`order-list`、`search-form`、`api`、`views`
- 不推荐：`userCenterModule`、`allOrderManageList`、`commonFuncFolder`

#### 文件命名

- 所有文件统一使用 `kebab-case`
- Vue 组件文件也使用横杆命名，如 `user-table.vue`
- **Nuxt（`customer-web`）**：组件文件同上（如 `example-card.vue`）；模板里仍可按 Nuxt 约定书写 **PascalCase 标签**（如 `<ExampleCard />`），与自动导入一致，不视为与「文件 kebab-case」冲突
- 组合式函数文件建议使用 `use-xxx.ts`
- Store 文件优先按业务域命名，如 `user.ts`、`order.ts`
- API 文件按业务域命名，避免出现 `api-list.ts`、`request-util-final.ts` 这类无效后缀

**示例**：

- 组件文件：`user-table.vue`、`order-search-form.vue`
- 逻辑文件：`use-user-list.ts`、`auth-guard.ts`
- 接口文件：`user.ts`、`order.ts`
- 样式文件：`theme-vars.scss`、`page-header.scss`

#### 统一案例

以“用户订单列表”模块为例：

- 目录：`views/order-list/`
- 页面文件：`order-list-page.vue`
- 子组件：`order-filter-form.vue`
- API 文件：`api/modules/order.ts`
- Store 文件：`stores/order.ts`
- 组合式函数：`composables/use-order-list.ts`
- 方法名：`fetchOrderList()`、`submitSearch()`、`removeOrder()`
- 参数名：`orderId`、`queryParams`、`isExporting`、`pageNo`

控制规则：

- 单个标识符尽量控制在 `2` 到 `4` 个语义单词内
- 能用 `userId` 就不用 `currentSelectedUserIdValue`
- 能用 `fetchOrderList` 就不用 `getOrderListDataByCondition`

### 7.3 Git 与提交流程

- 提交信息遵循 Conventional Commits
- `pre-commit` 执行 `lint-staged`
- 团队在主干、发布、功能分支上执行统一协作策略

**提交格式**：

```text
<type>(<scope>): <subject>
```

**示例**：

```text
feat(user): add login page
fix(order): fix order status display
docs(architecture): update naming convention
refactor(api): simplify request wrapper
```

**提交前缀类型**：

| 前缀       | 说明             | 使用场景示例                           |
| ---------- | ---------------- | -------------------------------------- |
| `feat`     | 新功能           | 新增页面、新增模块、新增业务能力       |
| `fix`      | 缺陷修复         | 修复 bug、兼容性问题、异常处理         |
| `docs`     | 文档更新         | 修改方案文档、README、注释说明         |
| `style`    | 纯样式或格式调整 | 不影响逻辑的样式、格式、排版调整       |
| `refactor` | 重构             | 不改变功能前提下优化结构或实现         |
| `perf`     | 性能优化         | 优化渲染、接口调用、打包体积、加载性能 |
| `test`     | 测试相关         | 新增或调整单元测试、组件测试、E2E 测试 |
| `build`    | 构建或依赖调整   | 修改构建配置、打包流程、依赖版本       |
| `ci`       | CI/CD 配置调整   | 修改流水线、发布脚本、自动化检查       |
| `chore`    | 杂项维护         | 非业务功能变更，如脚本、配置、小型维护 |
| `revert`   | 回滚提交         | 回退某次历史提交                       |

**使用要求**：

- `subject` 使用简短英文短语或团队约定统一语言，避免过长
- `subject` 只描述本次变更核心，不堆砌实现细节
- 一个提交尽量只表达一类变更意图，避免同时出现功能、重构、格式化混合提交
- 文档类更新优先使用 `docs`
- 样式微调且不涉及逻辑时使用 `style`
- 依赖升级、脚本调整、工程小修正优先归入 `build`、`ci` 或 `chore`

### 7.4 文档站要求

组件库文档站为必选交付物，至少应包含：

1. 快速开始
2. 主题与样式说明
3. 组件目录与 API
4. 更新与兼容说明
5. 与脚手架联动说明

---

## 八、实施路线建议

### Phase 1：基础统一

- 冻结技术选型与端划分口径
- 初始化 PC 管理后台、C 端站点、移动端模板
- 落地统一脚手架、Lint、Git Hooks、环境变量规范
- 建立共享工具、类型、API 封装基础层

### Phase 2：核心沉淀

- 建立业务组件库仓库与文档站
- 完成 API Client、Service Layer、Pinia 基础模块
- 建立设计变量与样式主题机制
- 打通组件库、业务应用与脚手架联动链路

### Phase 3：规范深化

- 建立自动化测试体系
- 建立 CI/CD 与预发、生产部署流程
- 完善性能监控、错误追踪和埋点体系
- 根据团队规模评估是否引入微前端方案

---

## 九、结论

本方案最终形成的统一口径如下：

- PC 端分为管理后台和 C 端站点，其中只有 C 端站点使用 Nuxt 3
- 管理后台标准栈为 `Vue 3 + Vite + Vue Router + Element Plus + Pinia`
- 移动端跨端（H5 / 微信小程序等）标准栈为 **`uni-app + Vue 3（Vite）`**
- 多端共享工程化、状态管理、API 封装、样式规范与质量门禁
- 业务应用、组件库、文档站三者职责分离，通过脚手架和 npm 依赖形成稳定联动
- 通过目录规范、抽离原则、命名约束和 CI 流程保障长期可维护性
