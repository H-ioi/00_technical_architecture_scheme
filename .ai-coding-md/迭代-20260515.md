# 迭代-20260515

## admin-web 活动列表与旧版能力对齐

### 微信提醒

- **检索**：`list.config.ts` 增加 `wechatNotify` 下拉（与旧列表 `yesOrno` 一致）。
- **列表列**：在结束时间之后展示「微信提醒」标签列；`use-list.ts` 中 `formatRows` 规范化 `0/1`；`loadData` 将筛选传给 `getActivityPage`。
- **类型**：`ActivityListParams` 增加 `wechatNotify?: string`。
- **文案**：`activity.wechatNotify`（zh-CN / en）。

### 国际化修复

- `activity` 模块中原两个 **`exportQuestionnaire`** 键后者覆盖前者，导致活动列表「导出问卷答案」按钮实际显示为问卷单条「导出问卷」文案。
- 活动列表工具栏改用 **`exportQuestionnaireByActivity`**；问卷详情/答卷页仍用 **`exportQuestionnaire`**。

### 验证

- `npm run type-check`（admin-web）通过。

### 活动项目编辑页：去 Tab、UniForm 双卡片、只读态

- **`program/edit.vue`**：移除 **`el-tabs`**；**基本信息** / **项目规则** 各一块 **`el-card`** + **`UniForm`**（同一 **`form`**）；封面图、抽奖轮次配额分别用 **`#field-backgroundImage`**、**`#field-programQuotasSlot`** 插槽；**`?mode=view`** 时为 **`mode="view"`**（只读），展示 **编辑** 切换为 **`mode=edit`**；新建页始终可编辑。
- **`program-edit.config.ts`**（新建）：**`buildProgramBasicUniConfig`** / **`buildProgramRuleUniConfig`** 输出 **`UniFormConfig`**，按 **`programType`** 控制抽奖/比赛/祝福字段显隐；提交仍走 **`buildSubmitPayload`**，数据结构不变。
- **已移除**：**`program-form-body.vue`**（逻辑并入编辑页与 config）。
- 验证：`npm run type-check`（admin-web）通过。

### 活动项目列表/详情：对齐考勤流程列表与编辑页风格

- **`program/list.vue`**：`useListTableEmpty` + `ListTableEmpty`、`@request-error`、工具栏 **`density: true`**；命名与 **`activity/list`**、**`attendance/flow` 子面板**一致（`queryModel`、`loadData`、`columns` 等）；批量删除确认框补充 **`confirmButtonText` / `cancelButtonText`**；列表页描述使用与 **`attendance/flow/list`** 相同的次级文案样式（**`activity-program-list__desc`**）。
- **`program/list.config.ts`**（新建）：抽取 **`buildProgramSearchForm`**、**`buildProgramTableColumns`**，列补充 **`type: 'text'`** 等与流程列表一致的字段声明。
- **`program/detail.vue`**：外层 **`activity-program-detail uni-list-page`** 顺序与 **`holiday-flow-design`** 一致；副标题 **`activity-program-detail__page-desc`**；头部操作区 **`align-items: center`**；路由跳转统一 **`void router.push/replace`**。

### 活动项目：与旧版 `program/index` + `modal/form` + `detail` 行为对齐（全屏编辑页）

- **列表**：**`查看`** → **`ActivityProgramDetail`**；**`编辑`** → **`/activity/program/edit/:id`**（**`ActivityProgramEdit`**）；**`新增`** → **`/activity/program/edit/create`**（**`ActivityProgramEditCreate`**）。顶栏参考 **`attendance/flow/edit`**：**保存** + **返回**；主区域为双 **`el-card`** + **`UniForm`**（见上文「活动项目编辑页」），**不再使用 Tab**。
- **详情**：**`编辑`** → 同上编辑路由（**`goEdit`**）。
- **已移除**：**`program-form-dialog.vue`**（改全页 **`program/edit.vue`**）。
- **路由**：`route.activityProgramCreate` / **`route.activityProgramEdit`**（zh/en）；**`menu.ts`** 别名 **`/isacommunity/activity/program/edit/index`** → **`/activity/program/edit`**。
- **接口**：**`POST …/add`**、**`POST …/edit`** 不变。

### 问卷设计器：左题型 / 右折叠 + 拖拽排序

- **`questionnaire-builder.vue`**：布局改为左侧 **`qb__type-grid`（3 列）** 添加题型；右侧题目区为 **`vuedraggable` + 手风琴式折叠**（头部：手柄、展开箭头、序号、类型、题干摘要、必填/隐藏、删除；展开区为原表单配置）。展开动画使用 **`ElCollapseTransition`**；保留 debounce 回填与 `flushDraft` 逻辑。
- **依赖**：`admin-web` 增加 **`vuedraggable@^4.1.0`**。
- **文案**：`activity.qbListHint`、`qbEmpty`（zh/en）；新增 **`qbDrag`**（zh/en）。
- **选项区**：单选/多选/下拉的 **`qb__opt-block`** 为 **单列**，每行 **仅「选项文案」一个输入框**（不再编辑独立 value）；保存时 **`template-codec`** 若 **`value`** 为空则回填 **`label`**。



### 问卷详情（查看）：档案 + 题目只读一览

- **`questionnaire/detail.vue`**：页头 **`qDetailPageDesc`**；卡片内分块 **「问卷信息」**（原 `el-descriptions`）与 **「题目信息（只读）」**（`listByOuterId` + `templateDetail` → `deserializeTemplateFields`，有序列表展示序号、题型、题干、必填/隐藏）；无模板时 **`qDetailNoTemplate`** 空态。档案加载完成后再加载模板，题目区独立 **`v-loading`**。
- **文案**：`activity.qDetailPageDesc`、`qDetailMetaSection`、`qDetailQuestionsSection`、`qDetailNoTemplate`（zh/en）。

### Vite：`vuedraggable` 与 Vue 运行时

- **`vite.config.ts`**：`resolve.alias.vuedraggable` → **`node_modules/vuedraggable/src/vuedraggable.js`**（官方 `module` 指向 UMD，预构建会导致 `defineComponent` 中报 **`isFunction is not a function`**）；`resolve.dedupe` 增加 **`@vue/shared`**。**改后请删掉 `node_modules/.vite` 或执行 `npm run dev:force` 刷新依赖缓存。**

### 活动详情/编辑：内嵌 Tab（对齐旧版）

- **`detail/detail.vue`**：`el-tabs` 挂载「基本信息」+ 有 `id` 时的子 Tab（项目、问卷、报名、签到、获奖、投票、祝福、反馈）；`?tab=` 与路由同步；查看态增加 **「编辑」**（`busdriver_edit`）。
- **Tab 组件**（`detail/tabs/*`）：项目卡片列表（`pickList` + `normalizeEnvelope` 详情）；问卷 `UniDataTable`（跳转详情/设计）；其余子表为 **只读分页**，接口与旧 admin 一致。
- **API**：`activity.ts` 增加 `ticketPage`、`checkinPage`；新增 `activity-feedback`、`activity-blessing`、`activity-vote-record`、`activity-prize-award` 并在 `api/index.ts` 导出。
- **文案**：`activity.detailTab*`、`regCol*`、`winnerKind*` 等（zh/en）；更新 **`viewDetailHint`**。

### 活动详情页眉：说明文案 + 报名人数操作置顶

- 参考 **`attendance/flow/edit`**：标题下增加 **`activity-detail-page__desc`**（`detailPageDesc*`，随 `activeTab` 切换）；页眉右侧 **`align-items: center`**，**竖向分割线** 分隔主按钮与「返回」。
- **报名人数**：页眉仅保留 **新增 / 导入 Excel / 下载模板**（编辑态 + 当前 Tab）；**批量删除** 放在 **`UniDataTable` 左下角工具栏**（`#toolbar` slot，与刷新等同一行）；`TicketsTab` 通过 **`defineExpose`** 供父页触发导入与模板下载。



- **`api/activity.ts`**：`ticketAdd` / `ticketEdit` / `ticketRemove`（`del?ids=`）、`ticketTemplate`（blob）、`ticketImport`（`FormData` + 头 `VERSION: B`）。
- **`detail/components/ticket-form-dialog.vue`**：新增/编辑弹窗；`parentId` 校验与旧版 Long / 精度处理一致。
- **`detail/tabs/tickets-tab.vue`**：`UniSearchForm` + `UniDataTable`；页眉动作由父页调用 **`openAdd` / `triggerImport` / `downloadTpl`**；表格 **`#toolbar`** 内 **批量删除**（`busdriver_del`）；查看态隐藏写操作。
- **`detail/detail.vue`**：`TicketsTab` 传入 `:read-only="isView"`。
- **文案**：`regSearchPh`、`regForm*`、`confirmDeleteTicket`、`regPaid*`、`regParentId*` 等（zh/en）。

### 活动详情「基本信息」：查看与编辑同一 UniForm

- **`detail/components/basic-form.vue`**：改为 **`UniForm`** + **`#field-*`** 插槽（封面图、可见范围名单上传、报名人数「不限」）；**`props.mode: 'view' | 'edit'`** 控制只读/可编辑；查看态顶部保留 **`viewDetailHint`** 提示；详情加载仍走 **`activityApi.detail.get`**，提交逻辑不变（**`add` / `edit`**）。
- **`detail/components/basic-form.config.ts`**（新建）：**`buildActivityBasicFormSchema`** 输出字段 schema（含 **ID / 创建人** 只读、`activityTime` / `registrationTime` 的 **`viewRender`** 时间区间、`schoolIds` / `emailConfigIds` 查看态拼接文案）。
- **`detail/detail.vue`**：基本信息 Tab 只挂载 **`ActivityBasicForm`**，**`basicFormMode`**（有 `id` 时随路由查看/编辑切换，新建始终 **`edit`**）。
- **移除**：**`basic-view.vue`**（原 **`el-descriptions`** 查看页）。
- 验证：`npm run type-check`（admin-web）通过。

### admin-web：活动模块仅保留问卷管理前端

- **删除**：`views/activity` 下除 `questionnaire/`、`format-labels.ts`、`use-membership-school-options.ts` 外的页面（活动列表、活动详情与各 Tab、项目、奖品、投票、微信/邮箱配置、家长学生关联等）。
- **路由**：`/activity` 默认进入 `/activity/questionnaire`，子路由仅剩问卷列表/详情/设计/答卷。
- **API**：移除仅被上述页面使用的模块（奖项、投票、邮箱/微信配置、项目、签到/反馈等）；`activity.ts` 仅保留问卷下拉所需的 **`listBrief`**；**`activityQuestionnaire`** 不变。
- **常量**：去掉未再引用的 **`API_PATHS`**（项目/奖品/投票/学校微信/学校邮箱）。
- **`api/index.ts`**：活动相关仅导出 **`activityApi`**、**`activityQuestionnaireApi`**。
- **菜单别名**：旧后台活动各 path 统一映射到 **`/activity/questionnaire`**，避免权限菜单跳 404。
- **文案**：`route` 与 **`locales/.../modules/activity`** 去掉已删页面用词，仅保留问卷相关键（zh/en 键一致）。
- **`format-labels`**：仅保留问卷用到的 **`yesNoOptions`**、**`labelForValue`**、**`fmtTs`**。
- **类型**：删除 **`types/modules/activity.ts`**。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷设计器：列表行 / 属性区头部不再重复展示题干

- **`questionnaire-builder.vue`**：中间题目列表每行工具栏仅保留拖拽手柄、序号、题型标签、隐藏标记与删除；去掉列表头里的题干摘要。右侧属性区上下文条仅保留 **`#序号`**、题型标签与必填/隐藏开关，去掉头部题干重复展示。题干仍在下方预览表单与「题干」输入框中编辑。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷设计器：题型单列 + 预览题干加粗

- **`questionnaire-builder.vue`**：左侧「添加题型」**`qb__type-grid`** 改为单列（去掉原 3 列 / 窄屏 2 列）；中间预览题干样式由 **`qb__pv-label`** 承担 **`font-weight: 600`**。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷设计器：中间预览区不用 el-form

- **`questionnaire-builder.vue`**：题目列表内「答题预览」由 **`el-form` / `el-form-item`** 改为 **`qb__pv`** + **`qb__pv-field`** + **`qb__pv-label`** + **`qb__pv-control`** 自定义结构；控件仍为 **`el-input`** / **`el-radio-group`** 等，仅去掉表单框架。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷设计器：题干编辑改为多行

- **`questionnaire-builder.vue`**：右侧属性区「题干」**`draft.label`** 改为 **`el-input type="textarea"`**，**`autosize`** `minRows: 3`、`maxRows: 10`，保留 **`maxlength="255"`** 与 **`show-word-limit`**。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷设计器：属性区选项单列 + 单输入框

- **`questionnaire-builder.vue`**：**`qb__opt-block`** 固定单列；选项行 **`qb__opt--with-default`** 仅保留序号、一个 **`opt.label`** 输入、「默认」、删除（去掉 **`opt.value`** 输入框）。
- **`template-codec.ts`**：**`serializeFieldForApi`** 写入 API 时，若 **`value`** 为空/仅空白则用 **`label`**，避免仅存文案时 **`value`** 仍为空字符串。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷设计器：选项区滚动 + 底部添加 + 删除图标

- **`questionnaire-builder.vue`**：**「+ 添加选项」** 在列表底部（**`qb__opts-add`**）；删除用 **`Delete`** 图标 **`el-button`**，**`:title`** **`qbRemove`**；选项列表使用与中栏 **`el-scrollbar`**（**`qb__scroll qb__scroll--opts`** + **`qb__opts-strip` 右 padding**），窄屏与中栏一起在 **`≤960px`** 收紧 **`el-scrollbar__wrap` 的 max-height**，滚动条观感与题目列表一致（不再用原生 **`overflow-y: auto`**）。
- 验证：`npm run type-check`（admin-web）通过。


