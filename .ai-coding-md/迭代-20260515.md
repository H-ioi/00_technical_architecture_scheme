# 迭代-20260515

## admin-web 活动列表与旧版能力对齐

### 微信提醒

- **检索**：`list.config.ts` 增加 `wechatNotify` 下拉（与旧列表 `yesOrno` 一致）。
- **列表列**：在结束时间之后展示「微信提醒」标签列；`use-list.ts` 中 `formatRows` 规范化 `0/1`；`loadData` 将筛选传给 `getActivityPage`。
- **类型**：`ActivityListParams` 增加 `wechatNotify?: string`。
- **文案**：`activity.wechatNotify`（zh-CN / en）。

### 国际化修复

- `activity` 模块中原两个 **`exportQuestionnaire`** 键后者覆盖前者，导致活动列表「导出问卷答案」按钮实际显示为问卷单条「导出问卷」文案。
- 活动列表工具栏改用 **`exportQuestionnaireByActivity`**；问卷详情/答卷页仍用 **`exportQuestionnaire`**。

### 验证

- `npm run type-check`（admin-web）通过。

### 活动项目编辑页：去 Tab、UniForm 双卡片、只读态

- **`program/edit.vue`**：移除 **`el-tabs`**；**基本信息** / **项目规则** 各一块 **`el-card`** + **`UniForm`**（同一 **`form`**）；封面图、抽奖轮次配额分别用 **`#field-backgroundImage`**、**`#field-programQuotasSlot`** 插槽；**`?mode=view`** 时为 **`mode="view"`**（只读），展示 **编辑** 切换为 **`mode=edit`**；新建页始终可编辑。
- **`program-edit.config.ts`**（新建）：**`buildProgramBasicUniConfig`** / **`buildProgramRuleUniConfig`** 输出 **`UniFormConfig`**，按 **`programType`** 控制抽奖/比赛/祝福字段显隐；提交仍走 **`buildSubmitPayload`**，数据结构不变。
- **已移除**：**`program-form-body.vue`**（逻辑并入编辑页与 config）。
- 验证：`npm run type-check`（admin-web）通过。

### 活动项目列表/详情：对齐考勤流程列表与编辑页风格

- **`program/list.vue`**：`useListTableEmpty` + `ListTableEmpty`、`@request-error`、工具栏 **`density: true`**；命名与 **`activity/list`**、**`attendance/flow` 子面板**一致（`queryModel`、`loadData`、`columns` 等）；批量删除确认框补充 **`confirmButtonText` / `cancelButtonText`**；列表页描述使用与 **`attendance/flow/list`** 相同的次级文案样式（**`activity-program-list__desc`**）。
- **`program/list.config.ts`**（新建）：抽取 **`buildProgramSearchForm`**、**`buildProgramTableColumns`**，列补充 **`type: 'text'`** 等与流程列表一致的字段声明。
- **`program/detail.vue`**：外层 **`activity-program-detail uni-list-page`** 顺序与 **`holiday-flow-design`** 一致；副标题 **`activity-program-detail__page-desc`**；头部操作区 **`align-items: center`**；路由跳转统一 **`void router.push/replace`**。

### 活动项目：与旧版 `program/index` + `modal/form` + `detail` 行为对齐（全屏编辑页）

- **列表**：**`查看`** → **`ActivityProgramDetail`**；**`编辑`** → **`/activity/program/edit/:id`**（**`ActivityProgramEdit`**）；**`新增`** → **`/activity/program/edit/create`**（**`ActivityProgramEditCreate`**）。顶栏参考 **`attendance/flow/edit`**：**保存** + **返回**；主区域为双 **`el-card`** + **`UniForm`**（见上文「活动项目编辑页」），**不再使用 Tab**。
- **详情**：**`编辑`** → 同上编辑路由（**`goEdit`**）。
- **已移除**：**`program-form-dialog.vue`**（改全页 **`program/edit.vue`**）。
- **路由**：`route.activityProgramCreate` / **`route.activityProgramEdit`**（zh/en）；**`menu.ts`** 别名 **`/isacommunity/activity/program/edit/index`** → **`/activity/program/edit`**。
- **接口**：**`POST …/add`**、**`POST …/edit`** 不变。

### 问卷设计器：左题型 / 右折叠 + 拖拽排序

- **`questionnaire-builder.vue`**：布局改为左侧 **`qb__type-grid`（3 列）** 添加题型；右侧题目区为 **`vuedraggable` + 手风琴式折叠**（头部：手柄、展开箭头、序号、类型、题干摘要、必填/隐藏、删除；展开区为原表单配置）。展开动画使用 **`ElCollapseTransition`**；保留 debounce 回填与 `flushDraft` 逻辑。
- **依赖**：`admin-web` 增加 **`vuedraggable@^4.1.0`**。
- **文案**：`activity.qbListHint`、`qbEmpty`（zh/en）；新增 **`qbDrag`**（zh/en）。
- **选项区**：单选/多选/下拉的 **`qb__opt-block`** 为 **单列**，每行 **仅「选项文案」一个输入框**（不再编辑独立 value）；保存时 **`template-codec`** 若 **`value`** 为空则回填 **`label`**。



### 问卷详情（查看）：档案 + 题目只读一览

- **`questionnaire/detail.vue`**：页头 **`qDetailPageDesc`**；卡片内分块 **「问卷信息」**（原 `el-descriptions`）与 **「题目信息（只读）」**（`listByOuterId` + `templateDetail` → `deserializeTemplateFields`，有序列表展示序号、题型、题干、必填/隐藏）；无模板时 **`qDetailNoTemplate`** 空态。档案加载完成后再加载模板，题目区独立 **`v-loading`**。
- **文案**：`activity.qDetailPageDesc`、`qDetailMetaSection`、`qDetailQuestionsSection`、`qDetailNoTemplate`（zh/en）。

### Vite：`vuedraggable` 与 Vue 运行时

- **`vite.config.ts`**：`resolve.alias.vuedraggable` → **`node_modules/vuedraggable/src/vuedraggable.js`**（官方 `module` 指向 UMD，预构建会导致 `defineComponent` 中报 **`isFunction is not a function`**）；`resolve.dedupe` 增加 **`@vue/shared`**。**改后请删掉 `node_modules/.vite` 或执行 `npm run dev:force` 刷新依赖缓存。**

### 活动详情/编辑：内嵌 Tab（对齐旧版）

- **`detail/detail.vue`**：`el-tabs` 挂载「基本信息」+ 有 `id` 时的子 Tab（项目、问卷、报名、签到、获奖、投票、祝福、反馈）；`?tab=` 与路由同步；查看态增加 **「编辑」**（`busdriver_edit`）。
- **Tab 组件**（`detail/tabs/*`）：项目卡片列表（`pickList` + `normalizeEnvelope` 详情）；问卷 `UniDataTable`（跳转详情/设计）；其余子表为 **只读分页**，接口与旧 admin 一致。
- **API**：`activity.ts` 增加 `ticketPage`、`checkinPage`；新增 `activity-feedback`、`activity-blessing`、`activity-vote-record`、`activity-prize-award` 并在 `api/index.ts` 导出。
- **文案**：`activity.detailTab*`、`regCol*`、`winnerKind*` 等（zh/en）；更新 **`viewDetailHint`**。

### 活动详情页眉：说明文案 + 报名人数操作置顶

- 参考 **`attendance/flow/edit`**：标题下增加 **`activity-detail-page__desc`**（`detailPageDesc*`，随 `activeTab` 切换）；页眉右侧 **`align-items: center`**，**竖向分割线** 分隔主按钮与「返回」。
- **报名人数**：页眉仅保留 **新增 / 导入 Excel / 下载模板**（编辑态 + 当前 Tab）；**批量删除** 放在 **`UniDataTable` 左下角工具栏**（`#toolbar` slot，与刷新等同一行）；`TicketsTab` 通过 **`defineExpose`** 供父页触发导入与模板下载。



- **`api/activity.ts`**：`ticketAdd` / `ticketEdit` / `ticketRemove`（`del?ids=`）、`ticketTemplate`（blob）、`ticketImport`（`FormData` + 头 `VERSION: B`）。
- **`detail/components/ticket-form-dialog.vue`**：新增/编辑弹窗；`parentId` 校验与旧版 Long / 精度处理一致。
- **`detail/tabs/tickets-tab.vue`**：`UniSearchForm` + `UniDataTable`；页眉动作由父页调用 **`openAdd` / `triggerImport` / `downloadTpl`**；表格 **`#toolbar`** 内 **批量删除**（`busdriver_del`）；查看态隐藏写操作。
- **`detail/detail.vue`**：`TicketsTab` 传入 `:read-only="isView"`。
- **文案**：`regSearchPh`、`regForm*`、`confirmDeleteTicket`、`regPaid*`、`regParentId*` 等（zh/en）。

### 活动详情「基本信息」：查看与编辑同一 UniForm

- **`detail/components/basic-form.vue`**：改为 **`UniForm`** + **`#field-*`** 插槽（封面图、可见范围名单上传、报名人数「不限」）；**`props.mode: 'view' | 'edit'`** 控制只读/可编辑；查看态顶部保留 **`viewDetailHint`** 提示；详情加载仍走 **`activityApi.detail.get`**，提交逻辑不变（**`add` / `edit`**）。
- **`detail/components/basic-form.config.ts`**（新建）：**`buildActivityBasicFormSchema`** 输出字段 schema（含 **ID / 创建人** 只读、`activityTime` / `registrationTime` 的 **`viewRender`** 时间区间、`schoolIds` / `emailConfigIds` 查看态拼接文案）。
- **`detail/detail.vue`**：基本信息 Tab 只挂载 **`ActivityBasicForm`**，**`basicFormMode`**（有 `id` 时随路由查看/编辑切换，新建始终 **`edit`**）。
- **移除**：**`basic-view.vue`**（原 **`el-descriptions`** 查看页）。
- 验证：`npm run type-check`（admin-web）通过。

### admin-web：活动模块仅保留问卷管理前端

- **删除**：`views/activity` 下除 `questionnaire/` 外的页面（活动列表、活动详情与各 Tab、项目、奖品、投票、微信/邮箱配置、家长学生关联等）。
- **路由**：`/activity` 默认进入 `/activity/questionnaire`，子路由仅剩问卷列表/详情/设计/答卷。
- **API**：移除仅被上述页面使用的模块（奖项、投票、邮箱/微信配置、项目、签到/反馈等）；`activity.ts` 仅保留问卷下拉所需的 **`listBrief`**；**`activityQuestionnaire`** 不变。
- **常量**：去掉未再引用的 **`API_PATHS`**（项目/奖品/投票/学校微信/学校邮箱）。
- **`api/index.ts`**：活动相关仅导出 **`activityApi`**、**`activityQuestionnaireApi`**。
- **菜单别名**：旧后台活动各 path 统一映射到 **`/activity/questionnaire`**，避免权限菜单跳 404。
- **文案**：`route` 与 **`locales/.../modules/activity`** 去掉已删页面用词，仅保留问卷相关键（zh/en 键一致）。
- **类型**：删除 **`types/modules/activity.ts`**。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷设计器：列表行 / 属性区头部不再重复展示题干

- **`questionnaire-builder.vue`**：中间题目列表每行工具栏仅保留拖拽手柄、序号、题型标签、隐藏标记与删除；去掉列表头里的题干摘要。右侧属性区上下文条仅保留 **`#序号`**、题型标签与必填/隐藏开关，去掉头部题干重复展示。题干仍在下方预览表单与「题干」输入框中编辑。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷设计器：题型单列 + 预览题干加粗

- **`questionnaire-builder.vue`**：左侧「添加题型」**`qb__type-grid`** 改为单列（去掉原 3 列 / 窄屏 2 列）；中间预览题干样式由 **`qb__pv-label`** 承担 **`font-weight: 600`**。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷设计器：中间预览区不用 el-form

- **`questionnaire-builder.vue`**：题目列表内「答题预览」由 **`el-form` / `el-form-item`** 改为 **`qb__pv`** + **`qb__pv-field`** + **`qb__pv-label`** + **`qb__pv-control`** 自定义结构；控件仍为 **`el-input`** / **`el-radio-group`** 等，仅去掉表单框架。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷设计器：题干编辑改为多行

- **`questionnaire-builder.vue`**：右侧属性区「题干」**`draft.label`** 改为 **`el-input type="textarea"`**，**`autosize`** `minRows: 3`、`maxRows: 10`，保留 **`maxlength="255"`** 与 **`show-word-limit`**。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷设计器：属性区选项单列 + 单输入框

- **`questionnaire-builder.vue`**：**`qb__opt-block`** 固定单列；选项行 **`qb__opt--with-default`** 仅保留序号、一个 **`opt.label`** 输入、「默认」、删除（去掉 **`opt.value`** 输入框）。
- **`template-codec.ts`**：**`serializeFieldForApi`** 写入 API 时，若 **`value`** 为空/仅空白则用 **`label`**，避免仅存文案时 **`value`** 仍为空字符串。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷设计器：选项区滚动 + 底部添加 + 删除图标

- **`questionnaire-builder.vue`**：**「+ 添加选项」** 在列表底部（**`qb__opts-add`**）；删除用 **`Delete`** 图标 **`el-button`**，**`:title`** **`qbRemove`**；选项列表使用与中栏 **`el-scrollbar`**（**`qb__scroll qb__scroll--opts`** + **`qb__opts-strip` 右 padding**），窄屏与中栏一起在 **`≤960px`** 收紧 **`el-scrollbar__wrap` 的 max-height**，滚动条观感与题目列表一致（不再用原生 **`overflow-y: auto`**）。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷「详情查看」与设计页合一（只读）

- **`routes.ts`**：**`/activity/questionnaire/detail/:id`** 无组件，**`redirect`** 至 **`ActivityQuestionnaireEdit`** + **`query.mode=view`**；保留路由名 **`ActivityQuestionnaireDetail`**。**`/activity/questionnaire/edit/:id`** 为主，`/design/:id` 仅 redirect 兼容。
- **`edit.vue`**（原 **`design.vue`**）：**`mode=view`** 时用详情标题/副文案；隐藏 **保存题目**；页头 **复制报名链接**、**问卷设计**（去掉 `mode` 可编辑）、**答卷**、**导出**；**`QuestionnaireBuilder`** **`readonly`**。
- **`questionnaire-builder.vue`**：**`readonly`** 时隐藏题型与属性面板、禁拖拽/删除/点题编辑，**`qb__body--readonly`** 单列布局。
- **`submissions.vue`**：返回走 **`ActivityQuestionnaireEdit`** + **`mode=view`**。
- **删除**：**`views/activity/questionnaire/detail.vue`**。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷：`questionnaire-utils` 合并工具文件

- 新建 **`questionnaire/utils/questionnaire-utils.ts`**，合并原 **`questionnaire-shared.ts`** 与 **`questionnaire-external-urls.ts`**；**`list.vue`**、**`edit.vue`**、**`meta-form-dialog`**、**`batch-flag-dialog`** 均改为从此文件按需导入；删除上述两个旧文件。
- 验证：`npm run type-check`（admin-web）通过。

### admin-web：零散业务表单统一为 UniForm

- **问卷**：**`batch-flag-dialog`**、**`meta-form-dialog`**、**`copy-dialog`** 由 **`el-form`** 改为 **`UniForm`** + **`UniFormConfig`**；**`questionnaire-builder`** 右侧属性区：题干走 schema，其余类型相关配置走 **`#field-_qbEditor`** 插槽（**`DesignerFieldKnown._qbEditor`** 仅占位，不参与 **`template-codec`** 序列化）；**`qb__fake-*`** 样式替代原 **`el-form-item`** 排版。
- **考勤**：**`config-form-dialog`**（假勤通知人配置）、**`holiday-form-drawer`**（请假申请抽屉）、**`pass-form-dialog`**（放行条弹窗）改用 **`UniForm`**；复杂控件（学生联想、日期/时段、上传、周几等）使用 **`#field-*`** 插槽；**周几/每日时段** 在校验规则中按 **`fixed`** 分支，避免隐藏项误报。
- **流程**：**`proc-def-panel`** 分配办理人弹窗由动态 **`el-form`** 改为 **`UniForm`** + **`assignFormModel`**（与 **`assignFields`** 的 key 对齐）。
- **权限**：**`permission/role/list.vue`** 角色弹窗 **`UniForm`**，自定义部门树 **`#field-deptIds`**。
- **邮件**：**`send-mail-dialog`**、**`outbox-compose-drawer`**（表头字段 **`UniForm`**，正文仍 **`UniEditor`**；收件人交叉校验保留）。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷模块目录对齐（列表拆 config / use-list，扁平 utils，builder → template）

- **`list.config.ts`**：检索表单 **`searchForm`**、表格列 **`tableCols`**（与全站各视图 **`list.config.ts`** 导出命名一致）；是/否选项参数名为 **`ynDispOptions`**，与 **`member/student`** 等模块一致。
- **`use-list.ts`**：**`useQuestionnaireList`** 承接接口请求、活动下拉、行格式化、表格操作；**`list.vue`** 仅模板 + 弹窗 **`ref`** 注入。
- **`questionnaire-utils.ts`**、**`submission-display.ts`**：自 **`utils/`** 提到 **`questionnaire/`** 根目录（对齐 **`attendance/holiday/holiday-utils.ts`** 等「模块根工具文件」习惯）。
- **`template/`**（原 **`builder/`**）：**`types.ts`**、**`template-codec.ts`**、**`field-presets.ts`**，表示问卷表单模板编解码；内部互引用改为相对路径 **`./types`**、**`./template-codec`**。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷列表：校区列与成员学生列表一致

- **`list.config.ts`**：表格列去掉 **`_schoolLbl`**，改为 **`prop: 'schoolIds'`** + **`type: 'array'`** + **`options: schoolOptions`** + **`lookup: { splitValues: true }`**（与 **`member/student/list.config.ts`** 中 **`schoolIds`** 列一致）；**`tableCols(t, schoolOptions, ynDispOptions)`** 增加 **`schoolOptions`** 入参。
- **`use-list.ts`**：**`cols`** 传入 **`schoolOptions.value`**；**`fmt`** 中 **`normalizeSchoolIdsForTable`** 将后端 **`schoolIds`**（数组或分隔字符串）规范为数组，供表格映射展示；移除对 **`schoolIdsCsv`** 的表格用途依赖。
- **`questionnaire-utils.ts`**：**`schoolIdsCsv`** 注释更新为表单/摘要等场景的文案拼接说明。
- 验证：`npm run type-check`（admin-web）通过。

### 侧栏：活动管理分组与问卷菜单

- **`api/modules/menu.ts`**：**`MENU_PATH_ALIASES`** 将 **`/isacommunity/activity`**（及 **`/index`**）映射到 **`/activity`**，与静态路由 **`Activity`**（**`route.activity`**、**`icon: Bell`**）对齐，不再落到 **`/activity/questionnaire`**（此前父级与子级同一路由 → 无 icon、标题错乱）。
- 同一文件新增 **`dedupeMenuSiblingsByPath`**：**`/upms/menu/user`** 树里多条旧路径合并为同一前端 path 时，同级只保留一条，避免出现多条重复「问卷管理」。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷 list.config：导出/参数与全站 list.config 对齐

- **`list.config.ts`**：**`questionnaireSearchForm` / `questionnaireTableColumns`** 改为 **`searchForm` / `tableCols`**（`export const` 箭头函数）；是/否列与筛选项参数 **`ynOptions`** 改为 **`ynDispOptions`**（与 **`member/student`** 的 **`tableCols`** 一致）；活动下拉参数保持 **`activityOptions`**。
- **`use-list.ts`**：对应调整 import 与 **`activityOpts` → `activityOptions`**、**`ynOpts` → `ynDispOptions`**。

### 问卷：`design.vue` → `edit.vue` 与路由名

- **`edit.vue`** 替代 **`design.vue`**；路由名 **`ActivityQuestionnaireEdit`**，路径 **`/activity/questionnaire/edit/:id`**；**`/activity/questionnaire/design/:id`** 仅 **`redirect`**。
- **`use-list.ts`**、**`submissions`** 页、`edit.vue` 内 **`router.push`** 均已指向 **`ActivityQuestionnaireEdit`**（答卷页返回为 **`router.back()`**）。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷列表「数据」与答卷页 list 风格

- **`use-list.ts`**：首项行操作 **`查看`** 改为 **`数据`**（**`activity.questionnaireRowData`**），跳转 **`ActivityQuestionnaireSubmissions`**；移除仅打开 **`ActivityQuestionnaireDetail`** 的旧逻辑。
- **`submissions.vue`**：**`UniDataTable`** + **`useUniListState`**（**`filt` / `hdl` / `tb`**）、工具栏 **`refresh` + `columnSetting`**；**`submissions.config.ts`**：**`submissionTableCols`**、`submissionAttachColumnProp`；**`use-submissions.ts`**：模板/bootstrap、**`lod`**、**`composeSubmissionRow`**、导出与附件对话框。
- **`locales`**：中英 **`questionnaireRowData`**。
- 验证：`npm run type-check`（admin-web）通过。

### 答卷列表：页头按钮与问卷编辑页对齐

- **`submissions.vue`**：页头右侧 **「导出问卷」** 在前，**竖向 `el-divider`**，**「返回」** `plain` 在后；**`q-submissions__header-*`** 样式同 **`edit.vue`** 的 **`q-design__header-*`**。
- 验证：`npm run type-check`（admin-web）通过。

### admin-web `src/views`：scoped SCSS 统一嵌套写法

- **约定**：页面/组件 `scoped lang="scss"` 中，同一 BEM 根下的 **`.block__element`** 一律写在 **`.block { &__element { … } }`** 内；**`:deep(...)`**、**`@media`** 放在对应根块内嵌套。
- **脚本**：**`scripts/nest-views-scss-bem.mjs`** — 对「单一 BEM 根 + 扁平 `.根__*`」自动改为根块嵌套（已带 **`&__`** 的跳过）；在 **`admin-web`** 根目录执行 **`node scripts/nest-views-scss-bem.mjs`**。
- **已批量改写**：问卷（**`questionnaire-builder`**、**`edit`**、**`meta-form-dialog`**、**`batch-flag-dialog`** 等）、考勤、邮件、权限用户、校车、协议等 **26** 个视图；**`submissions.vue`**、**`bus-order-form-dialog.vue`** 因多根或 **`:deep`** 与类名并列，脚本未覆盖部分已 **手工嵌套**。
- **清理**：去掉仅含空内容的 **`<style scoped lang="scss"></style>`**（**`no-empty-source`**）；**`word-break: break-word`** 改为 **`overflow-wrap: anywhere`**（**`questionnaire/edit.vue`**、**`email/send/list.vue`**、**`outbox-view-dialog.vue`**）。
- **格式化**：对 **`src/views/**/*.vue`** 执行 **`prettier --write`**（模板/脚本可能随行宽略有变动）。
- 验证：**`npm run lint:style`**、**`npm run type-check`**（admin-web）通过。

### 问卷模块类型归位 `src/types/modules`

- **`types/modules/activity-questionnaire.ts`**：答卷 **`SubmissionColumnMeta`**、**`SubmissionAnswerField`**、**`SubmissionRowMap`**；列表 **`QuestionnaireListDialogRefs`**；设计器 **`DesignerField*`** / **`DesignerOption`** / **`FieldProperties`**。
- **`submission-display`（已并入 `use-submissions`）**、**`submissions.config`**、**`use-submissions`**、**`use-list`**、**`edit.vue`**、**`questionnaire-builder.vue`**、**`submissions.vue`**：设计器相关类型自 **`@/types/modules/activity-questionnaire`** 引用。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷：拆分工具逻辑到各自使用文件

- 删除 **`questionnaire-utils.ts`**、**`submission-display.ts`**：报名链接 / 校区下拉 / 是/否 / 列表时间格式化 → **`use-list.ts`**（列表专用 **`useMembershipSchoolOptions`** 仅含 **`schoolOptions` + `loadSchoolOptions`**）；详情页摘要 / 复制链接 → **`edit.vue`**；元数据弹窗 → **`meta-form-dialog.vue`**；批量标记 → **`batch-flag-dialog.vue`**。
- **`submission-display`** 内答卷列生成与单元格格式化 → 并入 **`use-submissions.ts`**（私有函数）。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷：`template-codec` / `field-presets` 并入使用文件

- 删除 **`template/template-codec.ts`**、**`template/field-presets.ts`**：**`deserializeTemplateFields` / `serializeTemplateBundle`**（及内部序列化）→ **`edit.vue`**；**`BUILDER_PALETTE_TYPES`**、**`presetField`**、**`builderAddOptionRow`** 与 **`createFontId` / `newTemplateFieldId`** → **`questionnaire-builder.vue`**。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷列表 `use-list` 命名对齐成员/权限列表

- **`use-list.ts`**：**`useUniListState`** 解构为 **`queryModel` / `filters` / `tableRef` / `search` / `reset` / `handleLoadSuccess`**；对外 **`searchCfg` / `columns` / `actions` / `loadData`**；请求参数别名 **`current` / `size`**；回调内筛选模型 **`filterModel`**；**`selectedRows` / `selectedIds`**；**`onSelectionChange` / `handleSaved` / `deleteSelected` / `copySignupLink`**；行类型 **`QuestionnaireListRow`**；辅助 **`stripTrailingSlash` / `formatListTimestamp` / `applyRowDisplayFormat`**。
- **`list.vue`**：绑定命名与 **`member/teacher/list.vue`** 一致（如 **`tableRef`、`queryModel`、`searchCfg`**）。
- 验证：`npm run type-check`（admin-web）通过。

### 问卷答卷 `use-submissions` 命名对齐

- **`use-submissions.ts`**：**`filters` / `handleLoadSuccess` / `tableRef`**；**`columns` / `loadData`**（请求内 **`current` / `size`**）；**`exportAnswers`**（原 **`doExport`**）；**`fileDialogVisible`**；**`SubmissionApiRow`**；列文案参数 **`columnLabels`**；私有 **`formatSubmissionStudentGender` / `formatSubmissionCellValue`**、**`parseJsonMaybe(value)`** 等。
- **`submissions.vue`**：与列表页一致的 **`tableRef`、`columns`、`loadData`、`filters`、`handleLoadSuccess`**。
- 验证：`npm run type-check`（admin-web）通过。

### 活动管理：恢复活动列表与详情（问卷并行）

- **路由**：`/activity` 默认 **`/activity/list`**；**`ActivityEventList`**、**`ActivityEventDetail`**（`query.id` + `query.mode`）；问卷子路由不变。
- **菜单别名**：`menu.ts` 将旧 **`list` / `detail`** 映射到新路径；其余活动子菜单暂仍可指向问卷直至独立页落地。
- **列表**：`views/activity/list/`：**`UniSearchForm` + `UniDataTable`**，接口与批量能力与后端一致。
- **详情**：见下节「活动详情 UniForm」。
- 验证：`npm run type-check`（admin-web）通过。

### 活动详情：UniForm 新建/查看/编辑 + 可见范围导入

- **`detail/index.vue`**：**`UniForm`** **`view` / `edit`** 随路由 **`mode`**；页头 **提交**、**编辑**（**`busdriver_edit`**）、**返回**；**已结束**活动不显示「编辑」；封面 **slot 上传**、报名人数 **slot（不限/限额）**。
- **`detail/detail-form.config.ts`**：**`buildActivityDetailFormConfig`**（时间区间、多校区、签到、票价、开关项、可见范围、邮箱多选、微信推送、详情长文本等）。
- **`detail/use-activity-detail-page.ts`**：**`buildPayload`** 拆时间区间与人数限制；剔除 **`magicNo`**；**`schoolEmailConfigApi`**（**`appModule: 1`**）+ 选项 **`detail` 补全**；**`protocolApi.upload`** 封面；**`activityApi.visibleScopeTemplate` / `visibleScopeImport`**。
- **API**：**`schoolEmailConfig`** 常量与 **`school-email-config` 模块**；活动 **可见范围** 下载/导入。
- **类型**：**`activity-detail-form.ts`**。
- **i18n**：表单与可见范围文案（zh/en），**`eventCreateTitle`** 等。
- 验证：`npm run type-check`（admin-web）通过。

### 活动详情：`UniEditor` 富文本（中/英详情）

- **组件**：`@/components/uni-editor`（与邮件撰写等一致）；**编辑态** `v-model` 绑定 **`form.detailCn` / `form.detailEn`**；**查看态** 无编辑器的 **`v-html`** 只读区（无实质文本时显示 **—**）。
- **配置**：`detail-form.config.ts` 中两字段改为占位 **`ElInput`** + **`formItemProps` 类名**，由 **`#field-detailCn` / `#field-detailEn`** 插槽渲染编辑器并 `:deep` 隐藏默认 input。
- **文案**：`activity.detailCnPh` / **`detailEnPh`**（zh/en）。
- 验证：`npm run type-check`（admin-web）通过。

### 活动项目（program）：admin-web 列表 + 全页编辑 + 批量复制

- **API**：`API_PATHS.activityProgram`；**`api/modules/activity-program.ts`**：`getProgramPage`、`get/:id`、**`add`/`edit`（body 为对象数组，与旧版一致）**、`del?重复 ids=`、**`copyBatch`**；**`api/index.ts`** 导出 **`activityProgramApi`**。
- **类型**：**`types/modules/activity-program-list.ts`**（分页查询）；**`activity-program-form.ts`**（表单模型 + 配额行）。
- **路由**：**`/activity/program`**、**`/activity/program/detail`**（`query.id` + `query.mode=view|edit`，新建无 `id`）；**`route.activityProgram` / `activityProgramDetail`**（zh/en）。
- **菜单别名**：**`menu.ts`** 将 **`/isacommunity/activity/program`**（含 index/detail/edit）映射到 **`/activity/program`** 与 **`/activity/program/detail`**。
- **列表**：**`views/activity/program/list.vue`** + **`list.config.ts`** + **`use-list.ts`**：检索与旧版字段对齐；单校区自动带 **`schoolIds`**；多选、行点击查看、编辑（**进行中仅单轮抽奖可进编辑**，与 **`program-edit-helpers`** 一致）、批量删除、**`program-copy-dialog`** 复制到目标活动。
- **编辑/查看**：**`views/activity/program/edit/index.vue`** + **`use-program-edit.ts`** + **`program-edit-form.config.ts`**：**`UniForm`**、封面 **`protocolApi.upload`**、抽奖 **每轮配额** 插槽；**进行中+单轮抽奖** 时 **`bodyLocked`** 仅 **奖品数量** 可改。
- **i18n**：**`activity.*`** 增补项目列表/表单/复制等文案（zh/en）。
- 验证：`npm run type-check`（admin-web）通过。

### uni-lib 请求封装：HTTP 401 跳转登录

- **`plugins/request.ts`**：`onUnauthorized` 在清登录态、`ElMessage` 提示后，按 **`shell.logoutRedirect`**（默认 `/login`）执行 **`window.location.assign`**，并附加 **`redirect`** 查询参数指向当前路径；**`handlingHttp401`** 防止并发 401 重复处理。
- **`onError`**：对 **401 / 403** 不再弹出通用网络错误文案（避免与鉴权提示重复）。
- **`types/uni-runtime.ts`**：补充 **`logoutRedirect`** 说明（含 401）。
- 验证：`npm run type-check`（uni-lib）通过。


