# 迭代日志 2026-06-09

## 修改文件

- `admin-web/isacommunity重构方案.md`

## 核心逻辑变更点

- 对照 `test/old-test` 最新代码重新校验重构方案，将缺口与修订写入方案文档。
- **当前进度**：明确 Phase 8 为「主链路已通，活动项目详情子能力待补齐」。
- **新增阶段**：P10 宿舍管理、P11 校医管理、Phase 12 收尾；原 Phase 10 收尾顺延为 Phase 12。
- **P3 扩展**：补充校巴考勤（`schoolbus/attendance`、`busattendance.js`）。
- **§2.7**：新增 `test/old-test` 增量盘点（dorm / schoolDoctor / busattendance、活动域澄清）。
- **§2.4 / §14.2**：补充宿舍、校医、校巴考勤菜单映射；考勤流程/任务/配置/放行条新路径统一为 `/attendance/flow` 等（与 `menu.ts` 一致）。
- **§14.2**：登记 `MENU_PATH_ALIASES` 待接入项（内容、宿舍、校医、校巴考勤、活动问卷书签）。
- **§15.9**：补充活动项目详情 4 项待补齐能力（奖品绑定、投票绑定、获奖池文件、奖池名单）。
- **§15.11～15.13**：新增宿舍、校医、收尾阶段说明。
- **§十九**：标注为历史第一轮边界，避免与当前 Phase 进度矛盾。
- **§2.6**：修复基础设置备忘表格中 `school/grade` 路径被截断的 Markdown 错误。

## 注意事项

- 方案修订**未改** `menu.ts`；宿舍/校医/校巴考勤迁移时须按方案同步路由与 `MENU_PATH_ALIASES`。

---

## 续：Phase 8 活动项目详情子能力

### 修改文件

- `admin-web/src/api/modules/activity-lottery-pool.ts`（新建）
- `admin-web/src/api/index.ts`
- `admin-web/src/views/activity/program/edit/index.vue`
- `admin-web/src/views/activity/program/edit/components/program-detail-bindings.vue`（新建）
- `admin-web/src/views/activity/prize/components/prize-form-dialog.vue`（支持 `bindProgramId`）
- `admin-web/src/views/activity/vote-program/components/form-dialog.vue`（支持 `bindProgramId`）
- `admin-web/src/locales/lang/zh-CN/modules/activity.ts`
- `admin-web/src/locales/lang/en/modules/activity.ts`

### 核心逻辑变更点

- 活动项目详情页（`program/edit` 查看态）补齐旧页三块内嵌能力：
  - **抽奖类（type=1）**：奖品绑定（单条）、奖池文件模板下载/导入/删除、奖池名单抽屉分页。
  - **投票类（type=2）**：投票节目绑定列表增删改查。
- 奖品/投票弹窗在项目详情内打开时固定 `programId`，隐藏项目下拉（对齐旧 `isBind`）。

### uni-review-web 复核修正

- 新增 `program-bind.config.ts`：表格列配置与 `list.config.ts` 一致，仅静态 schema。
- 奖池区模板/导入补 `v-uni-permission="'busdriver_edit'"`；奖池文件删除 action 补 `busdriver_del`。

### 验证

- `npm run type-check` 通过。

---

## 续：P3 扩展校巴考勤

### 修改文件

- `admin-web/src/api/modules/school-bus-attendance.ts`（新建）
- `admin-web/src/types/modules/school-bus-attendance.ts`（新建）
- `admin-web/src/api/index.ts`
- `admin-web/src/router/modules/school-bus.ts`
- `admin-web/src/api/modules/menu.ts`（`MENU_PATH_ALIASES` 校巴考勤）
- `admin-web/src/views/school-bus/attendance/list.vue`（新建）
- `admin-web/src/views/school-bus/attendance/list.config.ts`（新建）
- `admin-web/src/views/school-bus/attendance/components/form-drawer.vue`（新建）
- `admin-web/src/locales/lang/zh-CN/modules/school-bus-more.ts`
- `admin-web/src/locales/lang/en/modules/school-bus-more.ts`
- `admin-web/src/locales/lang/zh-CN/route.ts`、`en/route.ts`

### 核心逻辑变更点

- 迁移旧 `schoolbus/attendance`：分页列表、批量删除、新增/编辑/查看抽屉。
- 筛选：校区、姓名学号、考勤日期区间、班级、路线/站点级联、考勤状态；与旧 `getAttendancePage` 参数一致（`schoolId` → `schoolIds`）。
- 表单：学生远程检索（`membership/searchList`）、路线/站点按校区联动（`buscommon`）。
- 权限：`busattendance_add` / `busattendance_edit` / `busattendance_del`（与旧系统一致）。

### 验证

- `npm run type-check` 通过。

---

## 续：Phase 9 内容管理 — 公告内容

### 修改文件

- `admin-web/src/api/modules/content-common.ts`（新建）
- `admin-web/src/api/modules/content-announcement.ts`（新建）
- `admin-web/src/types/modules/content-announcement.ts`（新建）
- `admin-web/src/api/index.ts`
- `admin-web/src/router/modules/content.ts`（新建）
- `admin-web/src/router/modules/layout.ts`
- `admin-web/src/api/modules/menu.ts`
- `admin-web/src/views/content/announcement/**`（新建）
- `admin-web/src/locales/lang/**/modules/content.ts`、`route.ts`、`index.ts`

### 核心逻辑变更点

- 迁移旧 `content/announcement`：校区筛选、分页列表、新增/编辑弹窗、行内删除。
- 校区下拉：`buscommon/getNewSchoolList`；公告 CRUD：`/isacommunity/content/announcement/*`。
- 权限：`announcement_add` / `announcement_edit` / `announcement_del`。
- 旧 `detail/index.vue` 为错误拷贝（导航按钮），新实现未做查看态，与旧列表仅编辑/删除一致。

### 验证

- `npm run type-check` 通过。

---

## 续：Phase 9 — 文章分类 + 文章内容

### 修改文件

- `admin-web/src/api/modules/content-category.ts`、`content-article.ts`（新建）
- `admin-web/src/types/modules/content-category.ts`、`content-article.ts`（新建）
- `admin-web/src/views/content/article/category/**`、`list/**`（新建）
- `admin-web/src/router/modules/content.ts`、`api/index.ts`、`menu.ts`、locales

### 核心逻辑变更点

- **文章分类**：分页、增删改弹窗；权限 `category_add/edit/del`。
- **文章内容**：筛选、列表、查看详情、增改（`UniEditor` + 主图上传）；权限 `article_add/edit/del`；`content`/`enContent` 同源提交。
- 主图上传：`VITE_UPLOAD_URL` + `parent_weapp_upload`。

### 验证

- `npm run type-check` 通过。

---

## 续：Phase 9 — 动态内容（一周食谱 + 校园生活）

### 修改文件

- `admin-web/src/api/modules/content-food-weekly.ts`、`content-moment.ts`（新建）
- `admin-web/src/api/modules/content-common.ts`（`momentTypeList`）
- `admin-web/src/views/content/moment/food-weekly/**`、`school-life/**`（新建）
- `admin-web/src/router/modules/content.ts`、`api/index.ts`、`menu.ts`、locales

### 核心逻辑变更点

- **一周食谱**：校区筛选、列表、增改（`UniEditor` + 公众号链接）；权限 `foodweekly_*`；`cnContent`/`enContent` 同源。
- **校园生活**：标题/校区/类型/可见筛选；增改含图片/PDF 上传、推送/可见；权限 `schoollife_*`。
- 菜单映射：旧 `content/moent/*` → `/content/moment/*`。

### 验证

- `npm run type-check` 通过。
