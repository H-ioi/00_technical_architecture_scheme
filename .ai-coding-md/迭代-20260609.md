# 迭代日志 2026-06-09

## 修改文件

- `admin-web/isacommunity重构方案.md`

## 核心逻辑变更点

- 对照 `test/old-test` 最新代码重新校验重构方案，将缺口与修订写入方案文档。
- **当前进度**：明确 Phase 8 为「主链路已通，活动项目详情子能力待补齐」。
- **新增阶段**：P10 宿舍管理、P11 校医管理、Phase 12 收尾；原 Phase 10 收尾顺延为 Phase 12。
- **P3 扩展**：补充校巴考勤（`schoolbus/attendance`、`busattendance.js`）。
- **§2.7**：新增 `test/old-test` 增量盘点（dorm / schoolDoctor / busattendance、活动域澄清）。
- **§2.4 / §14.2**：补充宿舍、校医、校巴考勤菜单映射；考勤流程/任务/配置/放行条新路径统一为 `/attendance/flow` 等（与 `menu.ts` 一致）。
- **§14.2**：登记 `MENU_PATH_ALIASES` 待接入项（内容、宿舍、校医、校巴考勤、活动问卷书签）。
- **§15.9**：补充活动项目详情 4 项待补齐能力（奖品绑定、投票绑定、获奖池文件、奖池名单）。
- **§15.11～15.13**：新增宿舍、校医、收尾阶段说明。
- **§十九**：标注为历史第一轮边界，避免与当前 Phase 进度矛盾。
- **§2.6**：修复基础设置备忘表格中 `school/grade` 路径被截断的 Markdown 错误。

## 注意事项

- 方案修订**未改** `menu.ts`；宿舍/校医/校巴考勤迁移时须按方案同步路由与 `MENU_PATH_ALIASES`。

---

## 续：Phase 8 活动项目详情子能力

### 修改文件

- `admin-web/src/api/modules/activity-lottery-pool.ts`（新建）
- `admin-web/src/api/index.ts`
- `admin-web/src/views/activity/program/edit/index.vue`
- `admin-web/src/views/activity/program/edit/components/program-detail-bindings.vue`（新建）
- `admin-web/src/views/activity/prize/components/prize-form-dialog.vue`（支持 `bindProgramId`）
- `admin-web/src/views/activity/vote-program/components/form-dialog.vue`（支持 `bindProgramId`）
- `admin-web/src/locales/lang/zh-CN/modules/activity.ts`
- `admin-web/src/locales/lang/en/modules/activity.ts`

### 核心逻辑变更点

- 活动项目详情页（`program/edit` 查看态）补齐旧页三块内嵌能力：
  - **抽奖类（type=1）**：奖品绑定（单条）、奖池文件模板下载/导入/删除、奖池名单抽屉分页。
  - **投票类（type=2）**：投票节目绑定列表增删改查。
- 奖品/投票弹窗在项目详情内打开时固定 `programId`，隐藏项目下拉（对齐旧 `isBind`）。

### uni-review-web 复核修正

- 新增 `program-bind.config.ts`：表格列配置与 `list.config.ts` 一致，仅静态 schema。
- 奖池区模板/导入补 `v-uni-permission="'busdriver_edit'"`；奖池文件删除 action 补 `busdriver_del`。

### 验证

- `npm run type-check` 通过。

---

## 续：P3 扩展校巴考勤

### 修改文件

- `admin-web/src/api/modules/school-bus-attendance.ts`（新建）
- `admin-web/src/types/modules/school-bus-attendance.ts`（新建）
- `admin-web/src/api/index.ts`
- `admin-web/src/router/modules/school-bus.ts`
- `admin-web/src/api/modules/menu.ts`（`MENU_PATH_ALIASES` 校巴考勤）
- `admin-web/src/views/school-bus/attendance/list.vue`（新建）
- `admin-web/src/views/school-bus/attendance/list.config.ts`（新建）
- `admin-web/src/views/school-bus/attendance/components/form-drawer.vue`（新建）
- `admin-web/src/locales/lang/zh-CN/modules/school-bus-more.ts`
- `admin-web/src/locales/lang/en/modules/school-bus-more.ts`
- `admin-web/src/locales/lang/zh-CN/route.ts`、`en/route.ts`

### 核心逻辑变更点

- 迁移旧 `schoolbus/attendance`：分页列表、批量删除、新增/编辑/查看抽屉。
- 筛选：校区、姓名学号、考勤日期区间、班级、路线/站点级联、考勤状态；与旧 `getAttendancePage` 参数一致（`schoolId` → `schoolIds`）。
- 表单：学生远程检索（`membership/searchList`）、路线/站点按校区联动（`buscommon`）。
- 权限：`busattendance_add` / `busattendance_edit` / `busattendance_del`（与旧系统一致）。

### 验证

- `npm run type-check` 通过。

---

## 续：Phase 9 内容管理 — 公告内容

### 修改文件

- `admin-web/src/api/modules/content-common.ts`（新建）
- `admin-web/src/api/modules/content-announcement.ts`（新建）
- `admin-web/src/types/modules/content-announcement.ts`（新建）
- `admin-web/src/api/index.ts`
- `admin-web/src/router/modules/content.ts`（新建）
- `admin-web/src/router/modules/layout.ts`
- `admin-web/src/api/modules/menu.ts`
- `admin-web/src/views/content/announcement/**`（新建）
- `admin-web/src/locales/lang/**/modules/content.ts`、`route.ts`、`index.ts`

### 核心逻辑变更点

- 迁移旧 `content/announcement`：校区筛选、分页列表、新增/编辑弹窗、行内删除。
- 校区下拉：`buscommon/getNewSchoolList`；公告 CRUD：`/isacommunity/content/announcement/*`。
- 权限：`announcement_add` / `announcement_edit` / `announcement_del`。
- 旧 `detail/index.vue` 为错误拷贝（导航按钮），新实现未做查看态，与旧列表仅编辑/删除一致。

### 验证

- `npm run type-check` 通过。

---

## 续：Phase 9 — 文章分类 + 文章内容

### 修改文件

- `admin-web/src/api/modules/content-category.ts`、`content-article.ts`（新建）
- `admin-web/src/types/modules/content-category.ts`、`content-article.ts`（新建）
- `admin-web/src/views/content/article/category/**`、`list/**`（新建）
- `admin-web/src/router/modules/content.ts`、`api/index.ts`、`menu.ts`、locales

### 核心逻辑变更点

- **文章分类**：分页、增删改弹窗；权限 `category_add/edit/del`。
- **文章内容**：筛选、列表、查看详情、增改（`UniEditor` + 主图上传）；权限 `article_add/edit/del`；`content`/`enContent` 同源提交。
- 主图上传：`VITE_UPLOAD_URL` + `parent_weapp_upload`。

### 验证

- `npm run type-check` 通过。

---

## 续：Phase 9 — 动态内容（一周食谱 + 校园生活）

### 修改文件

- `admin-web/src/api/modules/content-food-weekly.ts`、`content-moment.ts`（新建）
- `admin-web/src/api/modules/content-common.ts`（`momentTypeList`）
- `admin-web/src/views/content/moment/food-weekly/**`、`school-life/**`（新建）
- `admin-web/src/router/modules/content.ts`、`api/index.ts`、`menu.ts`、locales

### 核心逻辑变更点

- **一周食谱**：校区筛选、列表、增改（`UniEditor` + 公众号链接）；权限 `foodweekly_*`；`cnContent`/`enContent` 同源。
- **校园生活**：标题/校区/类型/可见筛选；增改含图片/PDF 上传、推送/可见；权限 `schoollife_*`。
- 菜单映射：旧 `content/moent/*` → `/content/moment/*`。

### 验证

- `npm run type-check` 通过。

---

## 续：Phase 9 — 讨论管理（标签 + 内容列表）

### 修改文件

- `admin-web/src/api/modules/content-discussion-tag.ts`、`content-discussion.ts`（新建）
- `admin-web/src/types/modules/content-discussion.ts`、`content-discussion-tag.ts`（新建）
- `admin-web/src/views/content/discussion/tag/**`、`list/**`（新建）
- `admin-web/src/router/modules/content.ts`、`api/index.ts`、`menu.ts`、locales

### 核心逻辑变更点

- **讨论标签**：分页、增删改弹窗；权限 `tag_add/edit/del`；删除 `POST .../delete?id=`，更新 `PUT`。
- **讨论内容**：关键字/校区/标签/可见/置顶/推荐筛选；查看详情、增改（9 图 + PDF）；权限 `discussion_add/edit/del`。
- 菜单映射：旧 `/isacommunity/content/discussion/*` → `/content/discussion/*`。

### 验证

- `npm run type-check` 通过。

---

## 续：Phase 10 — 楼栋管理（宿舍）

### 修改文件

- `admin-web/src/api/modules/dorm-building.ts`（新建）
- `admin-web/src/types/modules/dorm-building.ts`（新建）
- `admin-web/src/views/dorm/space/building/**`（新建）
- `admin-web/src/router/modules/dorm.ts`（新建）、`layout.ts`
- `admin-web/src/api/index.ts`、`menu.ts`、locales

### 核心逻辑变更点

- **楼栋管理**：卡片列表（对齐旧页）、校区/楼栋/状态筛选；增改弹窗；启用/禁用开关；删除确认。
- 权限：`building-add` / `building-edit` / `building-delete`；校区下拉用 `membershipApi.school` 的 `externId`。
- 菜单：`/isacommunity/dorm/space/building` → `/dorm/space/building`。

### 验证

- `npm run type-check` 通过。

---

## 续：Phase 10 — 楼层管理

### 修改文件

- `admin-web/src/api/modules/dorm-floor.ts`（新建）；`dorm-building.ts` 类型扩展 `includeFloor`
- `admin-web/src/types/modules/dorm-floor.ts`、`dorm-building.ts`（楼层嵌套类型）
- `admin-web/src/views/dorm/space/floor/**`（新建）
- `admin-web/src/router/modules/dorm.ts`、`api/index.ts`、`menu.ts`、locales

### 核心逻辑变更点

- **楼层管理**：`building/page?includeFloor=true` 按楼栋分组卡片；筛选校区/楼栋/楼层状态。
- 增改提交字段 `school`（非 `schoolId`），与旧接口一致；权限 `floor-add/edit/delete`。
- 男女住宿生数以彩色数字展示（未引入旧头像图）。

### 验证

- `npm run type-check` 通过。

---

## 续：Phase 10 — 房间管理

### 修改文件

- `admin-web/src/api/modules/dorm-room.ts`、`dorm-project.ts`（新建）；`dorm-floor.ts` 补充 `page`/`list`
- `admin-web/src/types/modules/dorm-room.ts`（新建）
- `admin-web/src/views/dorm/space/room/**`（新建，含 `room-card` 子组件控制页体积）
- `admin-web/src/views/dorm/space/room-assign/**`（床位卡片、新增床位/入住/换宿弹窗）
- `admin-web/src/api/modules/dorm-bed.ts`、`dorm-student.ts`（新建）；`dorm-room.ts` 补充 `list`
- `admin-web/src/router/modules/dorm.ts`、`api/index.ts`、`menu.ts`、locales

### 核心逻辑变更点

- **房间管理**：`floor/page?includeRoom=true` 按楼层分组卡片；筛选校区/楼栋/楼层/性别/姓名/房号。
- 增改弹窗：学校→楼栋→楼层→属性级联；权限 `room-add` / `room-edit` / `room-delete` / `room-assignment`。
- 入住状态标签、性别卡片底色与旧页映射一致（gender 1/2 与底色 class 对应关系保留）。
- 床位分配入口跳转 `/dorm/space/room-assign/:id`（隐藏路由，`activeMenu` 指向房间列表）；菜单别名含旧 `/isacommunity/dorm/space/room/:id`。

### 验证

- `npm run type-check` 通过。

---

## 续：Phase 10 — 床位分配

### 修改文件

- `admin-web/src/api/modules/dorm-bed.ts`、`dorm-student.ts`（新建）；`dorm-room.ts` 补充 `list`
- `admin-web/src/types/modules/dorm-bed.ts`、`dorm-student.ts`（新建）
- `admin-web/src/views/dorm/space/room-assign/**`（`index` + `bed-card` + 三个弹窗 + `assign.config.ts`）
- `admin-web/src/api/index.ts`、locales `dorm.roomAssign`

### 核心逻辑变更点

- **床位分配**：`GET room/:id` 取 `beds` 卡片列表；新增床位、空闲床位删除、办理入住、换宿、退宿。
- 入住：`student/page`（`hasBed: false` + `gender`）；换宿：学校→楼栋→楼层→房间→床位级联 + `bed/assignment/move`。
- 退宿参数对齐旧页：`bed_id` + `admission_no`（兼容 `admissionNo` / `student.admission_no`）。

### 验证

- `npm run type-check` 通过。

---

## 续：Phase 10 — 属性管理

### 修改文件

- `admin-web/src/api/modules/dorm-project.ts`（扩展 `page`/`detail`/`create`/`update`/`deleteBatch`）
- `admin-web/src/types/modules/dorm-project.ts`（新建）
- `admin-web/src/views/dorm/space/attribute/**`（新建）
- `admin-web/src/router/modules/dorm.ts`、`api/modules/menu.ts`、locales

### 核心逻辑变更点

- **属性管理**：`UniSearchForm`（校区）+ `UniDataTable` 分页；增改弹窗（学校/名称/状态 `isActive`）。
- 批量删除 `deleteBatch?ids=`；权限 `attribute-add` / `attribute-edit` / `attribute-delete`。
- 菜单：`/isacommunity/dorm/space/attribute` → `/dorm/space/attribute`。

### 验证

- `npm run type-check` 通过。

---

## 续：Phase 10 — 自动分配规则

### 修改文件

- `admin-web/src/api/modules/dorm-assign-rule.ts`（新建）
- `admin-web/src/types/modules/dorm-assign-rule.ts`（新建）
- `admin-web/src/views/dorm/space/rule/**`（新建）
- `admin-web/src/router/modules/dorm.ts`、`api/index.ts`、`menu.ts`、locales

### 核心逻辑变更点

- **自动分配规则**：`UniSearchForm`（校区/规则名/状态）+ `UniDataTable` 分页与批量删除。
- 增改弹窗：`schoolId`、`ruleName`、动态 `rulesList`（`ruleItemList` 下拉）、`is_active`；提交 `school_id`/`rule_name`/`rule_items`/`is_active`。
- 权限 `rule-add` / `rule-edit` / `rule-delete`；菜单 `/isacommunity/dorm/space/rule` → `/dorm/space/rule`。

### 验证

- `npm run type-check` 通过。

---

## 续：Phase 9 — 讨论评论 + 点赞收藏

### 修改文件

- `admin-web/src/api/modules/content-discussion-comment.ts`、`content-discussion-like-save.ts`（新建）
- `admin-web/src/api/modules/content-discussion.ts`（补充 `listAll`）
- `admin-web/src/types/modules/content-discussion-comment.ts`、`content-discussion-like-save.ts`（新建）
- `admin-web/src/views/content/discussion/comment/**`、`like-save/**`（新建）
- `admin-web/src/router/modules/content.ts`、`api/index.ts`、`menu.ts`、locales

### 核心逻辑变更点

- **讨论评论**：分页、增删改；权限 `comment_add/edit/del`；删除 `POST /comment/{id}`。
- **点赞收藏**：分页、增删改；权限 `likeandsave_add/edit/del`；表单含讨论下拉、`parentId`、点赞/收藏布尔。
- 讨论下拉共用 `contentDiscussionApi.listAll`；菜单 `likeandsave` → `/content/discussion/like-save`。

### 验证

- `npm run type-check` 通过。

---

## 续：宿舍管理侧栏图标

### 修改文件

- `admin-web/src/router/modules/dorm.ts`

### 核心逻辑变更点

- 宿舍管理一级菜单补充 `meta.icon: 'OfficeBuilding'`，与侧栏 `resolveIcon` 及其他域（如 `House` 工作台、`Van` 校车）保持一致。

### 注意事项

- 新增一级菜单时须在路由 `meta` 配置 `icon`（Element Plus 图标名），否则侧栏仅显示文字。

---

## 续：宿舍管理搜索区无 label

### 修改文件

- `admin-web/src/views/dorm/space/building/list.config.ts`
- `admin-web/src/views/dorm/space/floor/list.config.ts`
- `admin-web/src/views/dorm/space/room/list.config.ts`

### 核心逻辑变更点

- 楼栋/楼层/房间列表 `searchForm` 对齐邮件群发：`label: ''`、去掉 `labelPosition: 'top'`，字段含义仅由 `placeholder` 表达；房间页 6 项改为 `span: 4` 单行排布。

---

## 续：今日重构页搜索 label 全量复核

### 修改文件

- `admin-web/src/views/content/discussion/list/list.config.ts`
- `admin-web/src/views/dorm/space/attribute/list.config.ts`

### 复核结论

| 页面 | 搜索 label | 说明 |
| --- | --- | --- |
| 公告/文章/食谱/校园生活/校巴考勤 | 已合规 | `label: ''` + placeholder |
| 楼栋/楼层/房间 | 已合规 | 上轮已修 |
| 讨论标签/评论/点赞收藏/文章分类 | 无搜索区 | — |
| **讨论内容列表** | **已修** | 原 `labelPosition: top` + 显示 label |
| **宿舍属性管理** | **已修** | 同上 |

### 规范

- 列表 `searchForm`：`label: ''`，含义走 `placeholder`；弹窗 `dialogFormConfig` 保留 label。
