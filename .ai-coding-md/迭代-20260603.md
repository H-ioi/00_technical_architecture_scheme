# 迭代日志 2026-06-03

## 修改文件

- `admin-web/src/router/routes.ts`：改为从 `router/modules` 聚合导出
- `admin-web/src/router/modules/constant.ts`：登录、403、404
- `admin-web/src/router/modules/layout.ts`：主布局及 children 聚合
- `admin-web/src/router/modules/dashboard.ts`
- `admin-web/src/router/modules/member.ts`
- `admin-web/src/router/modules/activity.ts`
- `admin-web/src/router/modules/base.ts`
- `admin-web/src/router/modules/school-bus.ts`
- `admin-web/src/router/modules/permission.ts`
- `admin-web/src/router/modules/attendance.ts`
- `admin-web/src/router/modules/email.ts`
- `admin-web/src/router/modules/protocol.ts`
- `admin-web/isacommunity重构方案.md`：目录树与权限章节路由说明同步
- `admin-web/README.md`：补充 `router/modules` 约定
- `前端管理后台(admin-web)构建方案.md`：目录树与路由章节同步

## 核心逻辑

- 路由 path、name、redirect、meta 与拆分前一致，仅文件组织按业务域拆分；`router/index.ts` 仍 `import { routes } from './routes'`。

## 注意事项

- 新增业务域时新增 `router/modules/<域>.ts`，在 `layout.ts` 的 `children` 中注册；协议详情等无分组路由可仿 `protocol.ts` 导出数组再展开。

---

## 补充（同日）

- `前端开发规范.md`：新增 **5.4 方法与子逻辑抽离规范**（原 5.4 `uni-lib` 顺延为 5.5）；第十二章、验收清单同步禁止无收益抽方法。
- 用户级 Skill（`~/.cursor/skills/`）：`uni-review-web`、`uni-review-mobile`、`uni-review-pc`；仓库 `.agents/skills` 仅保留说明 README。

---

## 补充（uni-review-web 自包含）

- `~/.cursor/skills/uni-review-web/SKILL.md`：重写为自包含规范，**不再依赖** `前端开发规范.md`；纳入目录、页面形态、分层、权限、抽离、命名、注释、样式、Pinia、API、质量/提交/测试、禁止项与评审 checklist。
- `.agents/skills/README.md`：说明评审以 Skill 为准，`前端开发规范.md` 仅作可选人类文档。
- `uni-review-web` §2.1：明确 `composables`=多页共用业务逻辑，`utils`=全局纯方法（如时间格式化）。
- `前端开发规范.md` 新增 **3.7** `composables/` 与 `utils/` 分工；同步 **5.4**、第十二章。
- `uni-review-web` / `uni-review-mobile` / `uni-review-pc`：均写入 composables/utils 分工与 Finish 检查项。
- `uni-review-mobile`：重写为自包含（不依赖 `前端开发规范.md`）；补 `locales`/`stores`/`types`/`static`/`data`/`custom-tab-bar`/`manifest.json`；页面允许 `floors.vue` 等非 `index.vue`；体积 ≤440 / >800 必拆。

---

## 补充（composables / utils 合规治理）

### 修改文件

- `admin-web/src/utils/school-bus.ts`：移除 `isSpreadsheetFilename`、`detailCellDisplay`（§2.1 不宜进 utils）
- `admin-web/src/utils/form-display.ts`：移除 `formatRangeLabel`
- `admin-web/src/utils/tool.ts`：移除未在业务使用的 `getUUID`、`getCustomerUuid`；`coerceIdList` 迁至单页
- `admin-web/src/composables/use-list-table-empty.ts`：`hasActiveListFilters` 改为模块内私有
- `admin-web/src/views/activity/detail/use-activity-detail-page.ts`：页内 `toIdList`
- `admin-web/src/views/activity/detail/detail-form.config.ts`：日期范围 viewRender 内联
- 校车多页 `list.vue` / `tab.vue`：Excel 后缀校验与 `detailCellDisplay` 页内实现
- 对应 `__tests__/utils/*`、`__tests__/composables/use-list-table-empty.test.ts`

### 核心逻辑

- **utils** 仅保留多形态归一化、格式化契约、下载等 ≥5 行且 2+ 处复用的纯函数；薄包装与单页逻辑回退内联。
- **composables** 保持 api/ref/i18n 编排；辅助纯判断不对外 export。

### 注意事项

- `tool.ts` 文件名仍偏泛，后续可按 `date-format.ts` 等拆分（需批量改 import）。
- 交付前在 `admin-web` 执行 `npm run lint`、`npm run type-check`、`npm run test:run`。
- 治理时注意保存为 **UTF-8**，避免中文注释变成 `????`（已修复 `tool.ts`、`form-display.ts`、`detail-form.config.ts`、`school-bus.test.ts`）。

---

## 补充（取消 use-list.ts 拆分约定）

### 修改文件

- `~/.cursor/skills/uni-review-web/SKILL.md`：列表页改为 `list.vue` + `list.config.ts`；单页逻辑写在 `.vue` `<script setup>`；禁止新增页面级 `use-list.ts` / `use-*.ts`
- `前端开发规范.md`：§3.1 目录树、§3.7、§5.3、§5.4、§6.4 同步
- `前端管理后台(admin-web)构建方案.md`、`前端架构方案总览.md`、`.agents/skills/README.md`

### 核心逻辑

- **存量** `use-list.ts` 可保留；**新增与迁改**合并进对应 `.vue` 或上提多页 `composables/`
- 页面过大时拆 **子组件**，不再拆 `use-list.ts`
- `list.config.ts` 仍仅静态 schema

### 注意事项

- `composables/use-list-table-empty.ts` 等为**多页复用**，与页面级 `use-list.ts` 无关，规则不变

---

## 补充（移除 views 下全部 use-list.ts）

### 修改范围

- 删除 `admin-web/src/views/**/use-list.ts` 共 **32** 个
- 列表逻辑并入同目录 `list.vue` 的 `<script setup>`（原 composable 调用处替换为内联实现）
- 脚本：`merge-use-list-v2.py`（主合并）、`fix-merged-list-vue.py`、`cleanup-merged-list-vue.py`

### 核心逻辑

- 多行 `import` 整段合并，避免首版脚本截断 import
- 带参 composable：`callbacks`/`options`/`refs`/`formDlg` 等按调用实参替换（如 `callbacks.onEdit` → `openEdit`，`refs.metaDlg` → `metaDlg`）
- `useUniI18n` 去重合并为单次解构；`return { ... }` 用括号匹配剥离
- 顺带修复：`school-bus/student/apply` 补 `openDetail`；`attendance/pass` 批量删除后 `refresh()` → `refreshTable()`

### 验证

- `npm run type-check` 通过
- `eslint src/views/**/list.vue` 无 error
- `views` 下无 `use-list.ts`、无 `from './use-list'`

### 注意事项

- Tab/多文件页（如 `use-outbox-sent.ts`、`use-holiday-leave.ts`）未动，仅治理 `use-list.ts`
- 部分 `list.vue` 行数较多（如问卷、校车申请），后续按规范拆 **子组件**，不再新建 `use-list.ts`

---

## 校车订单表单抽屉拆分（bus-order-form-dialog）

### 修改文件

- `admin-web/src/views/school-bus/student/components/bus-order-form-dialog.vue`（主抽屉编排，~194 行）
- `bus-order-form-dialog-setup-main.ts`、`bus-order-form-dialog-setup-route.ts`（表单逻辑，未新建 `use-list.ts`）
- `bus-order-form-types.ts`、`bus-order-form-helpers.ts`、`bus-order-form-dialog.scss`
- `bus-order-form-sections/`：`admission-field`、`route-table`、`person-table`、`approval-payment`、`sign-field`、`payment-detail`、`route-dialog`、`person-dialog`

### 核心逻辑

- 行为与拆分前一致；权限仍用 `useUniPermission`（`isshow_*` / `isedit_*`），未改 `v-uni-permission`
- 路线/接送人子弹窗通过 `defineExpose` 暴露 `UniForm` ref，供校验与上传后 `validateField`

### 验证

- `npm run type-check` 通过

### 注意事项

- 样式在 `bus-order-form-dialog.scss` 非 scoped 引入，子区块组件共用 BEM 类名

---

## utils 归位与请假/销假 Tab 内联（admin-web）

### 修改文件

- 新增 `admin-web/src/utils/attendance-holiday.ts`（自 `views/attendance/holiday/holiday-utils.ts`）
- 新增 `admin-web/src/utils/mail-page-utils.ts`（自 `views/email/mail-page-utils.ts`）
- `admin-web/src/views/attendance/holiday/tab.vue`：内联原 `use-holiday-leave` / `use-holiday-return`（~403 行）
- 删除 `holiday-utils.ts`、`use-holiday-leave.ts`、`use-holiday-return.ts`、`views/email/mail-page-utils.ts`
- 更新 `email/group` 下 `list.vue`、`list.config.ts`、`group-dialog.vue` 与对应测试 import
- `admin-web/src/utils/form-display.ts`：修复 JSDoc 中文乱码

### 核心逻辑

- 纯归一化/展示函数进 `src/utils/`；单页列表逻辑留在 `tab.vue` script setup，符合 uni-review-web
- 未改动任何 `v-uni-permission` 或权限码

### 验证

- `npm run type-check` 通过

### 注意事项

- `school-bus/driver/use-list.ts` 磁盘上已不存在（无需再删）

---

## 超大列表页 / 问卷设计器拆分

### 修改文件

- `school-bus/student/apply/list.vue`（430 行）及 `apply/components/`：`page-header`、`cascade-alert`、`batch-toolbar`、`reject-dialog`
- `school-bus/route/exception/list.vue`（359 行）及 `exception/components/`：`page-header`、`detail-dialog`、`exception-list-helpers.ts`（搜索 schema + 行格式化）
- `activity/questionnaire/components/questionnaire-builder.vue`（109 行）及子组件：`palette`、`list`、`editor`、`field-preview`；样式抽至 `questionnaire-builder.scss`

### 核心逻辑

- 列表页逻辑仍留在 `list.vue`；`list.config.ts` 未改；未新建 `use-list.ts`
- 驳回弹窗、详情弹窗采用 `defineExpose({ open })` 与 batch-flag 一致
- 问卷设计器仍用 `use-questionnaire-builder.ts`，主文件仅编排三栏子组件

### 验证

- `npm run type-check` 通过

### 注意事项

- 未改动任何 `v-uni-permission` 指令或权限码

---

## P0 收尾：lint 清零与组件目录化（权限项除外）

### 修改文件

- `components/list-table-empty/index.vue`：自根目录单文件迁入目录；删除 `list-table-empty.vue`；全量 import 改为 `@/components/list-table-empty/index.vue`
- 校车 8 处列表/Tab：内联 Excel 后缀校验；`operation`/`plan/tab` 内联 `detailCellText`（原 `detailCellDisplay`）
- `activity/detail/tabs/blessing.vue`、`feedback.vue`：`openDetail` 与 `decorateRows` 对齐，消除 `no-undef`
- `bus-order-form-sections/*`：`provide/inject(busOrderRuleFormKey)` 替代 prop 双向绑定，消除 `vue/no-mutating-props`
- 测试与多处页面：移除未使用 import、`as any` → 具体类型；`eslint --fix` 格式化 572 条 warning

### 验证

- `npm run lint` 通过（0 error / 0 warning）
- `npm run type-check` 通过
- `npm run test:run`：32 文件 / 490 用例通过

### 未处理（用户要求跳过）

- 问卷/活动域 `busdriver_*` 权限码、`v-uni-permission` 补全、`hasPermission` → `v-uni-permission` 统一等权限类项
