# 迭代日志 20260604

## 修改文件

- `.gitignore` — 新增 `uni-lib-cli/.npmrc` 忽略规则
- `uni-lib-cli/.npmrc` — 改为本地模板（无 token），不再纳入版本控制

## 核心变更

- GitHub Push Protection 因 `uni-lib-cli/.npmrc` 含 npm Access Token 拦截推送
- 重写未推送的 6 个 commit，从历史中移除含 token 的 `.npmrc`
- 跳过仅注释 token 的 commit `8fa0850`（注释后仍会被 secret scanning 识别）

## 注意事项

- **务必在 npmjs.com 撤销已泄露的 token 并重新生成**
- 本地认证：`npm login` 或在 `.npmrc` 中配置（该文件已被 gitignore）
- 备份分支 `fix-isacommunity-backup` 保留修复前历史，确认无需要后可删除

---

## customer-mobile（uni-review-mobile 整改）

### 修改文件

- `customer-mobile/src/services/request.ts` — `VITE_API_BASE_URL` 拼接、`VITE_USE_MOCK` 控制 Mock
- `customer-mobile/src/services/modules/user.ts` — 路径改为 `/user/profile`，Mock 种子外置
- `customer-mobile/src/data/mock/user-profile.ts` — 新增 Mock 种子（标注 Mock）
- `customer-mobile/src/styles/**`、`app.scss`、`App.vue` — 全局样式分层
- `customer-mobile/src/static/common/logo.png`、`static/tabbar/*.png` — 静态资源按域归类
- `customer-mobile/src/pages.json` — tabBar 图标路径
- `customer-mobile/.env.*` — 增加 `VITE_USE_MOCK`
- `customer-mobile/src/env.d.ts` — 环境变量类型；合并原 `shime-uni.d.ts`
- `customer-mobile/src/composables/use-user-profile.ts` — 失败 toast + i18n
- `customer-mobile/src/locales/lang/*` — `mine.loadProfileFailed`
- `customer-mobile/package.json` — `name` 改为 `customer-mobile`
- `customer-mobile/.gitignore` — 明确忽略 `.env.local`

### 核心变更

- 开发环境默认 Mock（`VITE_USE_MOCK=true`），生产/测试走真实 `uni.request`
- Mock 昵称与 `zh-CN.mine.guest` 对齐为「未登录」

### 注意事项

- tabBar 图标为占位 PNG，上线前请替换为设计稿资源
- 生产部署前确认 `VITE_API_BASE_URL` 与后端网关一致

---

## customer-mobile 对齐 01_community-teacher 公共基建

### 修改文件（摘要）

- `locales/lang/*/` 按域拆分（tab/nav/auth/common/home/mine/login），与教师端结构一致
- `locales/index.ts` 系统语言检测、`applyTabBarTexts` H5 跳过
- `services/request.ts` loading 栈、Token 头、401、`normalizeResponse`
- `utils/auth-session.ts`、`stores/modules/app.ts`（token/userInfo）
- `components/common/locale-toggle.vue`，`main.ts` 全局注册
- `styles/`：`@use` + `_form-reset`、`_pill`；`uni.scss` 主题变量
- `pages/login/index` 占位；`vite`/`tsconfig`/`eslint` 与教师端对齐
- 移除 `VITE_USE_MOCK`（Mock 与教师端一致：`mockData` 即走 Mock）

### 注意事项

- 教师端 H5 自绘 tabBar / `page-header` 等未迁入模板（属业务 UI）
- `LOCALE_STORAGE_KEY` 仍为 `customer-mobile-locale`（教师端误用同名 key，模板保持独立）

---

## customer-mobile 无用代码清理

### 删除

- `yarn.lock`（统一使用 `package-lock.json`）
- `src/components/business/`、`src/static/common/logo.png`
- `src/styles/common/_pill.scss`（无页面引用）
-  barrel：`composables/index.ts`、`utils/index.ts`、`services/index.ts`、`types/index.ts`
- 未使用：`loginByAccount`、`LoginParams`、`LOCALE_DISPLAY_NAMES`、i18n `common.backToList`

### 精简

- `use-locale` 仅导出 `cycleLocale` / `nextLocaleShortLabel`
- `uni.scss` 仅保留当前引用的主题变量
- `app` store 去掉空 `clearLogoutRelatedStorage` 与未导出冗余方法

---

## customer-mobile 仅保留首页

### 删除

- 页面：`pages/mine`、`pages/login`
- `pages.json` tabBar；`static/tabbar/`
- 用户/登录相关：`use-user-profile`、`services/modules/user`、`data/mock`、`auth-session`、`types/modules/user`
- i18n：`tab` / `mine` / `login` / `auth` 词条

### 保留

- 唯一页面：`pages/home/index.vue`

---

## 规范：禁止拆碎步骤函数

### 修改文件

- `C:\Users\admin\.cursor\skills\uni-review-mobile\SKILL.md` — §7 新增「禁止拆碎步骤函数」
- `C:\Users\admin\.cursor\skills\uni-review-web\SKILL.md` — §2.1 / §8 同步
- `前端开发规范.md` — §5.4 新增同名小节
- `customer-mobile/src/utils/request.ts` — 内联 `attachAuthHeaders` 等步骤函数示范

### 核心规则（一句话）

**适用于所有函数/方法（无场景例外）：只调用一次、有效逻辑 <5 行、语义是「步骤」不是「动作」的，禁止单独拆函数，写在调用处。**

---

## 规范：步骤函数规则适用范围扩写

### 修改文件

- `uni-review-mobile/SKILL.md` — §7 增加「适用范围：一切函数与方法」；示例含页面 `openForm`；§11 / checklist 标注 **所有方法**
- `uni-review-web/SKILL.md` — §2.1 / §8 明确无场景例外
- `前端开发规范.md` — §5.4 适用范围 + 三问适用于任意新函数；§十二禁止项同步

### 说明

- 不仅限于 `utils/request` 或拦截器；页面、composable、store、api 同等约束

---

## customer-mobile 金额精度工具 math.ts

### 修改文件

- `customer-mobile/src/utils/math.ts` — 新增 `execute` 四则运算（精度处理），内部 `science` / `scienceNum` 等

### 说明

- 逻辑与历史 JS 实现一致；补充 TypeScript 类型；`case 3` 补 `break` 避免落入 `default`
- 对外仅导出 `execute`，使用：`import { execute } from '@/utils/math'`

---

## customer-mobile utils 聚合 index.ts

### 修改文件

- `customer-mobile/src/utils/index.ts` — `getUUID`、`urlDel`、`isGetFilter`、`add/subtract/multiply/divide`、`debounce`；从 `./math` 引入 `execute`
- `customer-mobile/src/utils/math.ts` — 修正 `scienceNum` 未使用赋值的 lint

### 说明

- `getUUID` 修正 y 位为 `(r & 0x3) | 0x8`（原 `'r&0x3'` 为笔误）
- 四则运算将 `acc` 传入 `execute` 第四参（原稿写死 `0`）
- `debounce` 补全 `FUNC_ERROR_TEXT` / `toNumber` 等缺失依赖，逻辑与 lodash 版一致

---

## 规范：禁止无谓参数/常量抽离（扩至全部定义抽取）

### 修改

- `uni-review-mobile/SKILL.md` — §7 小节改名为「禁止无谓参数/常量抽离」，适用范围扩至**所有参数、常量、配置项**
- `uni-review-web/SKILL.md` — §8 同步
- `前端开发规范.md` — §5.4 同步 + §十二禁止项

### 规则摘要（与步骤函数内联同一原则）

- 先数**真实消费者**（取值与引用次数、文件数）
- 仅 1 处使用 → 写在调用处，禁止 `constants`/`config`/对象表/单字段 export 再 import
- ≥2 文件或同文件同值 ≥2 次 → 允许消费方文件顶部 `const` 或多 module 共用函数

## 文档与 Skill 同步 customer-mobile 标准模板

### 修改

- `前端移动端(customer-mobile)构建方案.md` — 目录树、api/request 分层、i18n 结构、第十七章模板现状
- `customer-mobile/README.md` — 新建模板说明
- `uni-review-mobile/SKILL.md` — §2.0 模板对照表、§4 分层改为 api + utils/request

---

### customer-mobile：去掉 api/http-helpers pickData

- `utils/request.ts` 响应拦截器成功后将 `response.data` 设为后端 `body.data`
- 各 `api/modules/*` 使用 `http.get<T>` / `res.data`；删除 `http-helpers.ts`

---

## customer-mobile 旧 mixin 拆分为 composables

### 新增/修改

- `stores/modules/user.ts` — 用户、定位、登录跳转（替代 Vuex + 组件 data）
- `api/modules/{authority,commons,member}.ts` + `api/http-helpers.ts`
- `composables/use-app-common.ts` — **单一入口**聚合原 mixin（已删除 5 个拆分 composable）
- `utils/format-distance.ts`、`utils/baidu-to-gcj.ts`
- `types/modules/{user,authority,commons,member}.ts`

### 页面用法

```ts
const { userInfo, openToPage, getUserInfo, getAddressData } = useAppCommon({
  onAfterProfile: submit,
})
```

### 说明

- 基址走 `VITE_API_BASE_URL` + `@/utils/request`，无 `config/index.js`
- 登录/详情/WebView 等路由在 `APP_ROUTES`，需在 `pages.json` 注册后使用

---

## customer-mobile 新增 api 层标准案例

### 新增文件

- `src/api/constants.ts` — 路径前缀 `API_PREFIX`
- `src/api/modules/home.ts` — 对齐 admin-web 的 `{ url, name, get }` 模块写法
- `src/api/index.ts` — 统一导出 `homeApi`
- `src/types/modules/home.ts` — 请求/响应类型

### 说明

- HTTP 使用 `@/utils/request`（非 admin-web 的 `uni-ui-lib`）
- 页面用法：`import { homeApi } from '@/api'` → `await homeApi.status.get()`

---

## customer-mobile 移除平台判断

### 删除

- `customer-mobile/src/utils/modules/platform.ts`

### 修改

- `customer-mobile/src/pages/home/index.vue` — 去掉平台展示与 `platformText`
- `customer-mobile/src/locales/lang/*/home.ts` — 移除 `platformPrefix` / `platformWechat` / `platformH5`

### 说明

- 首页仅保留「应用状态」文案；跨端分支需时用 `#ifdef` 或业务处直接判断，不再维护 `isWeapp`/`isH5` 工具

---

## admin-web 移除 api/constants.ts

### 删除

- `admin-web/src/api/constants.ts`

### 修改

- `admin-web/src/api/modules/*.ts`（33 个引用文件）— 各模块顶部内联 REST 前缀常量
- `admin-web/src/__tests__/api/school-email-config.test.ts` — 移除对已删 constants 的 mock

### 说明

- 路径与旧版 `API_PATHS` 取值一致；新接口在对应 module 文件内维护 `const base` / `path` 等

---

## 规范：api 路径内联（uni-review-web + 前端开发规范）

### 修改文件

- `~/.cursor/skills/uni-review-web/SKILL.md` — §13 扩写 REST 前缀内联；§6.1 / §15 / checklist 同步
- `前端开发规范.md` — 新增 §4.4 管理后台 `api/modules` 路径约定
- `.agents/skills/README.md` — admin-web 接口路径摘要

### 核心规则（一句话）

**禁止 `api/constants.ts` / `API_PATHS`；各 `api/modules/*.ts` 顶部 `const` 字面量维护 REST 前缀，与权限码内联同理。**
