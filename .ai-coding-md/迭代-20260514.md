# 迭代-20260514

## admin-web 用户体验优化（承接计划）

### 1. 列表空态/请求错误（已完成，前期会话）

- `useListTableEmpty`、`ListTableEmpty` 统一无数据 / 筛选无结果 / 无权限与重试、重置筛选交互。
- 多业务列表接入 `#empty`、`@request-error` 与合并后的 `@load-success`。

### 2. 超长表单（校车订单弹窗）

- `bus-order-form-dialog.vue`：关闭前未保存二次确认（baseline JSON）、提交成功跳过确认；校验失败 `scrollToField` 及部分业务区滚动锚点。

### 3. 校车学生列表多级联动筛选

- `use-student-order-filters`：加载态、失败重试、`StudentOrderSearchCascade`；申请单/订单 list 配置禁用与 loading；页面 `el-alert` 提示与重试。

### 4. 路线规划导入导出反馈

- `route/plan/tab.vue`：导入全屏 `ElLoading`；`summarizeImportRows` 区分服务端文案与行级错误；有错误行时支持弹窗内「下载错误明细」（完整失败列表为 txt）。
- 文案：`schoolBus.importProgress`、`importHadRowErrors`、`downloadErrorReport`（zh/en）。

### 5. 可选：Tab 与 URL 同步

- 新增 `composables/use-tab-query-sync.ts`，默认同步 query `tab`。
- 接入：`school-bus/route/plan/tab.vue`（routes | term | station）、`attendance/holiday/tab.vue`（leave | return）、`email/outbox/tab.vue`（sent | draft），支持刷新恢复与分享带 Tab 的链接。

### 6. 校车申请/订单表单：主容器改为抽屉；审批与缴费状态同一行

- `bus-order-form-dialog.vue`：`el-dialog` 改为右侧 `el-drawer`（宽约 1000px，`min(1000px, 96vw)`）；抽屉 body 纵向 flex，滚动区在表单内容区；路线/接送人子弹窗仍为 `el-dialog` 且 `append-to-body`。
- 审批状态与缴费状态合并为 `approvalPaymentRowSlot`：`el-row` 两列（同意审批时显示缴费列，否则审批占满行）；标签用既有 `bus-order-form__nested-label` 样式。

### 7. Element Plus 数字输入：`controls-position="right"`（UniForm 层合并进 schema 渲染）

- `uni-lib` 的 `UniForm` 对 `component === 'ElInputNumber'` 在 `v-bind` 前合并默认 `controlsPosition: 'right'`，字段 `componentProps` 可覆盖；与 `resolveDynamicComponent` 搭配时比 `setPropsDefaults` 可靠。

### 8. 校车订单抽屉：去掉顶部跳转栏；底部按钮区固定；横向滚动优化

- 已移除表单顶部跳转快捷行；业务校验仍可 `scrollFormTo('bus-order-routes'|'bus-order-persons')`。
- **取消 / 确定** 使用 `el-drawer` **`#footer`**，固定在抽屉底部；body `padding-bottom: 0`，`el-drawer__footer` 顶部分割线；底部按钮直接置于 footer 槽内，**不再**包一层自定义 `flex + gap`。
- 横向：与先前一致（body/滚动区 `overflow-x`、表 `table-wrap`、审批行 flex）。
- 删除未使用的 `quickJump`、`quickJumpBasic` 文案（zh/en）。

### 9. uni-lib：`UniForm` 内为 `ElInputNumber` 合并默认 props；移除 `install` 中的 `setPropsDefaults`

- `uni-lib/src/components/uni-form/index.vue`：`resolveDynamicComponentProps`，对 `ElInputNumber` 注入 `controlsPosition: 'right'`（`componentProps` 后者覆盖）。
- 已撤销 `plugins/install.ts` / `UniLibInstallOptions.applyElementPlusPropDefaults`。
- `admin-web/src/main.ts`：恢复 `app.use(ElementPlus)` → `initUniLib` 的常规顺序。

### 10. 修复 `use-student-order-filters`：`matchesSchoolFilter` 误用 `Array.isBoolean`

- `Array.isBoolean` 非 JavaScript 标准 API；改为 `typeof raw === 'boolean'` 时直接不参与校区匹配。

### 11. 并列 `el-button`：统一用相邻外边距，去掉业务 footer 上的 `gap`

- `uni-lib/src/styles/element-plus.scss`：显式声明 `.el-button + .el-button { margin-left: 12px; }`，与 Element Plus 惯例一致；父级 flex **不再**依赖 `gap` 排按钮间距。
- admin-web：已去掉仅含主按钮的抽屉/弹层 footer 样式中的 `gap`（`route-form-modal` 主表单底栏与嵌套站点弹层、`email-compose-drawer`、`email-group-drawer` footer 与日期段操作行、表格行内操作 `row-actions`）。
- 仍为 **非按钮混排**（如 label+input、栅格、状态行两列）保留 `gap`。

### 12. admin-web `/base/*`：字典页文案与 `base` 国际化结构对齐

- `locales/.../modules/base.ts`：保持原先设计（共用键在 `base` 顶层，`school` / `grade` 仅存 `pageTitle`、`pageDesc`）。
- `views/base/components/dict.vue`：用 `BASE_DICT_I18N` 将 **`mt('page.title')` 等语义路径** 映射到实际键 **`base.phKeyword`、`base.label`** …，校区/年级页标题映射到 **`base.${scope}.pageTitle`** / **`pageDesc`**。

### 13. admin-web 权限 / 部门管理：树状表格 + 弹窗

- **`views/permission/dept/list.vue`**：左侧树 + 右侧抽屉式表单改为与菜单管理一致的 **`UniDataTable` 树状表格**（`default-expand-all`），层级与「部门名称 / ID / 上级 / 排序」同屏可见。
- **`list.config.ts`**：表格列、`deptTableTree`、`deptDialogFormConfig`（新增：`ElSelect` 选上级；编辑：上级与编码只读）。
- **`use-list.ts`、`components/dept-form-dialog.vue`**：行内「编辑 / 新增子部门 / 删除」；顶栏 **`permission.addDept`**（zh-CN：**「新增」**）用于在**当前选中行**下挂子部门，依赖表格 **单选高亮**（`selection="single"`）；保存仍走 `/upms/dept`。
- **`locales/.../permission.ts`**：补充 `permission.dept` 表格与弹窗文案；`permission.messages.pickDeptErr` 面向「点击表格行」提示；顶层 `addDept` 中文由「添加」改为「新增」。
- **顶栏**：仅保留「新增」（原「添加」文案）；编辑 / 删除以表格「操作」列为准。

### 14. admin-web 权限 / 用户列表：状态列回显

- **`views/permission/user/list.config.ts`**：`UniDataTable`/`UniTableCell` 的标签列只认列上的 **`options`**，误写为 `dict` 时 `resolveOption` 无法命中，会回退为原始「0」/「9」；已改为 **`options: lockOptsList`**，与 `lockFlag` 枚举（有效 / 锁定）一致。

### 15. admin-web：`UniSearchForm` 检索区 `rowProps.gutter` 统一为 8

- 各列表 `list.config` 中 **`export const searchForm` / `returnSearchForm`、`dict.config` 中 `dictSearchForm`**：`rowProps: { gutter: 16 }` → **`gutter: 8`**（或 email 先于 `schema` 的写法同步为 8）。
- **校车**：`route` 列表页内 **`searchCfg` 内联表单**中带 `colProps.span: 6` 的检索行同样改为 **`gutter: 8`**。
- **不写**：详情抽屉、新增/编辑弹窗、流程编辑等非检索 `UniForm`（仍多为 `gutter: 16`）。
- **`attendance/config/list.config`**：原无 `rowProps`，补上 **`rowProps: { gutter: 8 }`**。
- **`attendance/flow/components/flow-def-panel.vue`**：内联 `searchCfg` 补 **`rowProps: { gutter: 8 }`**。

### 16. admin-web 活动管理：`activity` 路由、节目列表收尾、节目详情起步

- `router/routes.ts`：`/activity` 分组（`redirect`→`/activity/list`），挂载活动列表、`/activity/detail`（隐藏 + `activeMenu`）、`/activity/program`、兼容旧菜单 **`/activity/program/detail`**（含 **`/index`**）带 `query.id` 重定向至 **`/activity/program/detail/:id`**，`ActivityProgramDetail` 使用 `tagDetailParam: 'id'`。
- `views/activity/program/list.vue`：表格 **`@selection-change`** 写入 `selRows`；列表列改用 `activity.colActivityId`、`colSortOrder`、`colTotalRounds`、`colCurrentRound`、`colOperator`、`colOperateTime`；批量删除改用 `confirmDeleteProgram`、`deleteOk`、工具栏 `activity.delBatch`。
- **新增** `views/activity/program/detail.vue`：`detail.get` + `normalizeEnvelope`，`el-descriptions` 展示基础字段，`programType===1` 时补充抽奖扩展块；封面图支持预览。
- 文案：`activity` zh/en（`confirmDeleteProgram`、`programDetail*`、`lottery*`、列表列头等）。
- 顺带：`activity-questionnaire.ts` 删除未使用的 `qsIds`；`basic-form.vue` 模板中避免 `|` 触发 `vue/no-deprecated-filter`（邮箱选项 `:value` 使用 `normalizeEmailOptValue`）。
- （修正）`activity-questionnaire.ts` 删除 `qsIds` 时勿删 `const path` 与 `export default {`，此前误删已恢复。

### 17. admin-web 活动管理子模块：问卷 / 奖品 / 投票 / 微信 / 邮箱 / 家长学生

- `router/routes.ts`：挂载 questionnaire、prize、vote-program、wechat-school、email-school（各含分页列表 + `:id` 隐藏详情）；`parent-student`。
- `api/modules/activity-school-email-config.ts`：新增 `appModules`。
- `views/activity/use-membership-school-options.ts`：共用校区下拉、`schoolIdsCsv`、`schoolSingleLabel`。
- 列表与简要详情对齐旧接口；奖品表格封面列 slot；微信/邮箱展示校区、`active`/应用模块等；问卷校区 CSV、枚举 tag；家长页 `parentLookup` + 学生/活动子表。
- `activity`、`route` 文案补充各确认删除、`loadDetailFail`、问卷冻结/学生信息、`parent*`、年级列等。

### 18. admin-web 活动管理子模块：奖品 / 投票节目 / 微信 / 邮箱「新增 / 编辑」

- **API**：`activity-prize`、`activity-vote-program`、`activity-wechat-school-info`、`activity-school-email-config` 各增加 `add` / `edit`（POST `/add`、`/edit`）。
- **表单弹窗**：`prize/prize-form-dialog.vue`、`vote-program/vote-program-form-dialog.vue`、`wechat-school/wechat-school-form-dialog.vue`、`email-school/email-school-form-dialog.vue`（`el-dialog` + 校验；奖品图 `protocolApi.upload`；项目下拉 `activityProgram.pickList` 带 `programTypes` 1/2；微信 `active` 数值提交；邮箱 `appModules` + 编辑时补 option）。
- **列表**：四类 `list.vue` 顶栏「新增」、操作列「编辑」、保存后 `refresh()`；筛选区奖品项目列表与表单一致为 `programTypes: ['1']`。
- **文案**：`activity` zh/en（弹窗标题、`wxSecret`、`rule*`、`uploadImageHint`、`saveOk`、`entryEdit` 等）。

### 19. admin-web 问卷管理：`add/edit/copy`、`editStatus/editFrozen`、`export`、报名链接

- **API**（`activity-questionnaire.ts`）：`add` / `edit`（JSON）、`copy`、`editStatus` / `editFrozen`（FormData，多 id）、单问卷 **`exportAnswersBlob`**（GET `/export/:id`，blob）。
- **列表**（`questionnaire/list.vue`）：顶栏「新增」→ 基本信息弹窗；工具栏批量「更改有效状态」「更改冻结状态」；行内查看 / 编辑基本信息 / 复制 / 复制报名链接；检索活动下拉 **`questionnaireFlag: 1`**；`VITE_COMMUNITY_WEB_ORIGIN` 未配置时复制链接提示配置。
- **弹窗**：`questionnaire/meta-form-dialog.vue`、`copy-dialog.vue`、`batch-flag-dialog.vue`。
- **详情**：导出问卷、`community-signup-link.ts`。
- **`env.d.ts` / `.env.development`**：`VITE_COMMUNITY_WEB_ORIGIN`（示例与 dev 对齐测试社区域名）。
- **说明**：旧系统「问卷动态表单 / 模板绑定」暂未迁入本仓库；基本信息弹窗内 `el-alert` 提示题目仍在旧表单维护。
- **文案**：`activity` zh/en。

### 20. admin-web 问卷：题目设计页 / 答卷兼容页外链

- **`questionnaire-external-urls.ts`**：统一管理 `signup`、`form`（题目设计）、`templateresult`（答卷汇总）；`VITE_QUESTIONNAIRE_EDITOR_ORIGIN` 优先，缺省沿用 `VITE_COMMUNITY_WEB_ORIGIN`。
- **列表**：顶栏「题目设计页（新建）」新开标签；行内「题目设计页」走 `type=edit&id=`。
- **详情**：同上 + 「答卷汇总（兼容页）」。
- **`env.d.ts` / `.env.development`**：补充 `VITE_QUESTIONNAIRE_EDITOR_ORIGIN` 说明行；删除冗余 `community-signup-link.ts`（迁入 `questionnaire-external-urls.ts`）。
- **文案**：`questionnaireDesigner*`、`designerUrlUnavailable`、`questionnaireAnswersLegacy`。



### 21. admin-web 问卷管理：站内设计器（动态表单）与答卷分页

- **API**：延续 `template-dynamic.ts`、`public-file.ts`（`/publik`）。


- **Builder（TS）**：`builder/types.ts`、`template-codec.ts`（known + raw 透传）、`field-presets.ts`；**表单设计 UI**：`components/questionnaire-builder.vue`。 

- **页面**：`questionnaire/list.vue`、`detail.vue`、`design.vue`、`submissions.vue`。 

- **路由**：`/activity/questionnaire/design/:id`、`/activity/questionnaire/submissions/:id`；外链仅保留家长 `signup`。 

- **文案**：`activity` zh/en、`route`。 


### 21.1 问卷设计器 Builder：交互与布局优化（不改提交结构）

- **`components/questionnaire-builder.vue`**：去掉「应用到本题」步骤；对当前题目 **debounce 深监听** 自动 `emit('update:modelValue')`，**切换选中题** 前对旧 `fontId` **同步 flush**；`syncingDraft` 避免载入草稿触发回写回路；卸载时清除定时器。
- **布局**：主区 **题型网格卡片**（题型图标 + 文案）、题目列表自适应高度滚动、右侧属性栏固定宽度；题干基础项 **分两列**，选项行 **四列栅格**（序号 | 文案 | value | 删除）；上移/下移/删除为 **图标圆形按钮**，列表项 **focus-visible** 可见焦点。
- **类型**：已知题徽章 `rowBadge` 使用 `(row as DesignerFieldKnown).type` / `DesignerFieldRaw` 收窄，`PALETTE_ICON` 使用 `vue` `Component` 类型。
- **文案**：移除已弃用的 `qbApplyPatch` / `qbApplyTip`（zh/en `activity`）。

### 21.2 问卷模块目录结构对齐（参考 `email/send`）

- **`components/`**：`batch-flag-dialog.vue`、`copy-dialog.vue`、`meta-form-dialog.vue`、`questionnaire-builder.vue`。
- **`builder/`**：仅保留设计器 **纯逻辑**（`types.ts`、`template-codec.ts`、`field-presets.ts`）。
- **`utils/`**：`questionnaire-external-urls.ts`、`submission-display.ts`。

### 21.3 问卷设计页：基础信息只读

- **`design.vue`**：问卷档案信息以 **`el-descriptions`** 展示；本页不写回 **`activityQuestionnaireApi`（既不 add 也不 edit）**。

### 21.4 问卷设计：去掉表单模板标题独立输入

- **`design.vue`**：不再有独立「表单模板标题」字段；模板 **`label`** 取 **`form.name`**（问卷名）。

### 21.5 问卷设计页：顶栏与「仅保存题目」

- **顶栏**（对齐 Holiday Flow **`edit.vue`**）：先 **「保存题目」**（Primary），垂直 **`el-divider`**，再 **「返回」**。
- **`saveQuestions`**：`outerReady` 外提示 **`qDesignNoQuestionnaire`**；仅 **`serializeTemplateBundle` → templateEdit / templateAdd + bind`**，不创建/更新问卷主表。
- **列表**：去掉「问卷设计（新建）」入口；新建问卷仅用 **新增**。


### 21.6 问卷设计器 UI：题型工具条 + 左列表右编辑

- **`components/questionnaire-builder.vue`**：题型改为顶部 **横向小按钮工具条**；主体 **左列表（标题两行 + 类型） / 右侧宽幅编辑**，编辑顶 **#序号 + Tag + 标题** 上下文条；≤960px 上下布局。**文案**：`qbPaletteTitle`、`qbTotalQuestions`、`qbListHint`、`qbEmpty`、`qbPickFirst`（zh/en）。

### 21.7 问卷设计页：版式收口（单层工作区 + 元信息排版）

- **`design.vue`**：页头 **`uni-list-page__description`**；卡片 **`q-design__card`**；提示条 **`q-design__banner`**；只读元信息 **`q-design__meta-grid`**（替换表格感 `el-descriptions`）； **`q-design__builder-head`**。**样式**：分隔线、`el-card__body` 内边距、**三栏**元信息网格（中端两栏、窄屏一栏）、**填写说明** 整行占位。
- **`components/questionnaire-builder.vue`**：**`qb__workspace`** 单层圆角外壳 + **`qb__masthead`**（题型条与列表/编辑区视觉上合一），弱化多层卡片边框；列表与编辑 **`qb__panel`** 在内壳内使用更轻的分割线。
- **文案**：`activity.questionnaireDesignPageDesc`、`activity.qMetaSectionTitle`（zh/en）。

### 21.8 问卷设计器：属性面板头部开关 + 行内默认项

- **`questionnaire-builder.vue`**：**必填 / 隐藏** 迁至灰色 **`qb__ctx`** 头部右侧（`qb__ctx-switches`），题干区仅保留「题干」输入。
- **单选框 / 单选下拉**：底部「默认值」`el-select` 移除；选项行内设 **`el-radio-group`** + 每行 `el-radio`，`title` 使用既有 **`qbDefault`** 提示默认项。
- **多选框 / 多选下拉**：底部多选下拉移除；每行 **`el-checkbox`** 维护 **`option_default`** 数组。
- **删除选项**：若当前默认值被删，单行默认回落到仍存在的第一项 ID；多选从数组去掉被删 ID。

### 21.9 问卷设计页：只读元信息三列紧凑排布

- **`design.vue`**：`q-design__meta-grid` 由 2 列改为 **3 列**（间距略收紧）；≤1080px **2 列**、≤640px **1 列**；问卷名、校区与普通项同列宽，**填写说明** 仍 **`q-design__meta-item--full`** 占满一行。


### 21.10 问卷设计器：三栏布局（题型 | 题目列表 | 右侧编辑）

- **`components/questionnaire-builder.vue`**：取消折叠面板；**左**题型网格（宽屏 **3** 列、≤960px **2** 列）、**中**题目行（拖拽手柄 + 序号 + 题型 + 标题 + 删除，点击选中 **`active`**）、**右**滚动编辑区（上下文条：**#序号 + Tag + 标题 + 必填/隐藏**，仅结构化题显示表单）。
- **`sel` / `selectedOrdinal` / `selectRow`**：中间列表点选，`selFontId` 切换前仍 **flushDraft**。
- **`locales` activity zh/en**：`qbListHint`、`qbPickFirst` 与「中间点选、右侧编辑」一致。


### 21.11 问卷设计器：中间栏加宽 + 答题预览（只读控件）

- **`questionnaire-builder.vue`**：**`qb__body`** 中间列 **`3fr`**、右侧 **`1fr`**，左侧仍为固定窄幅；列表滚动高度略抬高；移动端中间列表 **`max-height`** 增大以容纳预览块。
- 每题 **`qb__row-preview`**：**`effectiveKnown`** 在未选中时用列表快照、选中时用 **`draft`**，与右侧编辑实时一致；按需渲染 **`input`/`textarea`/单选 / 多选 / 下拉 / 日期（禁用控件）**；原题型、`isHide` 选项过滤与 **`必填 *`** 展示。
- **`locales` activity zh/en**：增补 **`qbPreviewLegacy`、`qbPreviewNoOptions`、`qbPreviewDatePh`** 等预览相关文案（后续 **`qbPreviewCaption`、`qbListHint`** 已在 **21.12** 移除）。

### 21.12 问卷设计器：去掉预览条与题目区说明文案

- **`questionnaire-builder.vue`**：删除每题卡片内 **`qbPreviewCaption`**「答题视图」行；删除「题目」列下 **`qbListHint`** 整段提示。
- **`activity` zh/en**：移除已无引用的 **`qbListHint`、`qbPreviewCaption`** 键。


### 验证

- `npm run type-check`（admin-web、`uni-lib`）通过。
