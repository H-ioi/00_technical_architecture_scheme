# 迭代日志 2026-06-26

## AI编码规则.md：原则导向重构

### 修改文件

- `AI编码规则.md`

### 核心变更

1. **硬编码数字改为原则**：删除「≤33 行」「>800 行」等固定阈值，改为可读性优先 + 约 30～50 行评估点；明确禁止为凑行数拆一次性步骤函数。
2. **常量规则精简**：「禁止提取常量」整节合并为「不要为了抽象而抽象」三条原则。
3. **工作流降阻**：复杂任务默认 MVP 先实现；仅在架构分歧、需求歧义、不可逆修改时先确认；迭代文档改为「环境支持文件修改时」才写。
4. **MVP 边界**：补充 MVP ≠ Demo，禁止 TODO 代替交付。
5. **新增章节**：规则冲突总原则、输出原则、AI 禁止行为、修改已有代码默认约束、Vue 专项（computed 优先、避免 watch 同步派生状态）。
6. **任务检查清单**：同步迭代文档条件化表述。

### 注意事项

- 与 Cursor user rules 中 Ponytail 段落若并存，以本文件为准时需手动同步 `.cursor/rules` 或 user rules 中的重复条目。

---

## AI编码规则.md：第二轮微调（9.6 → 9.9）

### 修改文件

- `AI编码规则.md`

### 核心变更

1. **决策阶梯 step 0**：修改已有项目时先理解上下文，禁止未读懂就重写。
2. **项目一致性**：§0 规则冲突下新增「项目约定优先于通用最佳实践」。
3. **MVP**：删除「可扩展」；明确无需为未来扩展增加设计。
4. **复杂需求**：改为默认完成用户要求，不再默认「先给 MVP」。
5. **分层**：由「≥2 页/≥2 处」改为「优先就近 + 已复用/明显会复用/独立领域边界」。
6. **修改已有代码**：补充风格、目录、异常、日志等与项目一致；禁止引入新编码风格。
7. **AI 禁止行为**：补充禁止为遵守规则而机械优化（DRY 碎拆、KISS 删逻辑、Native 弃成熟方案）。

---

## AI编码规则.md：定稿微调

1. 决策阶梯 step **7**：满足需求即停止，不继续优化/抽象/封装；step 3 改为「项目/平台优先」。
2. **Native First → Platform First**（项目已有方案 > 平台能力 > 新增依赖）。
3. 「无请求抽象」等移入 AI 禁止行为；Vue 顺序补 `defineOptions`。
4. 新增**异常处理**、**性能**；删除强制 `demo()/test_*`；输出原则补「先改代码」。
