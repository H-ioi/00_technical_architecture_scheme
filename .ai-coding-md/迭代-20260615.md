# 迭代日志 2026-06-15

## uni-lib：侧栏 Logo 改为 UniLogo + 名称 + 描述

### 修改文件

- `uni-lib/src/components/uni-layout/components/sidebar.vue`
- `uni-lib/src/components/uni-layout/index.vue`
- `uni-lib/src/styles/components.scss`
- `uni-lib/src/styles/variables.scss`

### 核心逻辑变更点

1. **侧栏品牌区**：默认由 `<img>` 改为 `UniLogo` + 名称 + 描述；折叠菜单时仅保留图标。
2. **框架布局联动**：根据 `frameworkLayout` / `preset` 自动切换默认文案：
   - `isa-light`：ISA SMS / 爱莎学校运营管理系统（深色字）
   - `ems-dark`：EMS / 教学业务管理系统（金色字）
   - `mas-dark`：MAS / 市场招生管理系统（金色字）
3. **可覆盖**：`UniLayout` 新增 `brandName`、`brandDescription` 可选 props；`#logo` 插槽仍可完全自定义。
4. **样式 token**：各 layout 增加 `--uni-layout-brand-color` 控制 Logo 与文字颜色。

### 注意事项

- 已移除 `UniLayout` 的 `logo` / `logoAlt` props；若业务需自定义图片 Logo，请使用 `#logo` 插槽。
- 主题设置中切换「框架布局」后，侧栏品牌文案与配色会同步更新。

## uni-lib：修复 UniLogo 生成后显示为色块

### 修改文件

- `uni-lib/scripts/generate-icons.mjs`
- `uni-lib/src/icons/generated/UniLogo.vue`（重新生成）
- `uni-lib/src/icons/svg/README.md`

### 核心逻辑变更点

1. **根因**：`logo.svg` 为内嵌 PNG + `fill="url(#pattern_0)"`；`icons:generate` 默认把非 `currentColor` 的 fill 全替换成 `currentColor`，图案引用丢失，矩形变成纯色块。
2. **修复**：生成脚本保留 `fill/stroke="url(...)"` 引用；重新执行 `npm run icons:generate`。

### 注意事项

- `logo.svg` 已替换为纯矢量 SVG（见下文）。

## uni-lib：UniLogo 改为纯矢量 SVG

### 修改文件

- `uni-lib/src/icons/svg/logo.svg`
- `uni-lib/src/icons/generated/UniLogo.vue`（重新生成）

### 核心逻辑变更点

1. **根因**：旧 `logo.svg` 内嵌 PNG 位图，颜色固定，无法随 `--uni-layout-brand-color` 切换。
2. **处理**：基于 `logo-j.png` 图标区轮廓矢量化，拆为 I / S / A 三段 `<path>`，`fill="currentColor"`。
3. **效果**：包体由 ~274KB 降至 ~3KB；ISA / EMS / MAS 布局下 Logo 可随主题色变化。

### 注意事项

- 若品牌 Logo 造型更新，请替换 `src/icons/svg/logo.svg` 后执行 `npm run icons:generate`。

## uni-lib：UniLogo 去除锯齿，改为平滑几何矢量

### 修改文件

- `uni-lib/src/icons/svg/logo.svg`
- `uni-lib/src/icons/generated/UniLogo.vue`（重新生成）

### 核心逻辑变更点

1. **根因**：上一版由 50×45 像素轮廓逐步追踪，path 全是 1px 阶梯线段，放大后锯齿明显。
2. **处理**：改为手绘几何：`I` 用 `<rect>`，`A` 用梯形 `<path>`，`S` 用三次贝塞尔曲线；并加 `shape-rendering="geometricPrecision"`。

### 注意事项

- 若 S 曲线与品牌稿仍有偏差，请在设计稿基础上微调 `logo.svg` 中 S 的 `d` 属性。

## uni-lib：UniLogo 按参考稿修正形状

### 修改文件

- `uni-lib/src/icons/svg/logo.svg`
- `uni-lib/src/icons/generated/UniLogo.vue`（重新生成）

### 核心逻辑变更点

1. **根因**：手绘贝塞尔与像素简化均与品牌稿 `Horizontal purple@3x` 偏差较大，S 形尤其变形。
2. **处理**：按参考 PNG 精确测量 I / S / A 外轮廓比例重写：`I` 矩形、`A` 梯形、`S` 按外边界关键点折线描边；`viewBox="0 0 1024 919"` 与参考稿一致。
3. **效果**：保留 `currentColor` 主题染色，边缘平滑且造型对齐参考稿。

## uni-lib：UniLogo S 形双弧改为贝塞尔曲线

### 修改文件

- `uni-lib/src/icons/svg/logo.svg`
- `uni-lib/src/icons/generated/UniLogo.vue`（重新生成）

### 核心逻辑变更点

1. **根因**：S 左右外弧用 `L` 折线连接，放大后上下两段弧度呈多边形锯齿。
2. **处理**：按参考稿边界采样，Catmull-Rom 转三次贝塞尔（`C`）；左侧外弧合并为一条连续曲线过收腰点 `(471,460)`。

## uni-lib：UniLogo 直接由品牌 PNG 追踪为纯矢量

### 修改文件

- `uni-lib/src/icons/svg/logo-source.png`（品牌稿 `Horizontal purple@3x`）
- `uni-lib/src/icons/svg/logo.svg`
- `uni-lib/scripts/trace-logo-from-png.py`
- `uni-lib/src/icons/svg/README.md`
- `uni-lib/src/icons/generated/UniLogo.vue`（重新生成）

### 核心逻辑变更点

1. **根因**：手绘/近似贝塞尔仍与品牌稿偏差，用户要求直接使用提供的 PNG。
2. **处理**：从 `logo-source.png` 提取 I / S / A 三块轮廓；I 用矩形、A 用梯形、S 用边界点 Catmull-Rom 转 `C` 曲线；`fill="currentColor"`。
3. **再生**：`python scripts/trace-logo-from-png.py` → `npm run icons:generate`。

## uni-lib-cli：同步 admin-web 模版至最新

### 修改文件

- `uni-lib-cli/templates/admin-web/**`（共 110 个文件，批量同步 + 手工修正）

### 核心逻辑变更点

1. **同步范围**：仅覆盖模版目录内已有路径，不引入 `admin-web` 全量业务模块（无 `router/modules/`、无各业务 `views/`）。
2. **批量同步**：从 `admin-web` 复制 105 个同名文件（配置、工具、权限页、API 模块、测试等）。
3. **清理废弃**：删除 `use-list.ts`（4 处）、`api/constants.ts`、误拷贝的 `.codegraph/` 与方案文档。
4. **模版专用保留**：`router/routes.ts`、`api/index.ts` 仍为精简导出；`list-table-empty.vue` 保持单文件路径并修正 role/user 列表 import。
5. **配置对齐**：`vite.config.ts` 合并 `vite.config.shared.ts` 最新能力（PWA、代理、alias 数组、分包）；`package.json` 依赖与脚本与主工程对齐（无 E2E / 三份 Vite 配置）。

### 注意事项

- 模版登录页未包含 `isa.png` 背景图（主工程有 `src/assets/images/isa.png`）；需背景时可自行添加并给 `UniLoginPage` 传 `background-image`。
- `templates/` 在 `.gitignore` 中，发布 CLI 前需在本地确认模版可用。

## uni-lib-cli：补同步 admin-web 工程配置

### 修改文件

- `uni-lib-cli/templates/admin-web/vite.config.shared.ts`（新增）
- `uni-lib-cli/templates/admin-web/vite.config.local.ts`（新增）
- `uni-lib-cli/templates/admin-web/vite.config.ts`
- `uni-lib-cli/templates/admin-web/tsconfig.node.json`
- `uni-lib-cli/templates/admin-web/package.json`
- `uni-lib-cli/templates/admin-web/.nvmrc`（新增）
- `uni-lib-cli/templates/admin-web/.gitignore`
- `uni-lib-cli/templates/admin-web/README.md`
- `uni-lib-cli/templates/admin-web/src/components/list-table-empty/index.vue`（由单文件迁入目录）

### 核心逻辑变更点

1. **Vite 三份配置**：与主工程一致（shared / local / build），`dev:force` 联调 sibling `uni-lib/dist`。
2. **Node 版本**：补充 `.nvmrc`（22）。
3. **组件路径**：`list-table-empty` 改为目录 + `index.vue`，权限 role/user 列表与主工程 import 对齐。

### 注意事项

- 模版仍不含 E2E（Playwright）与 `extract:legacy-isacommunity-base` 脚本。

## uni-lib-cli：补全脚手架源码（分支删除后恢复）

### 修改文件

- `uni-lib-cli/package.json`
- `uni-lib-cli/tsconfig.json`
- `uni-lib-cli/README.md`
- `uni-lib-cli/.gitignore`
- `uni-lib-cli/.npmrc.example`
- `uni-lib-cli/src/**`（cli / create / config / utils / types）

### 核心逻辑变更点

1. 从本地 `dist/` 反推并恢复 TypeScript 源码，结构与编译产物一致。
2. 支持交互式 / 非交互 `create` 命令，模版来源：monorepo 同级工程优先，其次 `templates/`。
3. `npm run build` 已通过，非交互创建 admin-web 冒烟验证通过。

### 注意事项

- `templates/` 仍在仓库根 `.gitignore` 中，发布 npm 前需确保包内 bundled templates 完整。
- `.npmrc` 仅本地使用，参考 `.npmrc.example`。

## uni-lib-cli：admin-web 三页模版同步脚本

### 修改文件

- `uni-lib-cli/scripts/sync-admin-web-template.mjs`（新增）
- `uni-lib-cli/package.json` — 脚本 `sync:template:admin-web`
- `uni-lib-cli/README.md`
- `uni-lib-cli/templates/admin-web/**`（脚本执行后全量重建）

### 核心逻辑变更点

1. **一键同步**：清空模版目录（保留 `.npmrc`）→ 从 `admin-web` 复制基础配置与基建 → 仅保留三个业务示例页。
2. **业务页**：工作台（`/dashboard`）、路线规划、协议管理、学生档案。
3. **自动生成**：精简版 `router/modules/*`、`api/index.ts`、中英文 `locales/lang/*/index.ts`、模版 README。
4. **依赖追踪**：同步关联 API、types、utils、composables、i18n、vitest 样例等。
5. **E2E**：同步 `playwright.config.ts`、`.env.e2e.example`、`e2e/auth.setup.ts`、`e2e/helpers/auth.ts`；`smoke`/`shell` 用例按模版路由生成。

### 使用方法

```bash
npm run sync:template:admin-web --prefix uni-lib-cli
```

## uni-lib-cli：工作台改为静态演示页

### 修改文件

- `uni-lib-cli/scripts/template-admin-web/dashboard/**`（新增静态工作台源码）
- `uni-lib-cli/scripts/sync-admin-web-template.mjs`
- `uni-lib-cli/templates/admin-web/**`（脚本执行后重建）

### 核心逻辑变更点

1. **工作台不再从 admin-web 同步**：改为写入 `scripts/template-admin-web/dashboard/` 下的静态文件（`static-demo-stats.ts` + 精简 `index.vue` + 图表组件）。
2. **移除 dashboard 专用依赖**：不再同步 `use-dashboard-stats`、6 个 dashboard 聚合 API（member-student/teacher、attendance-daily/holiday、bulk-email、school-bus-order）及对应 types / 测试。
3. **快捷入口**：仅指向模版内三个业务页（路线规划、协议管理、学生档案）。
4. **同步精简**：协议页排除未引用的 `panel.vue`；stores 不再含 `demo.ts`；e2e 仅复制 setup/helpers（不含 `.auth` 运行时产物）；`package.json` 去掉无效 `extract:legacy-*` 脚本。
5. **文案**：dashboard 国际化 `statsHintBody` 同步后自动替换为「模版演示数据」说明。

### 注意事项

- 调整演示 KPI / 图表数据请改 `scripts/template-admin-web/dashboard/static-demo-stats.ts` 后重新执行同步命令。
- 主工程 `admin-web` 工作台仍为动态聚合统计，不受影响。

## uni-lib-cli：发布版本号 0.1.1 → 0.1.2

### 修改文件

- `uni-lib-cli/package.json`

### 核心逻辑变更点

- npm 禁止覆盖已发布版本 `0.1.1`（E403），将版本升至 `0.1.2` 以便重新发布。

### 注意事项

- 在 `uni-lib-cli` 目录执行 `npm publish` 即可发布新版本。

## uni-lib-cli：新增 release 一键发布脚本

### 修改文件

- `uni-lib-cli/scripts/release.mjs`（新增）
- `uni-lib-cli/package.json`
- `uni-lib-cli/README.md`

### 核心逻辑变更点

1. **`npm run release`**：对齐 uni-lib 发布流程——构建 → 校验 `dist/index.js` 与四套模版 → `npm version` 递增 → dry-run → publish。
2. 支持 `npm run release -- minor|major` 指定版本策略。
3. 使用 `--ignore-scripts` 避免与 `prepublishOnly` 重复构建；`publishConfig.access: public` 与 uni-lib 一致。

### 注意事项

- 版本递增使用 `--no-git-tag-version`，发布后需自行 commit / 打 tag。

## uni-lib-cli：release 流程加入模版同步

### 修改文件

- `uni-lib-cli/scripts/release.mjs`
- `uni-lib-cli/package.json`
- `uni-lib-cli/README.md`

### 核心逻辑变更点

1. **`npm run release`** 流程调整为：同步模版 → 构建 → 递增版本 → publish（去掉 dry-run）。
2. 新增 **`npm run sync:templates`**，当前执行 `sync:template:admin-web`，后续可扩展其他模版。

### 注意事项

- 同步依赖 monorepo 内 `admin-web` 源工程；单独发布时需确保源工程已更新。

## uni-lib-cli：新增全局更新命令

### 修改文件

- `uni-lib-cli/src/update.ts`（新增）
- `uni-lib-cli/src/cli.ts`
- `uni-lib-cli/package.json`
- `uni-lib-cli/README.md`

### 核心逻辑变更点

1. **CLI 子命令 `uni-lib-cli update`**：执行 `npm install -g uni-lib-cli@latest` 更新全局脚手架。
2. **npm 脚本 `update:global`**：Monorepo 内快捷更新全局安装。
3. **`--version`** 改为读取 `package.json`，不再硬编码。

### 注意事项

- Windows 全局安装/更新可能需要管理员权限终端。

## uni-lib-cli：修正全局更新文档

### 修改文件

- `uni-lib-cli/README.md`

### 核心逻辑变更点

- 区分「已在 uni-lib-cli 目录」与「仓库根目录」的 `update:global` 用法，避免 `--prefix` 路径重复。
- 补充旧版全局 CLI 无 `update` 子命令时的兜底说明（先 `npm install -g uni-lib-cli@latest`）。

## 前端开发规范：Git 分支管理规范

### 修改文件

- `前端开发规范.md`

### 核心逻辑变更点

1. **新增 §10.2 Git 分支管理规范**：环境与分支映射（main/dev/feat/fix/hotfix）、命名、来源职责、feat/fix/hotfix 工作流、合并规则、发布流程、分支与 commit 类型对应、禁止事项。
2. **章节重编号**：原 §10.2 Git 提交规范 → §10.3；原 §10.3 提交要求 → §10.4。

### 注意事项

- UAT 对齐 `dev`；`main` 仅对应 PROD；**禁止 `dev` → `main`**，上线只合已验收的 `feat-*` / `fix-*`；hotfix 须 main → dev 双向同步。

## 前端开发规范：Git 分支上线策略修正

### 修改文件

- `前端开发规范.md`

### 核心逻辑变更点

1. **明确禁止 `dev` → `main`**：`dev` 为多人 UAT 集成场，上线须将**已验收的 `feat-*` / `fix-*`** 单独合入 `main`。
2. 更新工作流、合并规则表、发布流程图与禁止事项，避免误将 dev 整支发版。

### 注意事项

- 合入 `main` 前功能分支须 rebase/merge 最新 `main` 解决冲突。

## 前端开发规范：main → dev 定期同步

### 修改文件

- `前端开发规范.md`

### 核心逻辑变更点

1. **§10.2 新增「main → dev 定期同步」**：触发时机、操作命令、与禁止 `dev → main` 的区分说明。
2. 合并规则表增加「基线同步 `main` → `dev`」行；`dev` 职责与发布流程第 3 点补充定期同步要求。

### 注意事项

- hotfix 合入 main 后须立即同步 dev，不可只等定期节奏。

