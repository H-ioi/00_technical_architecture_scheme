# 迭代-20260518

## uni-search-form 样式在 admin-web 中不生效（排查结论）

**说明**：`scoped` 本身没有问题。业务侧正常引入同一版本的 `uni-ui-lib`（`dist/index.mjs` + `uni-ui-lib/style.css` / `index.css`）时，`data-v-*` 与样式选择器一致，可以正常加载。

**异常场景**：若联调时出现「组件 JS 与全局 CSS 不是同一次构建」（例如 link、`file:`、未 rebuild、或 Vite 依赖缓存混用），会出现 DOM 上的 `data-v-*` 与 `index.css` 里不一致，表现为组件样式「像没加载」。

**联调习惯**：改 `uni-lib` 后执行 `yarn uni-lib:build`（或 `uni-lib` 内 `npm run build`），必要时 `vite --force`。

## uni-lib 组件样式去掉 scoped

**范围**：`uni-lib/src` 下全部 Vue 组件的 `<style scoped>` 改为非 scoped；原 `:deep(...)` 改为在对应根类（如 `.uni-data-table`、`.uni-theme-settings`、`.uni-layout-tags`）下嵌套的普通选择器。

**目的**：打包后的 `dist/index.css` 不再依赖 `data-v-*`，业务侧 JS/CSS 不同步时布局类样式仍可命中。

**说明**：`uni-layout/index.vue` 中原先用于 Teleport 下拉的第二个 `<style lang="scss">`（`.uni-layout-user-dropdown`）保持不变。

## isacommunity 重构方案进度对齐

**范围**：更新 `admin-web/isacommunity重构方案.md` 的当前进度说明。

**内容**：将重构状态对齐为已推进到 **Phase 8 活动管理**；明确 Phase 0～Phase 7 阶段性完成，活动管理当前已接入活动列表、活动详情、活动项目、问卷管理、问卷设计与答卷页。

**注意**：该次对齐时奖品列表、投票节目、家长学生关联管理、微信配置、邮箱配置尚未独立成页；当前奖品列表、投票节目、家长学生关联管理已补齐，微信配置、邮箱配置后续仍需先核对旧项目页面、接口和权限码后再拆分落地。

## 活动管理投票节目独立页

**范围**：按旧项目 `test/old-test/src/views/isacommunity/activity/voteprogram` 与 `api/isacommunity/voteprogram.js` 对齐，在 `admin-web` 新增投票节目独立页面。

**内容**：新增 `/activity/vote-program` 路由、菜单映射、API、类型、列表配置、列表页、详情弹窗和新增/编辑弹窗；保留旧业务字段与接口：关键词分页查询、中文名、英文名、活动项目、表演者、查看、编辑、批量删除。

**说明**：投票节目活动项目下拉沿用旧逻辑，仅查询比赛类型活动项目（`programTypes: ['2']`）；交互使用 `UniSearchForm`、`UniDataTable`、`UniForm` 对齐当前重构风格。

**调整**：投票节目查看与新增/编辑复用同一个 `form-dialog.vue`，仅通过 `UniForm` 的 `mode` 在 `view` / `edit` 间切换，避免维护两套弹窗配置。

## 活动项目状态切换对齐

**范围**：`admin-web/src/views/activity/program` 与 `admin-web/src/api/modules/activity-program.ts`。

**内容**：补齐旧活动项目的 `editStatus` 能力，在活动项目列表中按状态提供「开始 / 结束」操作；切换前会校验所属活动是否已结束，避免绕过旧业务限制。

**说明**：保持当前重构风格不回退到旧弹窗/Tab 结构，继续使用 `UniSearchForm`、`UniDataTable`、`UniForm` 与现有目录拆分；活动项目详情页也增加直接 URL 编辑时的状态保护。

## 活动项目编辑入口修正

**范围**：`admin-web/src/views/activity/program/list.vue` 与 `use-list.ts`。

**内容**：取消活动项目列表的整行点击查看，改为只通过操作列「查看 / 编辑」进入详情页，避免操作按钮点击冒泡后被 `row-click` 覆盖为查看态。

## 活动项目操作按钮状态控制

**范围**：`admin-web/src/views/activity/program/use-list.ts`。

**内容**：活动项目列表操作列按每行 `programStatus`、`programType`、`totalRounds` 控制按钮显示；不可编辑的数据不再显示「编辑」，保持旧业务的待开始可编辑、进行中仅单轮抽奖可编辑、已结束不可编辑规则。

## 活动项目表单输入修正

**范围**：`admin-web/src/views/activity/program/edit`。

**内容**：为活动项目编辑页增加 `formModel` computed 适配层，将 `UniForm` 的整对象 `v-model` 更新转换为 `Object.assign` 写回当前 `reactive` 表单，解决新增/编辑页输入框无法输入的问题。

## 活动项目配额输入框显示修正

**范围**：`admin-web/src/views/activity/program/edit/index.vue`。

**内容**：移除活动项目编辑页中用于隐藏占位输入的过宽样式，避免误伤「每轮配额」插槽里的 `ElInputNumber` 内部输入框，导致只显示上下调节按钮。

## admin-web 活动奖品列表重构

**范围**：新增 `admin-web/src/views/activity/prize` 奖品列表页，补齐 `activityPrizeApi`、奖品类型、路由、旧菜单路径映射和中英文文案。

**内容**：按当前活动管理重构风格接入 `UniSearchForm`、`UniDataTable`、`UniForm`；保留旧业务的关键词/活动项目筛选、分页、多选删除、查看、新增、编辑、图片上传能力。

**兼容**：旧页面已隐藏 `prizeCount` 字段，本次继续隐藏界面字段，并在新增/编辑提交时默认传 `prizeCount = 1`，避免后端参数缺失。

**交互调整**：奖品查看和编辑统一使用同一个表单弹窗；查看时将 `UniForm` 切到 `view` 模式，编辑/新增时使用 `edit` 模式，减少重复详情 UI。

## 活动管理家长学生关联管理重构

**范围**：新增 `admin-web/src/views/activity/parent-student` 页面，并接入 `/activity/parent-student` 路由、旧菜单路径映射、API、类型与中英文文案。

**内容**：对齐旧项目 `test/old-test/src/views/isacommunity/activity/parentstudent/index.vue`，保留手机号查询、家长信息、关联学生列表、关联活动列表四块业务能力；接口沿用 `/isacommunity/activity/parentstudent/lookupByPhone`。

**说明**：该页面为只读查询能力，旧业务未提供新增、编辑、删除，本次不额外扩展操作；旧路径 `/isacommunity/activity/parentstudent/index` 与 `/isacommunity/activity/parentstudent` 已改为映射到新页面。

## 活动管理微信配置、邮箱配置重构

**范围**：新增 `admin-web/src/views/activity/wechat-school` 与 `admin-web/src/views/activity/email-school`，补齐微信配置 API、邮箱配置 CRUD API、类型、路由、旧菜单路径映射和中英文文案。

**内容**：对齐旧项目 `activity/wechatSchool`、`activity/emailSchool` 页面与接口；保留校区/关键词分页查询、查看、新增、编辑、批量删除能力，字段仍沿用微信 AppID、Secret、模板、Token、激活状态，以及邮箱地址、应用模块。

**说明**：交互统一改为当前活动模块重构风格，使用 `UniSearchForm`、`UniDataTable`、`UniForm` 与 Element Plus 描述弹窗；旧路径 `/isacommunity/activity/wechatSchool/index`、`/isacommunity/activity/emailSchool/index` 已从问卷临时承载改为独立页面。

## 活动配置弹窗查看/编辑统一

**范围**：`admin-web/src/views/activity/wechat-school` 与 `admin-web/src/views/activity/email-school`。

**内容**：微信配置、邮箱配置的查看与编辑统一复用同一个 `UniForm` 弹窗；查看时使用 `mode="view"`，新增/编辑时使用 `mode="edit"`，避免详情弹窗与表单字段重复维护。

## 活动表单弹窗编辑态回写修复

**范围**：`admin-web/src/views/activity/prize/components/prize-form-dialog.vue`、`activity/vote-program/components/form-dialog.vue`、`activity/wechat-school/components/form-dialog.vue`、`activity/email-school/components/form-dialog.vue`。

**内容**：将复用查看/编辑弹窗的 `UniForm v-model` 表单模型从 `reactive` 改为 `ref` 对象；`UniForm` 内部会整体替换 model，使用 `ref` 后编辑输入可正常回写，查看模式仍通过 `mode="view"` 保持只读展示。

## 微信/邮箱配置编辑入口修正

**范围**：`admin-web/src/views/activity/wechat-school/list.vue` 与 `admin-web/src/views/activity/email-school/list.vue`。

**内容**：移除列表整行点击查看事件，避免点击操作列「编辑」时事件冒泡后又打开查看模式；查看与编辑均通过操作列显式触发。

