# 迭代日志 2026-06-10

## 校医管理侧栏分组修复

### 修改文件

- `admin-web/src/api/modules/menu.ts`

### 核心逻辑变更点

- 补充父级菜单别名：`/isacommunity/schoolDoctor` → `/school-doctor`。
- 后端菜单树中「校医管理」父节点可匹配前端路由 `/school-doctor`，子菜单（学生档案、就诊记录等）不再被 `buildMenus` 展平为顶层项。

### 注意事项

- 与宿舍管理 `/isacommunity/dorm` → `/dorm`、活动 `/isacommunity/activity` → `/activity` 同一模式；仅缺父级别名会导致侧栏丢失分组。

---

## 学生档案 / 医疗信息 — 搜索展开收起

### 修改文件

- `admin-web/src/views/school-doctor/student-record/list.vue`
- `admin-web/src/views/school-doctor/medical-info/list.vue`

### 核心逻辑变更点

- `UniSearchForm` 的 `collapsed-rows` 由 `2` 改为 `1`，与旧页一致：默认展示第一行（学校、关键词、过敏原、定期用药）；展开后显示「是否有疾病」及学生档案的「档案状态」。
- `collapsed-rows=2` 时两行已全部展示，不会触发「展开/收起」按钮。

---

## P12 收尾（质量门禁与校医核验）

### 执行命令（`admin-web`）

- `npm run type-check` — **通过**
- `npm run lint` — **未通过**（764：3 errors、761 warnings；`--max-warnings 0`）
- `npm run lint:style` — **未通过**（10 errors，非校医域：activity/program-cards、school-bus scss、content detail-dialog）
- `npm run build` — **通过**（修复缺失导出后）

### 校医 P12 核验

- `router/modules/school-doctor.ts`：8 子路由均已注册；`layout.ts` 已挂载
- `api/modules/menu.ts`：`MENU_PATH_ALIASES` 含 `/isacommunity/schoolDoctor/**` → `/school-doctor/**`（8 模块 + 父级）

### 本次修复（构建 / 校医 lint）

- `src/utils/form-display.ts`：恢复 `formatRangeLabel`
- `src/utils/tool.ts`：恢复 `coerceIdList`
- `src/views/school-bus/route/exception/components/detail-dialog.vue`：内联 `detailCellDisplay`（原错误引用未导出符号）
- `src/views/school-doctor/medical-info/components/{form-panel,health-section,detail-drawer}.vue`：`defineModel` 消除 `vue/no-mutating-props`（4 errors → 0）

### 文档

- `admin-web/isacommunity重构方案.md`：文首「当前进度」主推 Phase 由 Phase 8 调整为 Phase 12 收尾

### P12 未完成项（诚实清单）

- 全仓 `npm run lint`：761 条 `vue/html-closing-bracket-newline` 等 warning + 3 条 error（activity `UniTableColumn` 未使用、dorm `vue/no-mutating-props`、`normalizeArray` 未使用）— 未批量 `lint:fix`（diff 过大）
- `lint:style` 10 项未修
- §15.13：P9～P11 隐藏路由 / 别名全量审计、无效样式与重复映射清理、跨模块联调回归（邮件群组、活动导入导出、考勤流程书签）未做
- 校医 8 路由手工 QA（权限菜单、抽屉提交流程）需人工

### 建议下一步

- 人工冒烟 `/school-doctor/*` 八页；或分批 `eslint --fix` + 修 3 个 error 后再跑 lint
- 内容域 **Phase 9** 与 P12 并行仅当产品排期明确（当前未启动）

### admin-web room-card 编译修复

- **文件**：dmin-web/src/views/dorm/space/room/components/room-card.vue
- **变更**：修复编码损坏导致的未闭合字符串与标签（空值回退统一为 '--'）；补全模板 }}/</span>；校区/项目/学生等标签冒号与 ieldProject 换行；恢复 gender 注释文案
- **验证**：dmin-web 下 
pm run type-check 通过

### ESLint 全量收尾（lint 子任务）

- **lint 前**：764 problems（3 errors，761 warnings）
- **lint 后**：`npm run lint` **通过**（0 errors，0 warnings，`--max-warnings 0`）
- **type-check**：`npm run type-check` **通过**

#### 手工 error 修复（非纯 format）
- `activity/.../pool-member-drawer.vue`：移除未使用 `UniTableColumn`
- `dorm/.../checkin-dialog.vue`：移除未使用 `normalizeArray`
- `dorm/.../room-card.vue`：`el-switch` 改 `:model-value` + `onStatusChange` 消除 `vue/no-mutating-props`；修复编码损坏的空值 `''--''` 与标签闭合
- `content/article/.../detail-dialog.vue`：CMS 预览 `v-html` 增加 `eslint-disable-next-line vue/no-v-html`

#### 批量 auto-fix
- `npm run lint:fix` 全仓 `src/**/*.{ts,vue}`：主要消化 `vue/html-closing-bracket-newline` 等（约 185 文件变更，含校医 34 个 vue）

#### lint:style（未改）
- 仍 **10 errors**（`:deep` 伪类、media range、`word-break: break-word` 等），与本次 ESLint 任务分开处理


### P12 收尾 — lint:style + 全绿核验（2026-06-10 续）

#### 基线（本回合开始时）
- **type-check**：PASS
- **lint**：0 errors / 0 warnings（`--max-warnings 0`；已知 3 error 已在上一子任务消除）
- **lint:style**：**10 errors** / 0 warnings

#### lint:style 修复后
- **lint:style**：**0 errors** / 0 warnings
- **lint**：0 / 0（校医 `eslint --fix` + 全仓 `lint:fix` 无新增问题）
- **type-check**：PASS
- **build**：PASS

#### 变更文件（本回合 style 收尾，4 文件）
- `admin-web/.stylelintrc.cjs`：根规则对齐 recommended-vue，忽略 `:deep` / `v-deep` 等 Vue 伪类（修复 standalone `.scss` 的 `bus-order-form-dialog.scss` 误报）
- `admin-web/src/views/activity/detail/tabs/program-cards.vue`：`@media (width <= …)`（`lint:style:fix` / media-feature-range-notation）
- `admin-web/src/views/content/article/list/components/detail-dialog.vue`：`overflow-wrap: break-word` 替代 deprecated `word-break: break-word`
- `admin-web/src/views/content/discussion/list/components/detail-dialog.vue`：同上

#### 计数对比
| 命令 | 前 | 后 |
| --- | --- | --- |
| `npm run lint` | 0 / 0 | 0 / 0 |
| `npm run lint:style` | 10 / 0 | 0 / 0 |

#### P12 仍待产品/人工（非 CI 阻塞）
- §15.13：隐藏路由/别名全量审计、无效样式与重复映射清理、跨模块联调回归
- 校医 8 路由手工 QA（权限菜单、抽屉提交流程）


---

## stylelint 收尾与 P12 §15.13 四门验证

### stylelint 前后

- **修复前**（本轮任务基线前已知）：
pm run lint:style **10 errors**
  - media-feature-range-notation ×4：program-cards.vue 中 @media (max-width: …)
  - selector-pseudo-class-no-unknown ×6：us-order-form-dialog.scss 中 Vue :deep
  - declaration-property-value-no-unknown ×2：article/discussion detail-dialog.vue 中 word-break: break-word
- **修复后**（工作区已含 e9cf46de 子任务部分改动；本任务 fresh baseline）：**0 errors**

### 修改文件与手段

- dmin-web/.stylelintrc.cjs：selector-pseudo-class-no-unknown / selector-pseudo-element-no-unknown 增加 deep / -deep 等 Vue 穿透伪类忽略（与全项目 :deep 写法一致，无需改 scss）
- dmin-web/src/views/activity/detail/tabs/program-cards.vue：@media (max-width: 992px|576px) → (width <= 992px|576px)
- dmin-web/src/views/content/article/list/components/detail-dialog.vue：word-break: break-word → overflow-wrap: break-word
- dmin-web/src/views/content/discussion/list/components/detail-dialog.vue：同上

### P12 四门验证（dmin-web，本任务终验）

| 命令 | 结果 |
| --- | --- |
| 
pm run type-check | **PASS** |
| 
pm run lint | **PASS**（0 errors，0 warnings） |
| 
pm run lint:style | **PASS**（0 errors） |
| 
pm run build | **PASS** |

### P12 §15.13 仍须人工 QA（自动化未覆盖）

- P9～P11 隐藏路由与 MENU_PATH_ALIASES 全量审计
- 清理无效样式、重复映射、未使用权限码占位
- 跨模块联调回归：邮件群组、活动导入导出、考勤流程书签路由
- 校医 /school-doctor/* 八页权限菜单与抽屉提交流程冒烟

### 阻塞项

- 无自动化门禁阻塞；上述人工项为产品/回归范围，非本次 stylelint 任务 blocker。

---

## 重构方案文档 — P12 状态同步

### 修改文件

- `admin-web/isacommunity重构方案.md`

### 核心逻辑变更点

- 文首「当前进度」、§〇 当前状态、§2.1 P12 行、§15 阶段表与 §15.13：标记 **P12 CI 门禁已完成 ✅**（type-check / lint / lint:style / build 均 PASS）。
- P12 整体标为 **部分完成**；§15.13 别名审计、跨模块回归、校医八路由冒烟单独标 **待人工/进行中**，与 CI 完成态区分。

---

## Phase 9 内容域 — 启动盘点（2026-06-10）

### 结论

- 对照 §15.10 / §14.2：**9 个子模块均已迁移**，本 session **无新增代码**（避免重复实现）。
- `npm run type-check`：**PASS**；内容域 `eslint` 抽检：**PASS**（0 issues）。

### Phase 9 范围（计划）

| 优先级 | 模块 | 新路由 | 旧路径 | API 前缀 |
| --- | --- | --- | --- | --- |
| 1 | 公告内容 | `/content/announcement` | `/isacommunity/content/announcement/index` | `/isacommunity/content/announcement` |
| 2a | 一周食谱 | `/content/moment/food-weekly` | `/isacommunity/content/moent/foodweekly/index` | `/isacommunity/content/foodweekly` |
| 2b | 校园生活 | `/content/moment/school-life` | `/isacommunity/content/moent/schoollife/index` | `/isacommunity/content/moment` |
| 3a | 文章内容 | `/content/article/list` | `/isacommunity/content/article/list/index` | `/isacommunity/content/article` |
| 3b | 文章分类 | `/content/article/category` | `/isacommunity/content/article/category/index` | `/isacommunity/content/category` |
| 4a | 讨论列表 | `/content/discussion/list` | `/isacommunity/content/discussion/list/index` | `/isacommunity/content/discussion` |
| 4b | 讨论标签 | `/content/discussion/tag` | `/isacommunity/content/discussion/tag/index` | `/isacommunity/content/discussion-tag` |
| 4c | 讨论评论 | `/content/discussion/comment` | `/isacommunity/content/discussion/comment/index` | `/isacommunity/content/comment` |
| 4d | 点赞收藏 | `/content/discussion/like-save` | `/isacommunity/content/discussion/likeandsave/index` | `/isacommunity/content/like-and-favor` |

### 已迁移 vs 旧代码差异

- **已迁移**：上表 9 模块 — `views/content/**`、`router/modules/content.ts`、`api/modules/content-*.ts`、`types/modules/content-*.ts`、`locales/lang/*/modules/content.ts`、`menu.ts` 别名。
- **旧代码有、计划未列**：`test/old-test/.../content/navbutton/`（导航按钮，`/isacommunity/content/navigate-button`）— 未接入 admin-web；§14.2 无菜单映射，待产品确认。

### 本 session 修改文件

- `admin-web/isacommunity重构方案.md` — 文首进度、§15 阶段表、§15.10 状态与验收清单
- `.ai-coding-md/迭代-20260610.md` — 本条目

### 建议下一步

1. **人工 QA**：9 路由权限菜单、富文本/封面上传、详情抽屉、多语言标题与状态切换。
2. **P12 别名审计**：确认 `/isacommunity/content/**` 无遗漏 path（含历史 bookmark）。
3. **可选**：若菜单仍含「导航按钮」，单独立项迁移 `navbutton`（含 `el-transfer` 关联文章、图标上传）。

