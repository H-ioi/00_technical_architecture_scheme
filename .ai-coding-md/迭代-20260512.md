# 迭代-20260512

## Phase 6 考勤：请假管理列表（对齐旧页）

- **依据**：`test/admin-web/old/src/views/isacommunity/attendance/holiday/index.vue`、`test/old-test/src/api/isacommunity/holiday.js`（`GET /attendance/holiday/page`、`GET /attendance/holiday/:id`、`GET /attendance/holiday/back/:procId/:id`）；校区筛选取值与旧页一致为 **`enName`** → 请求参数 **`studentSchool`**。
- **路由**：`/attendance/holiday`（挂在「学生每日考勤」之后）；`menu.ts` 中旧路径映射已存在，无需改。
- **实现**：
  - `api/modules/attendance-holiday.ts`、`types/modules/attendance-holiday.ts`
  - `views/attendance/holiday/*`：`UniSearchForm` + `UniDataTable`；**查看**使用 **`el-drawer` + `UniForm` 查看态**（方案 2.5.1）；打开时请求详情接口并 `formatHolidayDetailView`。
  - **撤回**：条件与旧页一致（`procId` 存在、`status !== '102'`、`!isEnd`）；调用双路径参数 **`procId` + `id`**（旧页曾只传 `procId` 与接口签名不一致，此处按 `holiday.js` 修正）。
  - **新增**：按钮保留，点击 **`ElMessage.info`** 说明下一阶段对齐 **`dialog/add.vue`**、公共上传 **`/publik/file/upload`** 与 **`saveHoliday` / `updateHoliday`** 后再实现完整表单。
- **i18n**：`locales/lang/*/modules/attendance.holiday`、`route.attendanceHoliday`。

## 请假管理：请假 / 销假 Tab（对齐路线规划聚合页）

- **交互**：参考 `views/school-bus/route/plan/tab.vue`——页头说明 + `el-tabs`，各 Tab 内独立 `UniSearchForm` + `UniDataTable`；校区由 `tab.vue` `onMounted` 拉取一次 `membershipApi.school` 注入两个 composable。
- **入口**：路由组件由 `list.vue` 改为 **`tab.vue`**（`/attendance/holiday` 不变）。
- **接口**：销假列表 **`GET /attendance/holiday-return/return-page`**（`holiday.js` → `listHolidayEnd`）；检索 **`attendanceHolidayReturnSearchForm`**：仅 **学校 + 学号/姓名**；请求体 **`AttendanceHolidayReturnListParams`** 仅 `keyword`、`studentSchool`（分页）。
- **代码**：`holiday-utils.ts`（分页/详情 unwrap）；`use-holiday-leave.ts`、`use-holiday-return.ts`（销假 Tab 仅「查看」，不调撤回）；`attendance.holiday.tabs.leave` / `tabs.return` 文案。
- **销假表格字段**：`return-page` 返回常与请假分页字段不一致（嵌套 `holiday`、`admissionNo`/`studentAdmissionNo`、snake_case 等）。已增加 **`normalizeHolidayReturnRow`** 摊平嵌套 VO 并写入请假同款 prop；**查看详情**优先 **`holidayId`/`leaveId`**，否则回落 **`id`**。
- **销假列表列**：与旧「销假管理」截图一致：**学号、姓名、学校、年级、班级、返校时间、创建时间**（另由表格组件渲染「查看」操作列）；**不含**原请假时段、销假说明、请假单号、状态等扩展列。
