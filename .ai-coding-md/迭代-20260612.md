# 迭代日志 2026-06-12

## admin-web：本地联调 uni-ui-lib 直读 sibling dist

### 修改文件

- `admin-web/vite.config.ts`
- `admin-web/src/uni.ts`
- `uni-lib/package.json`

### 核心逻辑变更点

1. **Vite alias**：检测到 `../uni-lib/dist/index.mjs` 存在时，将 `uni-ui-lib` 与 `uni-ui-lib/style.css` 统一解析到 sibling `uni-lib/dist`，避免 `file:../uni-lib` 在 Windows 复制到 `node_modules` 后 rebuild 仍读到旧 dist。
2. **uni.ts**：恢复标准 `import from 'uni-ui-lib'`，不再手写相对路径，避免与业务页 `uni-ui-lib` 导入形成双模块。
3. **uni-lib**：新增 `dev:watch`（`vite build --watch`），改组件库后自动更新 dist。

### 注意事项

- 仍指向 **dist**，改 `uni-lib/src` 后需 `yarn dev:watch --cwd ../uni-lib` 或手动 `build`；admin-web 保存 dist 后刷新即可。
- 无 sibling `uni-lib/dist` 时（如仅安装 npm 版）自动回退 `node_modules/uni-ui-lib/dist`。
- dist 更新后若样式仍旧，可 `yarn dev:force`。
- `uni-ui-lib` alias 须用 `/^uni-ui-lib$/` 精确匹配，不能用字符串前缀，否则会误解析 `uni-ui-lib/style.css` → `index.mjs/style.css` 报错。

## uni-lib：统一动效 token 与基础过渡

### 修改文件

- `uni-lib/src/styles/variables.scss`
- `uni-lib/src/styles/base.scss`
- `uni-lib/src/styles/components.scss`
- `uni-lib/src/components/uni-layout/components/tags.vue`
- `uni-lib/src/components/uni-login-page/security-verify-dialog.vue`
- `uni-lib/src/components/uni-search-form/index.vue`
- `uni-lib/src/components/uni-data-table/components/toolbar.vue`
- `uni-lib/src/utils/pwa.ts`

### 核心逻辑变更点

1. **Motion token**：在 `:root` 增加 `--uni-duration-*`、`--uni-ease-*` 及 `--uni-transition-colors/layout/opacity/transform/tag-close` 等组合变量，供全局与组件引用。
2. **无障碍**：`base.scss` 增加 `prefers-reduced-motion: reduce` 兜底，系统开启减少动效时压缩 animation/transition 时长。
3. **现有动效对齐 token**：layout sidebar/header、tags 关闭按钮、验证码滑块、PWA 卡片等硬编码 `0.2s ease` 改为 CSS 变量。
4. **复用过渡类**：`components.scss` 新增 `.uni-fade-*`（Vue `<Transition name="uni-fade">`）；`uni-search-form` 已选标签区使用该过渡。
5. **微交互补充**：layout 菜单项、表格 toolbar 按钮、搜索表单收起链接触发 `--uni-transition-colors`。

### 注意事项

- Dialog / Drawer / Popover 等仍依赖 Element Plus 内置进出场，未重复封装。
- PWA 注入样式通过 `var(--uni-*)` 读取 token，需确保页面已加载 uni-lib 样式。
- 业务页新增动效应优先引用 `--uni-transition-*`，避免再写裸 duration/easing。

## uni-lib：isa-light 主题视觉微调

### 修改文件

- `uni-lib/src/styles/components.scss`

### 核心逻辑变更点

1. **布局层级**：侧栏/顶栏/内容区统一 1px 浅分割线；内容区圆角 16px、内边距 24px，减少「白块堆叠」感。
2. **列表页卡片**：`uni-list-page__body` 改为轻边框 + 双层柔和阴影；搜索区与表格区间加分隔线，间距 20px。
3. **侧栏菜单**：字号 15/13px、项间距 2px；子菜单选中改为浅蓝底 pill，与一级选中态一致。
4. **表单/按钮/表格**：输入框白底 36px 高、聚焦 2px 光晕；按钮统一高度与 hover；表格行高/字号/表头/分页选中态统一主题色。
5. **标签页/状态 Tag**：页签 pill 化；`el-tag` 统一边框 + 浅色底，与主色体系对齐。

### 注意事项

- 仅 `[data-layout='isa-light']` 生效，不影响 ems-dark / mas-dark。
- 修改后需 `yarn build`（uni-lib）或 dev 热更新后刷新 admin-web 查看效果。

## admin-web：修复 UniForm v-model 与 reactive 编译告警

### 修改文件

- `admin-web/src/views/permission/role/list.vue`
- `admin-web/src/views/attendance/holiday/components/holiday-form-drawer.vue`
- `admin-web/src/views/attendance/pass/components/pass-form-dialog.vue`
- `admin-web/src/views/activity/questionnaire/components/copy-dialog.vue`
- `admin-web/src/views/activity/questionnaire/components/batch-flag-dialog.vue`
- `admin-web/src/views/activity/questionnaire/components/meta-form-dialog.vue`
- `admin-web/src/views/activity/program/components/program-copy-dialog.vue`

### 核心逻辑变更点

1. **根因**：`UniForm` 的 `v-model` 会整体替换 model 对象，不能直接绑在 `const form = reactive(...)` 上，否则触发 `[@vue/compiler-sfc]` 告警。
2. **修复**：沿用项目既有写法（如活动报名编辑弹窗），增加 `formModel` / `roleFormModel` 计算属性，`get` 返回 reactive 对象，`set` 用 `Object.assign` 合并更新，模板改绑计算属性。

### 注意事项

- 业务逻辑仍读写原 `form` / `roleForm` reactive 对象，无需改提交与校验代码。
- 新建 `UniForm v-model` 时优先 `ref` 或上述 computed 桥接，避免 `const reactive` 直绑。

## uni-lib：修复 defineModel + v-model 编译告警

### 修改文件

- `uni-lib/src/components/uni-search-form/index.vue`

### 核心逻辑变更点

1. **根因**：`defineModel()` 默认绑定名为 `model`；模板 `<UniForm v-model="model">` 会编译成对 `model` 的赋值，`const model = defineModel(...)` 触发 `[@vue/compiler-sfc]` 告警。
2. **修复**：将 `const model` 改为 `let model`（Vue 官方推荐写法，与编译器自动降级一致）。

### 注意事项

- 同类写法（如 `toolbar.vue` 的 `stripe`/`border`、`theme-settings` 的 `visible`）若出现同名告警，同样改为 `let`。

## uni-lib：Element Plus 表头全局样式重置

### 修改文件

- `uni-lib/src/styles/element-plus.scss`

### 核心逻辑变更点

1. **表头背景**：`--el-table-header-bg-color` 与 `th.el-table__cell` 统一为 `rgba(241, 245, 251, 1)`。
2. **表头边框**：移除表头单元格上下 border，仅保留左右分隔（由 EP 列边框逻辑负责）。

### 注意事项

- `uni-data-table` 组件内仍有 `#fafbfc` 表头背景覆盖，若列表页未生效需同步调整该组件 scoped 样式或 rebuild uni-lib dist。

## uni-lib：布局顶栏全局菜单搜索

### 修改文件

- `uni-lib/src/components/uni-layout/components/search.vue`（新增）
- `uni-lib/src/components/uni-layout/components/header.vue`
- `uni-lib/src/components/uni-layout/index.vue`
- `uni-lib/src/locales/lang/zh-CN/layout.ts`
- `uni-lib/src/locales/lang/en/layout.ts`
- `uni-lib/src/styles/components.scss`

### 核心逻辑变更点

1. **GlobalSearch**：基于侧栏菜单树扁平化，按标题/路径/父级路径过滤，选中后跳转路由。
2. **HeaderBar**：顶栏右侧、语言切换与用户区之间插入搜索框；支持 `#header-search` 插槽与 `showGlobalSearch` 开关。
3. **UniLayout**：`autoWire` 模式下自动注入 `menuStore.menuRoutes` 并处理 `@search-select` 导航。

### 注意事项

- 需 rebuild uni-lib dist 后 admin-web 联调可见。
- 自定义顶栏时可传 `:show-global-search="false"` 或使用 `#header-search` 覆盖。

## admin-web：Vite 配置拆分为本地联调与上线构建

### 修改文件

- `admin-web/vite.config.shared.ts`（新增）
- `admin-web/vite.config.local.ts`（新增）
- `admin-web/vite.config.ts`
- `admin-web/package.json`
- `admin-web/tsconfig.node.json`
- `admin-web/README.md`

### 核心逻辑变更点

1. **vite.config.shared.ts**：抽取 PWA、代理、build 分包、SCSS、公共 alias 等共用配置。
2. **vite.config.local.ts**：`dev` / `dev:force` 使用；保留 sibling `uni-lib/dist` alias 与 `optimizeDeps.exclude`。
3. **vite.config.ts**：`build` / `preview` 使用；走 `node_modules` 标准解析，不含本地联调 hack。

### 注意事项

- 改 uni-lib 组件库本地开发：`yarn dev`（自动读 `vite.config.local.ts`）。
- CI / 上线构建：`yarn build`（读 `vite.config.ts`）。
- dist 更新后样式仍旧：`yarn dev:force`。

## uni-lib：登录页背景图避免打入 CSS/JS 包体

### 修改文件

- `uni-lib/src/components/uni-login-page/index.vue`
- `uni-lib/vite.config.ts`

### 核心逻辑变更点

1. **移除 SCSS `url()`**：`background-image` 改由 `backgroundImage` prop + `:style` 绑定，避免 PostCSS 将图片 base64 写入 `index.css`。
2. **库内不默认打包大图**：`backgroundImage` 默认空字符串，仅保留渐变底；宿主自行 `import` 或放 `public` 后传入 URL。
3. **构建配置**：`build.assetsInlineLimit: 0`，小图也输出为独立资源路径，不内联进 JS。

### 注意事项

- 使用背景图：`<UniLoginPage :background-image="loginBgUrl" ... />`，`loginBgUrl` 由业务侧 `import xxx from './login-bg.png'` 或静态路径提供。
- `isa.png` 约 1.6MB，不宜作为组件库默认资源；建议压缩后放宿主 `public` 或业务 assets。

## admin-web：登录页引入背景图

### 修改文件

- `admin-web/src/views/login/index.vue`

### 核心逻辑变更点

1. 业务侧 `import loginBgUrl from '@/assets/images/isa.png'`，通过 `:background-image` 传给 `UniLoginPage`。

### 注意事项

- 图片由 admin-web 构建管线输出为独立资源，不进入 uni-lib 包体。

## uni-lib：登录页左侧文案边缘发光

### 修改文件

- `uni-lib/src/components/uni-login-page/index.vue`

### 核心逻辑变更点

1. `&__intro` 增加多层白色 `text-shadow`，标题/副标题/描述在背景图上更易辨认。
2. `&__logo` 重置 `text-shadow: none`，避免深色徽标上出现多余光晕。

### 注意事项

- 修改后需 rebuild uni-lib dist 或 dev:watch 后刷新 admin-web。

## uni-lib：图标生成命名去掉 Icon 后缀

### 修改文件

- `uni-lib/scripts/generate-icons.mjs`
- `uni-lib/src/icons/svg/README.md`
- `uni-lib/src/icons/index.ts`（重新生成）
- `uni-lib/src/icons/generated/*.vue`（重新生成）
- `uni-lib/src/utils/icons.ts`
- `uni-lib/src/components/uni-layout/components/header.vue`

### 核心逻辑变更点

1. `generate-icons.mjs`：`bus-school.svg` → `UniBusSchool`（原为 `UniBusSchoolIcon`）。
2. 引用处同步：`UniZhEnIcon` → `UniZhEn`（`icons.ts`、`header.vue`）。

### 注意事项

- 新增 SVG 后执行 `yarn icons:generate --cwd uni-lib`。
- 业务若直接 import 旧 `*Icon` 导出名需改为无后缀版本。

## uni-lib：顶栏全局搜索图标与占位文案

### 修改文件

- `uni-lib/src/components/uni-layout/components/search.vue`
- `uni-lib/src/locales/lang/zh-CN/layout.ts`
- `uni-lib/src/locales/lang/en/layout.ts`

### 核心逻辑变更点

1. 输入框前缀图标由 EP `Search` 改为库内 `UniSparkles`。
2. 占位文案：`智能搜索` / `Smart search`。

### 注意事项

- 修改后需 rebuild uni-lib dist 或 dev:watch 后刷新 admin-web。
