# 迭代日志 2026-07-02

## 代码校验Skill.md：三源公共规则整合

### 修改文件

- `代码校验Skill.md`

### 核心变更

1. **补充 Skill frontmatter**：`code-review-common`，说明跨项目校验用途与平台 Skill 裁决关系。
2. **量化阈值**（来自 uni-review-mobile / uni-review-web）：33 行函数、5 行内联、440/800 行 `.vue`、2+ 模板块抽组件。
3. **公共分层与目录**：request → api → composable/页面 → 组件；`composables`/`utils` 判定表；类型放 `types/modules`。
4. **内联原则 §5**：步骤函数禁止清单 + 常量抽离判定表（合并 AI编码规则与双端 Skill §7/§8）。
5. **质量门禁**：lint、type-check、test:run、禁止 console/debugger、Conventional Commits。
6. **Critical 禁止清单、评审输出格式、Finish Checklist**；平台专项指向 `uni-review-mobile` / `uni-review-web`。

### 注意事项

- 本文档为**公共子集**；移动端/后台差异（如 `uni-ui-lib`、`pages.json`、权限码内联）不在此重复，校验时须叠加对应 Skill。

---

## 代码校验Skill.md：全前端通用规范扩展（第二轮）

### 修改文件

- `代码校验Skill.md`

### 核心变更

1. **定位升级**：Vue/uni 子集 → 全前端（Vue / React / UniApp / SSR / Electron）AI Agent 公共规则。
2. **§0.1 设计原则总纲**：简单>抽象、组合>继承、局部性>全局等 8 条。
3. **新增跨框架模块**：组件设计（§4）、状态管理（§5）、TypeScript（§9）、API/DTO（§10）、异步（§11）、React 专项（§13）、CSS（§14）、性能（§15）、i18n（§16）、a11y（§17）、安全/XSS（§18）、错误处理（§19）、日志（§20）、Import 顺序（§21）、Git 原子提交（§25）。
4. **§26 AI 常见错误**：过度抽象、Base 组件、错误分层、类型偷懒等 Critical 清单。
5. **§1/§5 去 Pinia 绑定**：改为局部状态优先 + 项目自选 Store 实现。
6. **组件行数**：`.vue` / `.tsx` 统一 440/800 阈值；Finish Checklist 覆盖 React/a11y/安全项。

### 注意事项

- Vue 专项保留为 §12；React 为 §13；Next/Nuxt 等平台细节仍由各项目 Skill 补充。

---

## AI-代码校验Skill.md：原则导向微调（第三轮）

### 修改文件

- `AI-代码校验Skill.md`（自 `代码校验Skill.md` 更名）

### 核心变更

1. **阈值裁决总纲**：数字仅作预警；禁止为满足行数机械拆分。
2. **33 行**：「必须拆」→「评估独立业务动作；单一完整流程可保留」。
3. **删除 440 硬限**：保留 **>800 必须拆**；**400～500 建议评估**。
4. **§5.1 数据流**：状态来源链、单一数据源、禁止 watch/effect 同步 derived、禁止重复状态与原地 sort。
5. **§1.1 依赖原则**：新增依赖三条件；禁止为 deepClone 引 lodash。
6. **TS**：双重断言改为「避免，确需须注释」。
7. **异步**：写操作并发须评估；性能补「禁止为优化增加同步成本」。
8. **CSS**：spacing/font/radius token；禁止 13px/17px 等魔法间距。
9. **Import 顺序**：types → constants → utils → api → store → hooks → components。
10. **复用抽取**：「第 3 次必须」→「稳定复用时」；Critical/Checklist 补重复状态与 derived 项。

### 注意事项

- 后续迭代宜基于 AI 误判案例增补 Checker 项，而非在 Global 重复评审流程。

---

## Guide / Checker 职责拆分 + Cursor 目录

### 修改/新增文件

- `AI-全局编码规则.md` — 重写为 **Guide**（怎么写）
- `AI-代码校验Skill.md` — 重写为 **Checker**（CHK-xxx 检查项 + 判定树）
- `.cursor/rules/global.mdc` — 同步 Global Rule，`alwaysApply`
- `~/.cursor/skills/code-review/SKILL.md` — 同步 Code Review Skill（用户级）

### 核心变更

1. **职责分离**：Global 删 Finish Checklist/Review 格式；Review 删决策阶梯/工作流/设计思想长文。
2. **重复收敛**：同一规则 Global 写「怎么写」，Review 写「CHK-xxx 怎么判」；约 20% 必要映射。
3. **Checker 格式**：`检查 → YES/NO → Critical/Warning/Pass → 修复`。
4. **Cursor 官方结构**：rules（alwaysApply）+ skills（code-review）。

### 维护约定

- 改 Global → 同步 `global.mdc`
- 改 Review → 同步 `~/.cursor/skills/code-review/SKILL.md` + `checks.md`

---

## Cursor Skill 落地：code-review（用户级）

### 安装位置

- `~/.cursor/skills/code-review/SKILL.md` — 工作流、快速扫描、质量门禁、输出格式、Finish Checklist
- `~/.cursor/skills/code-review/checks.md` — CHK-xxx §1–§17 判定树（与 `AI-代码校验Skill.md` 同步）

### 说明

- **用户级 Skill**，跨项目可用；与 `uni-review-web` / `uni-review-mobile` / `uni-review-pc` 同级
- Skill 名 `code-review-common`；触发词含 review/校验/自查/代码评审
- 平台专项仍叠加对应平台 Skill
