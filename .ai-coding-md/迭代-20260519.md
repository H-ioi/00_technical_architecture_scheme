# 迭代 20260519 — 反模式工具方法整改

## 修改文件（摘要）

- `questionnaire-builder.vue` / `use-questionnaire-builder.ts` / `questionnaire-builder-utils.ts`
- `program-edit-payload.ts` / `program-edit-detail.ts` / `use-program-edit.ts`
- `utils/tool.ts`（列表/考勤直接使用 `dateFormat`，无额外包装）
- `composables/use-activity-yes-no-options.ts`、`use-membership-school-options.ts`
- 考勤 6 个 `use-list.ts`、`holiday-utils.ts`
- 活动列表 `list|prize|program|questionnaire|email-school|wechat-school` 的 `use-list.ts`
- 活动详情 Tab：`blessing|winner|vote-info|feedback|checkin|registration.vue`

## 核心变更

1. **问卷设计器**：`.vue` 仅保留模板与 `useQuestionnaireBuilder`；预览区用 `buildKnownFieldPreview` 一次算出展示字段，删除 10+ 个单步 `preview*` 函数及 ~750 行重复 script。
2. **节目编辑**：`emptyProgramForm` / `buildProgramSubmitPayload` / `applyProgramDetailToForm` 等迁入独立模块，`use-program-edit` 只编排流程。
3. **时间与选项**：列表/考勤时间列直接调用 `dateFormat`；`useMembershipSchoolOptions`、`useActivityYesNoOptions` 供多列表复用。
4. **列表行装饰**：活动详情 Tab 将 `normalizeTime` / `blank` / `pick*` 内联进 `decorateRows`，避免单步箭头函数。

## 第二轮（续改）

- 问卷：`meta-form-dialog`、`batch-flag-dialog`、`edit.vue` 接入 `useActivityYesNoOptions` / `useMembershipSchoolOptions`（含 `formatSchoolIdsCsv`）。
- 活动：`use-activity-detail-page`、`list/use-list` 接入 composable；`detail-form.config` 去掉 `modelValue`、合并 `optionLabel`。
- 考勤：student/access/school/wechat/wechat-notice 的 `*Label` 小函数并入 `decorateRow`。
- 其它：`parent-student` 内联 `displayValue`；`use-dashboard-stats` 内联 `dayStr`；email/wechat 表单内联 `normalizeSchoolIdForSelect`；`program-cards` 合并 rule 相关单步 helper；`feedback` 内联 `satisfactionLabel`。

## 注意事项

- `npm run build` 已通过。
- `program-edit-form.config.ts` 中 `dp()` 未动（单文件内大量复用，属配置层折中）。
- `questionnaire/edit.vue` 仍保留 `mutationId`、`serializeFieldForApi` 等完整业务函数；`program-cards` 保留 `programTypeText` / `statusText`（多分支展示逻辑）。

---

## 页面描述（pageDesc）规范化

### 修改文件

- `locales/lang/{zh-CN,en}/modules/`：`activity`、`attendance`、`email`、`permission`、`base`、`school-bus`、`school-bus-more`、`protocol`
- `views/activity/detail/`、`program/edit/`、`questionnaire/submissions.vue`
- `views/attendance/flow/list.vue`、`views/protocol/detail.vue`

### 核心变更

1. 将含「对齐旧版」、接口路径、权限码等技术备注的 `pageDesc` / `description` 改为面向业务的正式说明（中英文同步）。
2. 为原先缺描述的页面补充文案：活动详情/项目编辑、问卷答卷列表、协议详情。
3. 流程设计列表页固定展示 `pageDesc`，不再在「流程定义」Tab 误用 `deployHint` 作为页头说明。

### 注意事项

- 问卷设计/详情、流程编辑等页的 `pageDescription` 类文案已保留业务语义，仅去掉开发备注。

## 第三轮（续改）

### 修改文件

- `attendance/holiday/holiday-utils.ts`
- `school-bus/route/plan/components/route-form-modal.vue`
- `school-bus/student/components/bus-order-form-dialog.vue`、`order-detail-dialog.vue`
- `school-bus/follow-teacher/use-list.ts`
- `utils/locale-name.ts`（新建，供校车订单双语展示）

### 核心变更

1. **销假归一化**：模块级 `firstNonEmptyField` / `firstNonEmptyString` 替代 `normalizeHolidayReturnRow` 内嵌 `pickScalar` / `pickStr`。
2. **线路表单**：`loadSchoolDependencies` 合并三处 `fetch*List` 与重复请求；去掉 `deepClone`，就地 `JSON.parse(JSON.stringify(...))`。
3. **校车订单**：`loc` / `sectionLabel` / `lineLabel` / `stationOptLabel` 统一为 `@/utils/locale-name` 的 `pickLocaleName`。
4. **跟车老师列表**：`schoolLabel` 单行 helper 内联进 `decorate`。

### 注意事项

- `program-edit-form.config.ts` 的 `dp()`、`program-cards` 的 `programTypeText` / `statusText` 仍保留。
- 校车其它列表内 `const loc = locale()` 为局部变量缓存，非单步导出函数，本轮未改。

## 第四轮（反模式续改）

### 修改文件

- 活动：`detail/index.vue`、`tabs/program-cards.vue`、`questionnaire-content.vue`、`parent-student/use-list.ts`、`tabs/checkin.vue`
- 考勤：`holiday/use-holiday-leave.ts`、`use-holiday-return.ts`；`access|school|student|daily/use-list.ts`
- 校车：`car|driver|follow-teacher/use-list.ts`；`route/operation|exception/use-list.ts`；`route/plan/tab.vue`、`use-route-lines.ts`
- 校车订单：`order-detail-dialog.vue`
- 公共：`composables/use-list-table-empty.ts`、`utils/download.ts`

### 核心变更

1. 删除单次/微 helper：`uploadRegistrationTickets`、`pickQuestionnaireId`、`deleteVisible`/`withdrawVisible`/`holidayBackProcPathSegment`、`resolveHolidayDetailId`、`unwrapLookupPayload`、`lookupCarNumber`、`getHeader` 等，逻辑并入调用处。
2. `program-cards` 模板内联 `programName`/`prizeName`/`statusTagType`/`canChangeStatus`；保留多用的 `programRuleTags`/`ruleObj`/`flagOn`。
3. `checkin` 修复未定义的 `pick`/`boolLike`，`buildListParams` 并入 `loadData`。
4. 考勤 `decorateRow` 内联 `optLabel`/`ynText`。
5. 7 处重复 `pickSchoolRecords`/`pickNamedList` 改为已有 `normalizeArray`。

### 注意事项

- `npm run build` 已通过。
- 仍保留：`program-edit-form.config` 的 `dp()`、`program-cards` 的 `programTypeText`/`statusText`（多分支展示）。

## 第五轮（校车去重 + 内联）

### 新增

- `utils/school-bus.ts`：`normalizeSchoolIdsOnRow`、`isSpreadsheetFilename`、`stripEmptyQueryParams`、`detailCellDisplay`

### 修改

- 5 处 `normalizeSchoolIdsField`、3 处 `stripEmptyParams` → 上述 util
- `operation`/`exception` 的 `labelOf` 内联进 `format*Row`
- 8 个 list + `plan/tab`：去掉本地 `isSpreadsheetFilename`/`detailRowText`/`pickImport`，导入 util 或 `@click="fileRef?.click()"`

### 注意事项

- `npm run build` 已通过。

## 第六轮（列表空态 + 表单展示）

### 修改

- `use-list-table-empty`：支持 `{ tableRef, afterLoadSuccess }`，导出 `retry`；27+ 列表页删除 `onTableLoadSuccess` / `retryTable` 包装。
- 新增 `utils/form-display.ts`（`formatOptionLabels`、`formatRangeLabel`）；`detail-form.config`、`order-detail-dialog` 接入。
- `dict.vue`、`plan/tab` 站点表：空态回调并入 composable 配置。

### 注意事项

- `program-edit-form.config` 的 `dp()` 仍保留（配置内 20+ 处 disabled 简写）。
- `npm run build` 已通过。

## 第七轮（选项展示统一 + 导入反馈）

### 修改

- 删除 `program-edit-helpers.labelFromOptions`；节目列表改用 `formatOptionLabels`。
- `plan/tab.vue`：合并 `summarizeImportRows` + `downloadTextFile` 进 `showImportFeedback(raw)`，调用处直接传接口结果。

### 注意事项

- 列表页 `onSelectionChange` 仅赋值 `selectedRows`，属模板绑定需要，暂保留。
- `npm run build` 已通过。

## 第八轮（详情 Tab + refreshTable）

### 新增

- `utils/csv.ts`：`formatCsvRow`（报名/签到/祝福/投票导出共用）

### 修改

- **修复** `checkin.vue` 导出引用未定义的 `buildListParams`。
- `registration.vue`：`buildListParams` → `fetchTicketPage`（列表与导出共用）。
- 活动/校车列表：删除 `handleSaved` / `reload` 包装，改用 `useUniListState.refreshTable`（奖品、邮件/微信学校、问卷、投票节目、车辆、跟车老师、校车申请/订单等）。

### 注意事项

- `winner.vue` 去掉单行 `programType()`，在 `loadPrograms` 内联 `pt`。
- `npm run build` 已通过。

## 第九轮（全库 reload / 空态包装收尾）

### 修改

- 校车：driver、operation、exception；协议 protocol — `reload` → `refreshTable`。
- 考勤 pass：`refresh` → `refreshTable`。
- 权限：user/role 去掉 `reloadTable`；user 内联 `rolesLabel`；menu 内联 `normalizeParentIdForSelect`；assign-menu 内联 `normalizeChecked`。
- 路线计划 `plan/tab`、邮件 `outbox/tab`：空态并入 `useListTableEmpty` 配置，删除 `onXLoadSuccess` / `retryX` / `refreshRoutes` 等包装。
- 邮件草稿：删除 `joinSemi`，收件人字段内联拼接。
- 活动 `winner.vue`：删除 `pageApi`，请求并入 `loadData`。
- 基础 `dict.vue`：删除 `reloadTable`；outbox 删除 `onComposeSubmit` 包装。

### 注意事项

- `program-cards` 展示函数、`program-edit-form.config` 的 `dp()` 仍保留。
- `reloadCommonData` 为级联选项完整加载，非表格 refresh 包装。
- `npm run build` 已通过。

## 第十轮（假期/协议空态 + 中奖 Tab + 问卷 URL）

### 新增

- `utils/questionnaire-url.ts`：`buildQuestionnaireSignupUrl`（列表与编辑页共用）

### 修改

- `attendance/holiday/tab.vue`：请假/销假表空态并入 `useListTableEmpty`；`onLeaveFormSuccess` → `refreshLeaveTable`。
- `protocol/panel.vue`、`protocol/detail.vue`：签署记录表空态同上。
- `winner.vue`：删除 `emptyForm` / `onProgramChange` / `changeKind`；逻辑内联。
- `driver/components/form.vue`：删除单次 `toNum`。
- `program-cards.vue`：修复损坏的 `.join('、')` 导致构建失败。

### 注意事项

- `npm run build` 已通过。

## 第十一轮（校车 schoolIds + 邮件模块选项 + 列表 open 包装）

### 新增

- `utils/school-bus.ts`：`schoolIdsForCascadeApi`（运营/异常路线表单级联共用）
- `utils/activity-email-school.ts`：`appModuleOptionsFromRows`

### 修改

- `operation/components/form.vue`、`exception/components/form.vue`：删除重复 `schoolIdsForApi`，改用 `schoolIdsForCascadeApi`。
- `email-school/use-list.ts`、`form-dialog.vue`：应用模块选项统一 `appModuleOptionsFromRows`。
- 奖品 / 微信学校 / 邮件学校 / 投票节目：删除 `openAdd`/`openEdit`/`openDetail` 一行包装；`list.vue` 直接 `formDlg?.open(...)`，表格操作内联 `formDlg.value?.open`。
- `use-activity-detail-page.ts`：`meaningfulHtml` 内联进 `detailHtmlCn` / `detailHtmlEn` computed。

### 注意事项

- 活动详情 Tab（blessing/winner 等）的 `openAdd` 含多步逻辑，仍保留。
- `npm run build` 已通过。

## 第十二轮（详情 Tab + 校车/问卷薄包装）

### 修改

- `program-cards.vue`：删除 `programName` 别名，模板直接 `pickLocaleName`。
- `registration.vue`：`openEdit` 内联到表格 `onClick`。
- `questionnaire/list.vue`：页头/批量操作直接 `metaDlg`/`batchDlg?.open`，删除 composable 中 `openMetaAdd` 等三处转发。
- 校车运营/异常/路线、订单/申请列表：`openDetail`/`openRouteEdit` 内联到 `actions`，去掉仅赋值两行的包装函数及无用 export。

### 注意事项

- `checkin.vue` 的 `openEdit` 含字段映射与快照，保留。
- `feedback`/`blessing` 的 `openDetail`/`openEdit` 含接口拉取，保留。
- `npm run build` 已通过。

## 第十三轮（全库薄 helper 内联 + coerceIdList）

### 新增

- `utils/tool.ts`：`coerceIdList`（活动详情 `rowToModel` 三处复用）

### 修改

- `program-cards.vue`：内联轮次/状态展示；修复 `roundBadgeText` 损坏文案；删除 `showRoundBadge`/`roundBadgeText`/`statusText`。
- `use-activity-detail-page.ts`：用 `coerceIdList`；删除未使用的 `goEdit`。
- `checkin.vue`：`decorateRows` 内联 yes/no 与时间格式化。
- `questionnaire/use-list.ts`：内联 `schoolIds` 归一化与设计器/填报路由跳转。
- `protocol/use-list.ts`：内联详情路由跳转。
- 考勤 5 个列表：内联 `showDetail`。
- 路线 plan 学期/站点：`openXxx` 内联；`tab.vue` 新增按钮直接改 ref。
- `dict.vue`：内联 `parseSortField`/`sortPayload`。
- `email-school/form-dialog.vue`：内联 `ensureAppModuleOption`。
- `use-bus-order-form-dialog.ts`：删除未使用的 `closeForm`。
- 活动/节目列表：`goCreate` 移至 `list.vue` 直接 `router.push`。

### 注意事项

- `goDetail`/`openForm`/`openFormAdd` 等多处调用或完整打开流程的仍保留。
- `npm run build` 已通过。
