# 迭代记录 - 2026-05-19

## 活动详情下载文件名旧版对齐

**范围**：`uni-lib/src/plugins/request.ts`、`admin-web/src/api/modules/activity.ts`、`admin-web/src/utils/download.ts`、`admin-web/src/views/activity/detail`。

**内容**：为请求封装增加显式 `rawResponse` 开关，仅下载类接口需要响应头时返回完整响应；活动反馈导出、报名模板、可见范围模板、获奖名单导出改为读取 `Content-Disposition` 文件名，并保留固定文件名兜底。

**规则**：默认接口仍返回解包后的业务数据，不改变现有列表、详情、保存接口行为；文件名解析兼容 `filename*=`、带引号 `filename=` 和普通 `filename=` 三种旧版格式。

## 活动详情子 Tab 主操作按钮位置调整

**范围**：`admin-web/src/views/activity/detail`。

**内容**：报名人数、签到人数、获奖名单、投票信息、祝福语内容、活动反馈的主操作按钮上移到详情页头部返回按钮左侧，随当前 Tab 显示对应的导出、下载 Excel 模板、导入、新增操作。

**规则**：各按钮沿用原有活动状态和权限控制；子 Tab 表格工具栏保留批量删除、批量可见/不可见等依赖表格勾选状态的操作，避免改变批量操作交互范围。

## 活动基本信息查看态回显映射

**范围**：`admin-web/src/views/activity/detail/detail-form.config.ts`。

**内容**：补齐基本信息查看态字段映射，活动状态、签到方式、是否推荐、是否 Banner、是否需要反馈、微信提醒、可见范围等枚举字段显示文案；活动校区、推送邮箱、推送校区等多选字段显示名称；活动时间、报名时间显示区间文本。

**规则**：仅调整查看态回显，不改变编辑态字段值和保存 payload；可见范围名单仅在可见范围为「指定」时展示。

## 活动列表操作按钮旧版对齐

**范围**：`admin-web/src/views/activity/list`。

**内容**：活动列表操作列恢复旧版行级业务边界，`查看` 始终显示，`编辑` 按 `busdriver_edit` 权限且活动处于待发布/已发布时显示；已开始、已结束活动不展示编辑入口。

**修正**：活动列表批量导出反馈适配下载接口返回完整响应的文件名解析；详情页查看态隐藏顶部编辑入口，返回按钮前分割线仅在左侧存在操作按钮时显示。

## 活动基本信息 UniForm 分模块展示

**范围**：`admin-web/src/views/activity/detail/detail-form.config.ts`、`index.vue`、活动模块 i18n。

**内容**：活动基本信息改用 `UniForm.sections` 分 7 组展示：基础信息、展示信息、时间与校区、报名与可见、推荐与开关、通知与推送、活动详细介绍；时间区间、多选、富文本等长字段整行，其余成对字段两列；详情页增加分组标题样式。

**规则**：仅优化表单排布与分组展示，不改变字段、校验、映射和保存 payload。

## 活动基本信息控件类型优化

**范围**：`detail-form.config.ts`、`index.vue`。

**内容**：是/否类字段（推荐、Banner、需反馈、微信提醒）改为 `ElSwitch`，`inline-prompt` + `active-text` / `inactive-text`（是/否），取值仍为 `'0'`/`'1'`；签到方式、可见范围、活动状态改为 `ElRadioGroup`；文本/时间/多选/数字等保持 `ElInput`/`ElDatePicker`/`ElSelect`/`ElInputNumber`。

**规则**：不改变保存 payload 与查看态枚举文案回显。

## 活动详情翻译函数简化

**范围**：`admin-web/src/views/activity/detail/use-activity-detail-page.ts`。

**内容**：移除 `tr` 翻译函数别名的残留调用，活动详情页组合函数内统一直接使用 `useUniI18n()` 返回的 `t`。
