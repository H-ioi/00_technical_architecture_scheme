# 迭代日志 20260605

## 修改文件

- `admin-web/src/api/modules/protocol.ts` — 修复 `signPage` 接口路径变量名拼写错误
- `~/.cursor/skills/uni-review-mobile/SKILL.md` — 明确移动端不加载 `uni-lib` / `uni-ui-lib`
- `前端移动端(customer-mobile)构建方案.md` — 选型表与基础约束补充 UI 边界
- `customer-mobile/README.md` — 模板要点表增加 UI 组件库说明

## 核心变更

- **admin-web 启动修复**：`protocol.ts` 第 95 行误写 `protocolBaseSign`，应为已定义的 `protocolSignBase`，导致模块加载时 `ReferenceError`
- 管理后台（`admin-web`）继续使用 `uni-ui-lib`；**`customer-mobile` 及 uni-review-mobile 规范禁止接入**
- 移动端 UI：uni-app 内置组件 + 项目 `components/`；跨项目复用不得指向 `uni-lib`
- 评审 Critical / Finish Checklist 增加 `package.json`、`main.ts` 无组件库依赖项

## 注意事项

- 与 `admin-web` 仅 **api 模块形态** 对齐，**不**对齐 UI 组件库与请求客户端（后台走 `uni-ui-lib` 的 `request`，移动走 `utils/request.ts`）

---

## uni-review-web Skill 整理

### 修改文件

- `~/.cursor/skills/uni-review-web/SKILL.md` — 结构重组、修正 HTTP 层描述、补充与移动端边界
- `admin-web/README.md` — 指向 Skill、注明 uni-ui-lib 与移动端差异

### 核心变更

- **§0** 裁决顺序、行数规则、与 **uni-review-mobile** 对照
- **§13 修正**：鉴权/请求在 **`uni-ui-lib` + `uni.ts`**，非 `utils/request`（admin-web 无该文件）
- **§2.0** 标准模板对照表；**§1** 补充 `UniSearchForm` 须含 `schema`、`main.ts` 安装顺序
- **§8** 合并原 §8 内联与 §2.1 重复表述；**§15/17** Critical 与 Checklist 对齐
- 明确禁止在后台引入 `uni-request` / 复制 uni-lib 源码

### 注意事项

- 存量 `views/**/use-*.ts`、`api/constants.ts` 仍允许存在，新增须按 Skill 写在 `.vue` 与 module 顶 `const path`

---

## 仓库 Cursor Rules（团队共享）

### 修改文件

- `.cursor/rules/project-standards.mdc` — 合并 User Rule + 双端蒸馏（`alwaysApply: true`）
- `.cursor/rules/README.md` — 团队使用说明

### 核心变更

- 工作流、440/800/33 行、内联原则、`admin-web` / `customer-mobile` 边界、禁止混栈写入仓库规则
- 规则正文**不引用**任何 Cursor Skill 名称；端细节指向各子项目构建方案文档

### 注意事项

- 成员需 Cursor 支持 Project Rules
