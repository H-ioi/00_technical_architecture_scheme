# 迭代-20260511

> **命名规则**：迭代说明使用 `迭代-[当天日期].md`（例：`迭代-20250511.md`）。**同一天内**的多次调整合并写在**同一个**文档中，用下方小节区分。

## 校车学生表单：审批状态切换误触发表单校验

- **现象**：申请意向、乘车学生共用的「新增/编辑」弹窗中，切换「审批状态」会在未点击确定时，对校区、学期、学号、接送方式等其它必填项展示校验错误。
- **原因**：`mainRules` 随 `approvalStatus` / 缴费联动变化，`el-form` 默认 `validateOnRuleChange` 为 `true`，rules 引用更新后会触发整表校验。
- **修改**：在 `bus-order-form-dialog.vue` 的 `mainFormConfig.formProps` 中设置 `validateOnRuleChange: false`，保留各字段自有 `trigger`（blur/change）及提交时 `validate()` 行为。

## 编辑弹窗：详情接口慢时增加表单区 loading

- **需求**：接口响应慢时，弹窗已打开但表单空白或旧数据闪一下；希望在拉取详情完成前显示加载态。
- **实现**：
  - 新增 `admin-web/src/composables/use-dialog-detail-loading.ts`：`detailLoading` + `runWithDetailLoading`，配合 `v-loading` 与 `common.loading` 文案。
  - 在 `common` 中英文案增加 `loading`。
  - 已对以下「打开后异步拉详情」的弹窗/表单区接入：`bus-order-form-dialog`（编辑）、司机/车辆/跟车老师/路线运营/路线异常表单、协议编辑与协议详情抽屉、学期/站点编辑弹窗、路线规划 `route-form-modal` 编辑态。
- **约定**：后续新增同类弹窗可在异步 `detail/get` 外包一层 `runWithDetailLoading`，并在 `UniForm` 外包一层带 `v-loading` 的容器。

## uni-lib UniLayout 侧栏菜单支持纵向滚动

- **问题**：侧栏 `overflow: hidden`，菜单随层级展开超出视口后被裁切，无滚动条。
- **修改**：`uni-layout/index.vue` 中侧栏改为纵向 flex（Logo 固定高度、`flex-shrink: 0`）；`el-menu` 外包一层 `uni-layout__menu-wrap`，设置 `flex: 1`、`min-height: 0`、`overflow-y: auto`，使菜单在 Logo 下方区域内独立滚动。

## uni-lib：`route.meta` 首帧 undefined 导致白屏

- **现象**：admin-web 切本地 `uni-lib` 后启动报错 `Cannot read properties of undefined (reading 'meta')`，栈在 UniLayout 的 computed。
- **原因**：写成 `route.meta?.xxx` 时，`route` 为 `undefined` 仍会先访问 `.meta` 再短路；在 `app.use(router)` 与首屏渲染交界，本地链接/多实例时偶发 `useRoute()` 尚未就绪。
- **修改**：`uni-layout/index.vue`、`tags-view.vue` 中凡读当前路由统一改为 `route?.fullPath`、`route?.meta?....`、`route?.matched`，并在同步标签处对 `matched` 做空数组保护。

## admin-web：本地 uni-lib rebuild 后仍像旧版本

- **原因**：Vite 会把依赖预构建到 `node_modules/.vite/deps/`；`uni-ui-lib` 走 `file:../uni-lib` 且入口为 `dist/index.mjs` 时，仍可能被缓存，重启 dev 仍命中旧 chunk。
- **修改**：`admin-web/vite.config.ts` 增加 `optimizeDeps.exclude: ['uni-ui-lib']`，开发时直接解析库产物、减少过时预构建；`resolve.dedupe` 增加 `vue`、`vue-router`，降低本地链接多实例风险。
- **脚本**：`package.json` 增加 `dev:force`（`vite --force`）、`cache:clean:vite`（删除 `node_modules/.vite`），可在仍异常时强制清 Vite 缓存。
- **其它**：若包管理器把 `file:` 装成拷贝而非 symlink，需在 uni-lib `build` 后于 admin-web 目录再执行一次安装以更新 `node_modules/uni-ui-lib`。

## 跟车老师：与旧版对齐说明 + 操作列权限修正

- **旧版代码**：仓库内无 `test/old-test/src/views/isacommunity/user/teacher` 源文件，仅文档与 API 注释约定旧菜单 `/isacommunity/user/teacher/index`，接口基底 `/isacommunity/teacher/user`。
- **新版行为核对**：列表与表单字段为校区（多校时）、昵称、部门、邮箱、手机号、状态；新增独有密码字段；编辑/查看不调密码。校验：必填 + 邮箱格式。权限前缀沿用 `teacheruser_*`（含历史拼写 `teacheruser_enble`）。
- **修正**：表格「编辑」操作列误写为 `busorder_edit`，已改为 `teacheruser_edit`，与 Toolbar 命名一致。

## 跟车老师表单：补齐旧版「模块」「角色」多选

- **依据**：`test/old-test/src/views/isacommunity/user/teacher/modal/form.vue` 中 `modules` / `roles` 多选及校验、选项 id（模块 1/2，角色 1/2/3）。
- **改动**：`FollowTeacherFormModel` 增加 `modules`、`roles`；`list.config.ts` 增加选项常量与表单项、数组必填校验；文案写入 `school-bus-more` 中英；`form.vue` 重置与回填时保证数组形态。密码仍为仅新增必填（编辑不要求密码，避免旧版规则与详情无密文冲突）。

## 权限用户弹窗表单 UniForm 重构

- **范围**：`admin-web/src/views/permission/user/components/form-dialog.vue`
- **内容**：将原 `el-form` 改为 `UniForm`，表单项与校验规则从 `list.config.ts` 的 `permissionUserDialogForm` / `permissionUserFormRules` 统一配置；`v-model` 绑定 `PermissionUserFormModel`；打开弹窗后 `nextTick` 调用 `clearValidate`；提交前 `validate()`；编辑模式下若密码为空则从提交体中删除 `password`。
- **原因**：与菜单编辑等弹窗一致使用 UniForm，减少重复模板并保持配置集中。

## 权限用户/角色列表：表格刷新 API 修正

- **原因**：`UniDataTable` 仅通过 `defineExpose` 暴露 `refresh`，不存在 `reload`。点击部门树或保存后调用 `tableRef.reload()` 会报 `reload is not a function`。
- **修改**：
  - `admin-web/src/views/permission/user/list.vue`、`use-list.ts`：`reload()` → `refresh()`。
  - `admin-web/src/views/permission/role/list.vue`、`use-list.ts`：同上，避免角色列表同类问题。

## 重构方案文档：Phase 实施顺序调整

- 更新 `admin-web/isacommunity重构方案.md`：Phase 5 之后的实施顺序调整为 **Phase 6 考勤 → Phase 7 群发邮件 → Phase 8 活动管理 → Phase 9 内容管理 → Phase 10 收尾**。
- 同步修订全文节奏表（文档中 2.1、2.3、六、九、十五及 15.8～15.10 等相关小节）。
- 「暂不处理」列表中与后续阶段相关的四项顺序与新区间一致。

## 菜单编辑：上级菜单 `-1` 显示为「根节点」

- **原因**：接口可能返回字符串 `parentId: "-1"`，下拉里根节点选项的 `value` 为数字 `-1`，`el-select` 严格相等匹配不到选项，折叠态会显示原始 `-1`。
- **修改**：`use-list.ts` 增加 `normalizeParentIdForSelect`，在 `openEdit` 与提交 `payload` 中将 `undefined` / `null` / `''` / `-1` / `"-1"` 统一为数字 `-1`，与 `permission.menu.rootParent`（根节点）选项一致。

## 侧栏图标：项目公共组件 `src/components/sidebar-icon`

- **更名**：原 `menu-sidebar-icon` / `MenuIconPicker` / `MenuIconDisplay` / `menuSidebarIcon*` 去掉与「菜单」强绑定命名，改为 **`sidebar-icon`**、`IconPicker`、`IconDisplay`、`sidebarIconMap` / `sidebarIconNames`。
- **路径**：`admin-web/src/components/sidebar-icon/`（`registry.ts`、`IconPicker`、`IconDisplay`、`index.ts` 统一导出；注册表与 `uni-ui-lib` 同源）。
- **引用**：`import { IconPicker, IconDisplay } from '@/components/sidebar-icon'`；权限菜单编辑、列表等已改用此路径。
- **文案**：弹窗标题与搜索占位使用 **`common.iconPicker`、`common.iconSearch`**（全局复用）；表单项 placeholder 仍可用业务文案（如 `permission.menu.icon`）。
- **图标范围**：`registry.ts` 与 `uni-lib/src/utils/icons.ts`（导出 `uniIconMap` / `uniIconNames`）使用 `import * from '@element-plus/icons-vue'` 载入 **EP 全量图标**（约 290+），再合并 `UniZhEn`；与 admin 侧栏解析对齐。**代价**：会明显增加打包体积；若需瘦身可改为按需导入 + 白名单。图标表为纯 `.ts` 模块（无模板 SFC 需额外 default 导出）。
- **实现**：库内扩展对象常量命名为 **`uniLibIcons`**（EP 之外的组件库图标，后合并进 `uniIconMap`）。

## 角色分配菜单弹窗：loading 不显示

- **原因**：`el-tree` 使用 `v-if="ready"`，请求未完成时容器内无子节点，包裹层高度为 0，`v-loading` 遮罩无可绘区域。
- **修改**：`assign-menu-dialog.vue` 内容区增加 `min-height: 280px`、`position: relative`，并 `:element-loading-text="t('common.loading')"`。

## Phase 6 考勤：学生考勤首屏迁移

- **依据**：`test/old-test/src/views/isacommunity/attendance/student/index.vue`、`api/isacommunity/attendance.js`（`/isacommunity/attendance/student/page|export`、`/isacommunity/attendance/common/getGradeList`）。
- **路由**：`/attendance` → `/attendance/student`；侧栏图标 `Calendar`。
- **菜单别名**：`menu.ts` 补充考勤域旧路径 → 新路径（含请假子路径等），便于后端菜单树映射。
- **实现**：`api/modules/attendance-student.ts`、`types/modules/attendance-student.ts`、`views/attendance/student/*`（`UniSearchForm` + `UniDataTable` + 详情弹窗）；导出按钮权限 **`student_attendance_export`**；查看按钮与旧页一致不设权限码。
- **i18n**：`locales/lang/*/modules/attendance.ts`、`route.attendance` / `route.attendanceStudent`。

## Phase 6 考勤：校园考勤

- **依据**：`test/old-test/.../attendance/school/index.vue`、`attendance.js`（`/school/page`、`/school/export`、`/common/getDepartmentList`）。
- **路由**：`/attendance/school`；导出权限 **`school_attendance_export`**；查看无权限码（与旧页一致）。
- **代码**：`api/modules/attendance-school.ts`、`types/modules/attendance-school.ts`、`views/attendance/school/*`；入校通道列兼容旧表头笔误字段 `entryAscChannel` / `entryAcsChannel`。

## Phase 6 考勤：门禁记录

- **依据**：`test/old-test/.../attendance/access/index.vue`、`attendance.js`（`/union/page`、`/union/export` 仅占位）。
- **路由**：`/attendance/access`；旧列表导出按钮已注释，本页**不提供导出**。
- **实现**：`api/modules/attendance-access.ts`、`types/modules/attendance-access.ts`、`views/attendance/access/*`；枚举对齐旧 `attendanceOpenType`、`enterOrExit`、`successOrfail`；部门下拉复用 `attendanceSchoolApi.departmentList`。

## Phase 6 考勤：微信 OpenID

- **依据**：`test/old-test/.../attendance/wechat/index.vue`、`attendance.js`（`/wechatOpenid/page`、`batchUpdateStatus`）。
- **路由**：`/attendance/wechat`；导出按钮旧版注释，本页未提供导出。
- **批量**：工具栏「归档」`status=1`、`使用」`status=0`，权限 **`archive_wx_openid`**，请求体为 `FormData`，字段 `ids`（逗号拼接）、`status`，与旧页一致。
- **代码**：`api/modules/attendance-wechat-openid.ts`、`types/modules/attendance-wechat-openid.ts`、`views/attendance/wechat/*`。

## Phase 6 考勤：微信通知

- **依据**：`test/old-test/.../attendance/wechatnotice/index.vue`、`attendance.js`（`/wechatNotice/page`）。
- **路由**：`/attendance/wechat-notice`，与 `menu.ts` 中 `/attendance/wechatnotice` → `/attendance/wechat-notice` 映射一致。
- **筛选**：`sendStatus` 与旧页下拉一致：**`0` 成功、`1` 失败**（与常规布尔语义相反，切勿调换）。
- **导出**：旧页导出注释关闭，本页不提供导出。
- **代码**：`api/modules/attendance-wechat-notice.ts`、`types/modules/attendance-wechat-notice.ts`、`views/attendance/wechat-notice/*`；校区选项与其他考勤页一致使用 **`membershipApi.school`**（旧页额外请求的 `getSchoolList` 未用于表单）。

## Phase 6 考勤：学生每日考勤

- **依据**：`test/old-test/src/views/isacommunity/attendance/index.vue`、`api/isacommunity/holiday.js`（`GET /attendance/holiday/getData`、`GET /attendance/holiday/getData2` 导出、`/isacommunity/attendance/common/getSchoolList` 下拉，`schoolName` 取校区 **`enName`**）。
- **路由**：`/attendance/daily`；菜单别名已有 **`/isacommunity/attendance/index`** → 新路径。
- **导出**：旧页导出按钮无独立权限码，本页导出按钮**不设 `v-uni-permission`**。
- **展示**：`MB` 来源考勤时间展示「第 n 节课」与旧 `renderSection` 一致；状态/来源枚举与旧页映射一致。
- **代码**：`api/constants.ts` 增加 `attendanceHolidayRest`；`api/modules/attendance-daily.ts`、`types/modules/attendance-daily.ts`、`views/attendance/daily/*`。

## 考勤模块：校区列按 schoolId 随语言切换

- **问题**：列表用接口 `schoolName` 展示，切换中英文不随 `membershipApi.school` 的 `name`/`enName` 变化。
- **处理**：门禁、校考勤、学生考勤、微信 OpenID、微信通知 五处列表列改为 **`prop: 'schoolId'`** + **`options: schoolOptions`**（与 `UniDataTable` 的 `text` lookup 一致，单值；司机页为 `array` + `splitValues`）。详情弹窗校区字段改为 **`schoolId`** + **`viewType: 'enum'`** + 同上 options。
- **接口**：列表/详情行需带 **`schoolId`**（与学校选项 `id` 一致）；列表**筛选**请求参数使用 **`schoolId`**（单值，非 `schoolIds`）；若后端字段名不同（如 `school_id`）需在映射层对齐。

## uni-lib：新增 Prettier 配置

- **文件**：`uni-lib/.prettierrc`，与 `admin-web/.prettierrc` 一致（无分号、单引号、无尾随逗号、`printWidth` 100、`bracketSameLine: true` 等），`npm run format` / `format:check` 将按此规则执行。**注意**：若库内原按 Prettier 默认风格书写，首次执行 `npm run format` 可能产生较大 diff，建议单独提交。
- **全局同步**：以 `uni-lib/.prettierrc` 为基准，已同步至仓库根目录 `.prettierrc`、`admin-web`、`customer-web`、`customer-mobile`（`.prettierrc.json`）；各子项目 Prettier 解析时优先使用各自目录下配置，根目录配置便于在仓库根打开零散文件时行为一致。
- **文档**：`前端开发规范.md` 中「Prettier 规则示例」已与上述配置对齐；`admin-web/isacommunity重构方案.md` 等未写 Prettier 细则处未增写（文中「格式化」多指业务 `format*` 工具）。

## uni-lib：UniForm 查看态长文本省略与 Tooltip

- **需求**：查看弹窗中长 JSON 等撑破布局。
- **默认**：`view` 模式单行省略；`ResizeObserver` 检测溢出后 **`el-tooltip`** 展示全文（未溢出不出气泡）。
- **配置**：`config.view.viewOverflow` 与字段 **`viewOverflow`**，`ellipsis` | `wrap` | `none`，字段优先。
- **对象值**：`viewRender` 返回非原始值时按 `JSON.stringify` 展示（与模板直出 `object` 的 `[object Object]` 区分）。
- **实现**：`components/uni-form/components/view-value-text.vue`；`.el-form-item__content` 增加 **`min-width: 0`** 以利 flex 下截断。
