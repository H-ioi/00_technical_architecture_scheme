export default {
  trigger: 'Upload file',
  maxSize: 'File size cannot exceed {size} bytes',
  maxTotalSize: 'Total file size cannot exceed {size} bytes',
  accept: 'File type must match {accept}'
} as const
