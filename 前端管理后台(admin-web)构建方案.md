# 前端管理后台(admin-web)构建方案

本文档基于 `前端架构方案总览.md` 与 `前端开发规范.md` 编制，作为 `admin-web` 项目的专项建设方案。目标是给出一套可直接落地的管理后台构建标准，覆盖技术选型、目录结构、权限体系、接口分层、组件策略、工程化、测试、部署与运维。

---

## 一、项目定位

`admin-web` 用于承载企业内部运营平台、管理控制台、配置后台、审核中心、数据看板等 PC 管理类系统。

该类系统的核心诉求不是 SEO，而是：

- 高开发效率
- 高表单与表格承载能力
- 完整权限模型
- 清晰的菜单与路由体系
- 稳定的工程规范和长期可维护性

因此 `admin-web` 统一采用 `Vue 3 + Vite + Vue Router + Element Plus + Pinia + TypeScript + Sass` 的 CSR 架构方案。

---

## 二、建设目标

- 建立统一的后台项目模板，降低重复搭建成本
- 统一管理后台在目录、命名、权限、接口和组件层面的实现方式
- 统一接入 `lg-lib` 业务组件库，减少重复业务组件开发
- 通过严格的校验、提交流程和 CI 门禁，保证代码质量
- 形成可复制、可迭代、可扩展的后台基座能力

---

## 三、技术选型

### 3.1 核心技术栈

| 类别 | 选型 | 说明 |
| ---- | ---- | ---- |
| 框架 | Vue 3 | 主体框架，采用 Composition API |
| 构建 | Vite | 提升本地开发与构建效率 |
| 路由 | Vue Router 4 | 后台菜单、权限路由、动态路由基础 |
| UI | Element Plus | 后台表单、表格、弹窗、布局基础组件 |
| 状态管理 | Pinia | 用户、权限、标签页、全局配置等状态统一管理 |
| 语言 | TypeScript | 强制类型约束，提升重构安全性 |
| 样式 | Sass | 全局变量、主题扩展、样式组织 |
| 业务组件库 | `lg-lib` | 沉淀跨项目复用业务组件 |
| 测试 | Vitest + Testing Library + Playwright | 覆盖单元、组件与 E2E |

### 3.2 不采用 Nuxt 的原因

- 管理后台主要服务于登录后的内部业务场景，不依赖 SEO
- 后台页面结构复杂，更关注权限、表单、表格、工作流与操作效率
- CSR 架构即可满足需求，且工程复杂度更低
- 使用 `Vue Router + Pinia + Element Plus` 更适合长期模板化建设

---

## 四、总体架构设计

### 4.1 架构模式

`admin-web` 采用典型的前端分层架构：

```text
View Layer -> Component Layer -> Store Layer -> Service Layer -> API Client
```

各层职责如下：

- `View Layer`：页面编排、页面级交互、页面布局组织
- `Component Layer`：通用组件、业务组件、页面私有组件
- `Store Layer`：用户、权限、全局配置、页面共享状态
- `Service Layer`：按业务域组织的接口能力
- `API Client`：请求实例、拦截器、认证注入、错误处理

### 4.2 总体能力模块

后台基础能力应统一沉淀为以下模块：

- 登录认证
- 权限控制
- 菜单路由
- 标签页导航
- 全局布局
- 主题配置
- 请求封装
- 错误处理
- 全局消息提示
- 表格与表单模板能力
- 文件上传下载
- 字典与枚举缓存
- 日志与监控接入

---

## 五、项目目录方案

### 5.1 标准目录结构

```bash
src/
├── api/                        # 接口层，只放请求方法、参数类型、响应类型
│   ├── modules/                # 按业务域拆分接口，如 user.ts、role.ts、order.ts
│   └── index.ts                # 统一导出
├── assets/                     # 静态资源层
│   ├── images/                 # 图片、图标、字体
│   └── styles/                 # 全局样式、变量、mixin、reset
├── components/                 # 当前项目复用组件层
│   ├── common/                 # 通用展示组件
│   └── business/               # 当前项目复用业务组件
├── composables/                # use-xxx 逻辑复用层
├── directives/                 # 全局或局部通用指令
├── layouts/                    # 后台布局层
├── plugins/                    # 第三方注册、全局初始化
├── router/                     # 路由层
│   ├── guards/                 # 路由守卫
│   ├── modules/                # 路由模块拆分
│   └── index.ts                # 路由统一聚合
├── stores/                     # Pinia 状态层
│   ├── modules/                # 按业务域拆分状态
│   └── index.ts                # 统一注册导出
├── types/                      # 全局类型、接口契约、模块类型
├── utils/                      # 纯函数、请求薄封装、存储工具
│   ├── request/                # 请求实例、拦截器
│   ├── storage/                # localStorage/sessionStorage 封装
│   └── index.ts                # 通用工具导出
├── views/                      # 页面编排层
│   └── user-list/              # 单页面目录
│       ├── components/         # 页面私有组件
│       └── user-list-page.vue  # 页面入口
├── App.vue                     # 根组件
└── main.ts                     # 应用启动入口
```

### 5.2 目录约束

- `src/` 顶层目录必须保持稳定，不允许随意新增平级目录
- 页面私有组件应优先放在页面子目录 `components/`
- 跨页面复用组件进入 `components/`
- 跨项目复用组件进入 `lg-lib`
- 同类能力禁止同时拆分到多个职责相近目录，例如 `hooks/` 与 `composables/` 并存

### 5.3 页面目录组织建议

典型页面目录建议如下：

```bash
views/
└── order-list/
    ├── components/
    │   ├── order-filter-form.vue
    │   ├── order-table.vue
    │   └── order-detail-dialog.vue
    ├── constants.ts
    ├── use-order-list.ts
    └── order-list-page.vue
```

适用规则：

- 页面主文件负责组织页面结构和交互流程
- 筛选区、表格区、弹窗区、详情区优先拆为页面私有组件
- 页面级逻辑优先拆到同目录的 `use-xxx.ts`
- 页面常量、列定义、枚举映射可拆到 `constants.ts`

---

## 六、布局体系设计

### 6.1 默认后台布局

后台采用统一布局壳，建议至少包含：

- 顶部导航区
- 左侧菜单区
- 主内容区
- 面包屑区
- 标签页区
- 全局消息 / 弹窗容器

### 6.2 推荐布局层级

```text
App.vue
  -> AdminLayout
    -> Sidebar
    -> Header
    -> TagsView
    -> MainContainer
      -> RouterView
```

### 6.3 布局能力要求

- 支持左侧菜单折叠
- 支持标签页缓存与关闭
- 支持面包屑导航
- 支持全屏、刷新、退出登录
- 支持主题色切换与布局配置
- 支持固定头部、固定标签栏等布局选项

---

## 七、路由与菜单方案

### 7.1 路由分类

后台路由建议拆分为三类：

- 静态路由：登录页、404、基础首页等
- 动态路由：按权限加载的业务页面
- 外链路由：对接外部系统或 iframe 页面的特殊路由

### 7.2 路由字段建议

```ts
interface AppRouteMeta {
  title: string
  icon?: string
  hidden?: boolean
  keepAlive?: boolean
  affix?: boolean
  permission?: string[]
  activeMenu?: string
}
```

### 7.3 菜单与路由关系

建议采用“后端返回菜单配置，前端转换为路由”的方案：

- 后端提供菜单树、权限码、路由标识
- 前端根据路由组件映射表生成动态路由
- 前端负责菜单展示、面包屑、标签页与权限指令联动

### 7.4 路由守卫

建议至少包含以下守卫逻辑：

- 未登录跳转登录页
- 已登录访问登录页时重定向首页
- 首次进入时拉取用户信息与菜单权限
- 无权限页面跳转 `403`
- 不存在页面跳转 `404`

---

## 八、认证与权限体系

### 8.1 认证方案

后台建议采用基于 Token 的认证方案：

- 登录成功后获取 `accessToken`
- Token 存储在受控存储层中
- 所有接口请求统一由拦截器注入认证头
- Token 过期时统一触发刷新或重新登录

### 8.2 权限模型

建议统一支持三类权限：

- 路由权限：控制页面可访问性
- 按钮权限：控制操作按钮是否可见
- 数据权限：控制列表、组织、范围数据访问

### 8.3 按钮权限实现

推荐两种方式并存：

- 权限函数：`hasPermission('user:add')`
- 权限指令：`v-permission="'user:add'"`

### 8.4 角色与菜单联动

建议由后端维护：

- 用户 -> 角色
- 角色 -> 菜单
- 角色 -> 权限码

前端负责：

- 根据权限码控制按钮与页面元素
- 根据菜单树生成左侧导航和动态路由

---

## 九、状态管理方案

### 9.1 Store 拆分建议

后台至少建议拆出以下 Store：

- `app`：布局配置、侧边栏状态、主题配置
- `user`：用户信息、token、登录状态
- `permission`：菜单、动态路由、权限码
- `tags-view`：页签缓存、页签关闭逻辑
- `dict`：字典与枚举缓存

### 9.2 Store 设计原则

- Store 只保存共享状态与业务动作
- 页面私有临时状态优先留在页面内
- 不在 Store 中堆积大量 DOM 逻辑
- 异步请求应通过 service 调用，不直接在组件中散落实现

### 9.3 持久化建议

适合持久化的内容：

- token
- 主题配置
- 用户基础信息
- 打开的标签页

不适合持久化的内容：

- 页面临时筛选条件
- 弹窗开关状态
- 页面局部 loading

---

## 十、接口封装方案

### 10.1 API 分层

统一采用：

```text
API Client -> Service Layer -> Store / View
```

### 10.2 请求层职责

`API Client` 应负责：

- `baseURL` 管理
- 统一请求头注入
- Token 注入
- 超时设置
- 通用错误提示
- 业务错误码处理
- 重复请求控制
- 文件下载处理

### 10.3 Service 层组织

示例：

```bash
api/
├── modules/
│   ├── user.ts
│   ├── role.ts
│   ├── permission.ts
│   └── order.ts
└── index.ts
```

每个模块建议按业务域组织，不按 HTTP 方法组织。

### 10.4 错误处理策略

统一区分：

- 网络错误
- 认证错误
- 权限错误
- 业务错误
- 服务器异常

错误展示建议统一走全局消息组件，不在每个页面重复造一套提示逻辑。

---

## 十一、页面开发模式

### 11.1 典型后台页面模型

后台高频页面通常由以下模块组成：

- 页面标题区
- 筛选区
- 操作按钮区
- 表格区
- 分页区
- 弹窗区
- 详情区

### 11.2 推荐拆分方式

以列表页为例：

```text
order-list-page.vue
  -> order-filter-form.vue
  -> order-table.vue
  -> order-detail-dialog.vue
  -> use-order-list.ts
```

### 11.3 页面组件抽离原则

- 页面私有组件：只在当前页面使用
- 项目级组件：在多个页面复用
- 组件库组件：在多个项目复用

当页面文件超过 `300` 到 `400` 行，或页面内存在多个明显独立区域时，必须评估拆分。

---

## 十二、组件体系方案

### 12.1 组件分层

后台组件建议分为：

- 基础组件：主要来自 Element Plus
- 项目通用组件：当前项目内复用，如页面头、导出按钮、筛选容器
- 业务组件库组件：来自 `lg-lib`
- 页面私有组件：仅服务于单页面

### 12.2 `lg-lib` 接入策略

推荐通过 `plugins/business-ui.ts` 统一接入 `lg-lib`：

- 全局注册常用组件
- 可按需注册大型组件
- 统一处理主题变量和样式入口

### 12.3 后台常见可沉淀组件

优先评估沉淀到 `lg-lib` 的能力：

- 数据表格
- 搜索表单
- 权限按钮
- 状态标签
- 上传组件
- 富文本容器
- 选择器弹窗

---

## 十三、表格与表单规范

### 13.1 表格页规范

- 查询条件与列表数据分离管理
- 列定义建议单独抽离
- 操作列按钮应与权限码联动
- 空状态、加载态、异常态统一处理
- 导出、批量操作必须加入二次确认与权限校验

### 13.2 表单页规范

- 表单模型统一使用类型约束
- 必填校验、长度校验、格式校验统一配置化
- 新增与编辑尽量复用同一套表单组件
- 保存前应做前端基础校验，避免无效请求

### 13.3 弹窗规范

- 弹窗尽量聚焦单一目标
- 弹窗打开与关闭状态由父级统一控制
- 弹窗表单提交后由父级负责刷新列表

---

## 十四、样式与主题方案

### 14.1 样式体系

统一使用 Sass，建议结构：

```bash
assets/styles/
├── _variables.scss
├── _mixins.scss
├── _reset.scss
├── _utilities.scss
└── index.scss
```

### 14.2 主题能力

后台建议支持：

- 品牌主色配置
- 暗色 / 亮色扩展能力
- 侧边栏配色切换
- Element Plus 变量覆写

### 14.3 样式原则

- 优先使用设计变量
- 避免深层嵌套
- 避免页面复制大段样式
- 公共样式统一抽离

---

## 十五、日志、错误与监控

### 15.1 错误处理

建议统一处理：

- 页面级加载异常
- 接口失败异常
- 权限失败异常
- 路由异常
- JS 运行时异常

### 15.2 监控接入

后台建议接入：

- `Sentry`：前端异常追踪
- `Lighthouse`：基础性能评估
- `RUM`：真实用户性能监控

### 15.3 日志策略

- 禁止提交调试 `console`
- 业务运行日志统一走封装日志方法
- 敏感信息禁止进入日志系统

---

## 十六、工程化与质量门禁

### 16.1 必备工程能力

- ESLint
- Prettier
- Stylelint
- lint-staged
- Husky
- Commitlint
- Type Check

### 16.2 强制校验要求

- 禁止提交 `console.log`、`console.debug`、`debugger`
- 所有 `ts`、`js`、`vue`、`scss`、`md`、`json` 文件必须参与格式化
- CI 中不得跳过 `lint`、`type-check`、`build`
- 提交信息必须符合 Conventional Commits

### 16.3 推荐脚本

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx,.vue --max-warnings=0",
    "lint:fix": "eslint . --ext .js,.jsx,.ts,.tsx,.vue --fix",
    "lint:style": "stylelint \"src/**/*.{vue,scss,css}\" --allow-empty-input",
    "lint:style:fix": "stylelint \"src/**/*.{vue,scss,css}\" --fix --allow-empty-input",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "type-check": "vue-tsc --noEmit"
  }
}
```

---

## 十七、测试方案

### 17.1 单元测试

优先覆盖：

- 工具函数
- 权限判断函数
- 数据转换函数
- composable 纯逻辑

### 17.2 组件测试

优先覆盖：

- 搜索表单
- 表格操作按钮
- 弹窗交互
- 权限控制组件

### 17.3 E2E 测试

至少覆盖：

- 登录流程
- 菜单访问
- 列表查询
- 新增 / 编辑 / 删除
- 权限受限场景

---

## 十八、环境与配置方案

### 18.1 环境变量

建议使用：

- `.env.development`
- `.env.test`
- `.env.production`

统一要求：

- 仅 `VITE_` 前缀变量暴露给前端
- 不提交密钥、密码、真实令牌
- 接口地址、上传地址、监控地址走环境变量配置

### 18.2 常见配置项

- `VITE_APP_TITLE`
- `VITE_API_BASE_URL`
- `VITE_UPLOAD_URL`
- `VITE_ENABLE_MOCK`
- `VITE_SENTRY_DSN`

---

## 十九、部署与发布方案

### 19.1 构建产物

后台项目构建后输出静态资源，可部署至：

- Nginx
- CDN + OSS
- 容器静态服务
- 企业内网静态资源平台

### 19.2 发布流程

推荐流程：

1. 本地开发完成并通过校验
2. 提交代码并通过 PR
3. CI 执行 `lint`、`type-check`、`test`、`build`
4. 构建产物部署到测试环境
5. 验收通过后发布预发 / 生产环境

### 19.3 回滚策略

- 静态资源版本化发布
- 保留最近稳定版本构建产物
- 支持按版本快速回滚

---

## 二十、安全要求

后台系统必须重点关注：

- 路由权限校验
- 按钮权限校验
- Token 过期处理
- XSS 风险控制
- 文件上传类型限制
- 敏感信息脱敏展示

统一原则：

- 前端做展示级权限控制
- 后端做最终权限校验
- 前端不得将权限控制视为最终安全边界

---

## 二十一、性能优化策略

### 21.1 基础优化

- 路由懒加载
- 大模块按需加载
- 公共依赖拆包
- 表格、图表等重型模块延迟加载
- 字典、配置类数据本地缓存

### 21.2 页面优化

- 大列表分页加载
- 减少无意义响应式状态
- 避免页面级重复请求
- 对筛选表单、字典、下拉选项做缓存

---

## 二十二、实施步骤建议

### Phase 1：模板初始化

- 初始化 `admin-web` 目录结构
- 完成 Vite、TypeScript、Sass、ESLint、Prettier、Stylelint 配置
- 接入 Element Plus、Pinia、Vue Router
- 完成基础布局、登录页、首页骨架

### Phase 2：基础能力建设

- 完成请求封装
- 完成登录认证与权限控制
- 完成动态路由、菜单、标签页
- 完成主题配置和全局布局能力

### Phase 3：业务沉淀

- 抽离项目通用组件
- 接入 `lg-lib`
- 建立表格页、表单页模板能力
- 增加监控、测试、CI/CD

---

## 二十三、结论

`admin-web` 的建设目标不是简单搭一个 Vue 项目，而是建立一套适用于长期后台开发的标准化基座。该基座必须同时具备：

- 稳定的技术选型
- 明确的目录与分层边界
- 完整的权限与路由体系
- 统一的请求、状态和组件策略
- 严格的工程规范与质量门禁
- 可持续沉淀的 `lg-lib` 组件协作机制

后续所有管理后台项目，原则上应以本方案为基线进行初始化与扩展，避免各项目再次各自为政。
