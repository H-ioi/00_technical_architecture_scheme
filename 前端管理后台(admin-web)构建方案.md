# 前端管理后台（admin-web）构建方案

本文档在以下三份基线之上**重构**编写，作为管理后台（`admin-web`）专项建设方案，适用于新工程初始化、存量重构评审与研发对齐：

| 基线文档 | 作用 |
|----------|------|
| [前端架构方案总览.md](./前端架构方案总览.md) | 多端划分、管理后台标准栈、工程化总表、目录/组件库联动、实施阶段 |
| [前端开发规范.md](./前端开发规范.md) | 目录结构、分层、命名、组件与 Git 等**执行细则** |
| [test/admin-web/微前端与组件库方案对比.md](./test/admin-web/微前端与组件库方案对比.md) | **多产品线/多域名/非统一 UI** 下的架构决策：**以模版工程 + 统一组件库为主，无界微前端不作为本轮主干** |

**说明**：下文与《开发规范》重复度高的部分采用「原则 + 引用」方式，避免三处长期维护同一长文；执行时以《开发规范》全文为准。

---

## 一、管理后台在总架构中的位置

依据《前端架构方案总览》：

- **PC Web — 管理后台** 推荐栈：`Vue 3 + Vite + Vue Router + Element Plus + Pinia + TypeScript + Sass`，以 **CSR** 为主。
- 业务组件库 **`lg-lib`**（`Vue 3 + Element Plus`）主要服务**管理后台类**及表格/表单中台能力。
- 「可选微前端基座能力」在总览中放在**演进阶段评估**；与本方案下文 **§二** 的决策一致：**默认不将无界作为 admin-web 主干**，除非出现明确「单一门户 URL 聚合多应用」的产品诉求（见方案对比文档 §5）。

---

## 二、架构决策摘要（必读）

以下结论摘编自 `test/admin-web/微前端与组件库方案对比.md`，用于统一评审口径：

### 2.1 事实前提（与存量/交付强相关）

- 可存在 **一套仓库承载多条产品线**、打包后仍按 **不同域名** 部署。
- **各产品线 UI、登录形态可不一致**，无法在集团层强制「同一套壳布局」。
- 能拉齐的主要是 **逻辑层**：鉴权、token、刷新、axios、权限解析、公共业务组件**行为**等。

### 2.2 采取的主路径

| 方向 | 策略 |
|------|------|
| **工程形态** | **模版项目**：统一脚手架、目录、构建、Lint、CI 模板，降低多工程并行成本。 |
| **横向能力** | **统一业务组件库 `lg-lib`**，并沉淀 **认证与安全相关逻辑**（可独立为 `auth-sdk` 或在 `lg-lib` 内模块化管理）；**登录页与各域布局 UI 由各产品线/各部署工程自持**。 |
| **微前端（无界等）** | **不作为本轮 admin-web 主干**。其强项是「单一门户内嵌多应用」；在用户**直达各域名**、且 **UI 无法统一** 的前提下，先上大壳往往增加宿主与子应用层级与治理成本，却不能替代 **SSO / 统一身份设计**。 |
| **增量选项** | 若未来出现 **强制单一门户域名、同一 URL 内聚合多产品线**，再 **单独立项** 评估壳 + 微前端（或门户 + SSO 跳转），作为**增量能力**叠加，而非推翻模版 + 组件库主路径。 |

### 2.3 对本方案章节的影响（落地时必须体现）

1. **布局（§七）**：区分「标准单站点后台模版」与「多产品线各自布局/登录 UI」——后者不由统一壳强行覆盖，由 **`lg-lib` / 设计令牌 + 各工程页面**实现差异化。
2. **认证（§九）**：强调 **OIDC/SSO 与各域回调** 由架构与后端共同定义；前端侧重 **组件库封装 token、拦截器、登出清理**。
3. **组件（§十三）**：**跨项目**沉淀进 `lg-lib`；**不得**把某条产品线的私有 store、路由名、接口路径写死在组件库内（见《开发规范》及总览 §六）。

---

## 三、项目定位与建设目标

### 3.1 定位

`admin-web` 用于承载企业内部 **运营平台、管理控制台、配置后台、审核中心、数据看板** 等 PC 管理类系统。核心诉求是开发效率、表单/表格承载、权限与菜单体系、工程可维护性；**非 SEO**，因此采用 CSR 即可（与总览一致）。

### 3.2 建设目标

- 建立可复用的 **管理后台模版**（目录、分层、权限、请求、组件策略一致）。
- 统一接入 **`lg-lib`**，减少重复业务组件；认证与公共业务逻辑优先沉淀至库 **协议层**，UI 由各项目自控（见 §二）。
- 工程化与 CI 门禁与《前端开发规范》《前端架构方案总览》对齐。
- **存量迁移**（若来自 Vue2 单体等多租户系统）：菜单/路由/接口拆分与 §二主路径一致，**不以微前端为先决条件**。

---

## 四、技术选型

### 4.1 核心技术栈

与《前端架构方案总览》§3.1、《前端开发规范》§2.1 **一致**：

| 类别 | 选型 |
|------|------|
| 框架 | Vue 3（Composition API） |
| 构建 | Vite |
| 路由 | Vue Router 4 |
| UI | Element Plus |
| 状态 | Pinia |
| 语言 | TypeScript |
| 样式 | Sass |
| 业务组件库 | `lg-lib` |
| 测试 | Vitest、Testing Library、Playwright（按需分期接入） |

### 4.2 不采用 Nuxt

管理后台登录后内部场景为主，不依赖 SEO；CSR + Vue Router + Pinia + Element Plus 更适合模版化与权限体系（同原方案 §3.2 理由）。

### 4.3 与「微前端」的关系

- **默认**：不引入无界/qiankun 作为 **admin-web 默认模版** 的必选项。
- **可选**：仅在 §二 2.2「增量选项」成立时，单独做技术评估与预算。

---

## 五、总体架构与分层

### 5.1 分层模型

与总览 §4.2、开发规范 §4 **一致**：

```text
View（页面） -> Component -> Store -> Service -> API Client
```

- **API Client**：请求实例、拦截器、鉴权注入、统一错误处理（可与 `lg-lib` / `auth-sdk` 协同）。
- **Service**：按业务域封装接口；页面与 Store 不直接散落裸请求。
- **禁止**：在 `api/`、`utils/` 中引用 `.vue`；在页面堆超大单文件不拆分（详见《开发规范》§4.3）。

### 5.2 基础能力模块

登录认证、权限、菜单路由、标签页、布局、主题、请求封装、错误处理、消息提示、表格/表单模板、上传下载、字典缓存、监控接入等——与原有方案一致，其中 **登录与 Token 策略**须与 §二多域名/SSO 结论一致。

---

## 六、目录与代码组织

### 6.1 标准目录

**以《前端开发规范》§3.1 全文为准**。以下为与总览对齐的完整骨架，便于脚手架对照：

```bash
src/
├── api/                        # 接口层：请求方法、参数类型、响应类型
│   ├── modules/                # 按业务域拆分，如 user.ts、role.ts、order.ts
│   └── index.ts
├── assets/
│   ├── images/
│   └── styles/                 # 全局样式、变量、mixin、reset
├── components/
│   ├── common/                 # 项目内通用展示组件
│   └── business/               # 项目内复用业务组件
├── composables/
├── directives/
├── layouts/
├── plugins/
├── router/
│   ├── guards/
│   ├── modules/
│   └── index.ts
├── stores/
│   ├── modules/
│   └── index.ts
├── types/
├── utils/
│   ├── request/                # 请求实例、拦截器
│   ├── storage/
│   └── index.ts
├── views/                      # 页面目录：kebab-case
│   └── order-list/
│       ├── components/         # 页面私有组件
│       ├── constants.ts
│       ├── use-order-list.ts
│       └── order-list-page.vue
├── App.vue
└── main.ts
```

### 6.2 目录约束与页面组织

- `src/` 顶层目录保持稳定，避免随意新增平级「职责重叠」目录（如 `hooks/` 与 `composables/` 并存）。
- 页面私有组件优先放在页面子目录 `components/`；跨页面复用进 `components/`；跨项目复用进 `lg-lib`。

典型列表页拆分示例：

```text
order-list-page.vue
  -> order-filter-form.vue
  -> order-table.vue
  -> order-detail-dialog.vue
  -> use-order-list.ts
```

单文件过大（例如超过约 300～400 行）或存在多个明显独立区域时，必须评估拆分（《开发规范》§4.3）。

### 6.3 放置与抽离原则

严格执行《前端开发规范》§3.5～§3.6 与总览 §五：**页面私有 → 多页复用 `components`/`composables` → 跨项目 `lg-lib` → 纯函数 `utils/`**。

### 6.4 命名约定

执行《前端架构方案总览》§7.2、《前端开发规范》中命名章节：**目录与文件 `kebab-case`**，标识符 **`camelCase`**，`lg-lib` 对外组件 **`Lg` 前缀**，业务工程 `components` 下**不得**占用 `Lg` 前缀。

---

## 七、布局体系

### 7.1 标准单站点模版

默认后台可采用统一 **AdminLayout**（顶栏、侧栏、面包屑、标签页、主内容区、`RouterView`），推荐层级：

```text
App.vue
  -> AdminLayout
    -> Sidebar
    -> Header
    -> TagsView
    -> MainContainer
      -> RouterView
```

布局能力建议至少支持：左侧菜单折叠、标签页缓存与关闭、面包屑、全屏/刷新/退出、主题色与布局选项、固定头部或固定标签栏等（与 Element Plus 生态及 `lg-lib` 能力衔接）。

### 7.2 多产品线 / 多域名 / UI 不可统一时的约定（与 §二对齐）

- **不要求**所有部署共用同一套可视化壳；各产品线工程可在 **`layouts/`、`views/login`** 自行实现品牌与布局。
- **统一侧**：交互与权限相关逻辑尽量走 **`lg-lib` + composables**（如 `useAuth`、`usePermission`）；样式侧用 **设计令牌 / SCSS 变量** 约束漂移，而非强制同一套 JSX/Vue 壳组件。
- **禁止**：在无法统一 UI 的前提下，为「上微前端」而额外维护一套 **仅作嵌套容器** 的宿主应用——除非已通过 §二 2.2 增量评审。

---

## 八、路由与菜单

### 8.1 路由类型

建议拆分为三类：

- **静态路由**：登录页、404、基础首页等；
- **动态路由**：按权限加载的业务页面；
- **特殊路由**：外链或 iframe 对接外部系统。

### 8.2 Meta 字段与菜单映射

```ts
interface AppRouteMeta {
  title: string
  icon?: string
  hidden?: boolean
  keepAlive?: boolean
  affix?: boolean
  permission?: string[]
  activeMenu?: string
}
```

推荐采用 **后端返回菜单树 → 前端按组件映射表生成动态路由**：后端提供菜单树、权限码、路由标识；前端负责菜单展示、面包屑、标签页与权限指令联动。

### 8.3 多部署实例下的菜单归属

- **每个独立域名 / 独立部署的 admin 工程** 维护 **自己的** 菜单与路由表（可与该站后端契约一致）。
- **不需要**为了拆仓库先实现「壳侧统一菜单树」；若未来存在真正的门户站点，再单独定义门户菜单与 **appKey** 映射（见总览演进思路）。

### 8.4 路由守卫

建议至少包含：

- 未登录访问受保护路由时跳转登录页；
- 已登录访问登录页时重定向首页；
- 首次进入或刷新时拉取用户信息与菜单权限；
- 无权限跳转 `403`，未知路由跳转 `404`。

---

## 九、认证与权限

### 9.1 Token 与存储

- 登录成功后获得 `accessToken`（及如有 `refreshToken`）；
- Token 存放在受控存储层；所有请求由拦截器统一注入；
- Token 过期时统一触发刷新或重新登录。

在 **§二** 前提下须额外强调：

- **多域名**：会话共享依赖 **SSO/OIDC** 与后端票据设计，**不**依赖嵌 iframe「代替」统一身份；
- **逻辑复用**：刷新、登出清理、权限缓存清理等优先收入 **`lg-lib` 或 auth 子模块**；各站登录页只负责 **收集凭证、品牌展示与跳转**。

### 9.2 权限模型

建议统一支持三类权限：

- **路由权限**：控制页面是否可访问；
- **按钮权限**：控制操作按钮是否展示/可用；
- **数据权限**：控制列表、组织、范围数据可见性。

按钮权限推荐 **`hasPermission('user:add')`** 与 **`v-permission="'user:add'"`** 两种方式并存。

### 9.3 角色与菜单联动

后端建议维护：用户 → 角色 → 菜单、角色 → 权限码。前端负责根据权限码控制按钮与页面元素，根据菜单树生成侧栏与动态路由。

### 9.4 与存量（Vue2 单体）迁移的衔接

若来自多租户、多登录路由的存量：**各登录入口可分期收敛到 SSO + 统一组件库逻辑**；UI 仍可分站点保留（方案对比 §5）。

---

## 十、状态管理（Pinia）

### 10.1 Store 拆分建议

至少建议拆分：

- `app`：布局、侧栏状态、主题等；
- `user`：用户信息、token、登录状态；
- `permission`：菜单、动态路由、权限码；
- `tags-view`：页签缓存与关闭逻辑；
- `dict`：字典与枚举缓存。

### 10.2 设计原则

- Store 只保存共享状态与业务动作；页面私有临时状态优先留在页面内；
- 不在 Store 中堆积大量 DOM 逻辑；
- 异步请求经 Service 调用，避免在组件中散落裸请求。

### 10.3 持久化边界

适合持久化：token、主题、用户基础信息、打开的标签页等。不适合：页面临时筛选、弹窗开关、局部 loading 等。

---

## 十一、接口封装

### 11.1 分层

```text
API Client -> Service Layer -> Store / View
```

### 11.2 API Client 职责

`baseURL`、统一请求头、`Token` 注入、超时、通用错误提示、业务错误码、重复请求控制、文件下载等。

### 11.3 Service 组织

按 **业务域** 组织模块（如 `user.ts`、`role.ts`、`order.ts`），**不要**仅按 HTTP 方法堆叠。

```bash
api/
├── modules/
│   ├── user.ts
│   ├── role.ts
│   └── order.ts
└── index.ts
```

### 11.4 错误处理

统一区分网络错误、认证错误、权限错误、业务错误、服务器异常；展示建议走全局消息，避免每页重复造提示。

---

## 十二、页面开发模式

### 12.1 典型后台页面组成

高频页面通常包含：标题区、筛选区、操作按钮区、表格区、分页、弹窗区、详情区等。

### 12.2 拆分与抽离

- **页面私有组件**：仅当前页使用；
- **项目级组件**：多页复用；
- **组件库组件**：跨项目复用。

列表页拆分示例见 **§六**；页面级逻辑优先 `use-xxx.ts`，常量与列定义可 `constants.ts`。

---

## 十三、组件体系与 `lg-lib`

### 13.1 分层

基础组件主要来自 **Element Plus** → 项目 `components` → **`lg-lib`** → 页面私有组件。

### 13.2 接入方式

推荐通过 `plugins/business-ui.ts`（或等价插件）统一接入 `lg-lib`：全局/按需注册、主题变量与样式注入。

### 13.3 沉淀优先级与边界

优先评估沉淀至 `lg-lib`：数据表格、搜索表单、权限按钮、状态标签、上传、富文本容器、选择器弹窗等。**禁止**耦合单一项目的 store、路由名、私有 API 路径（总览 §六、开发规范 §3.4）。

### 13.4 认证相关 UI 与逻辑分离

- **库内**：token 生命周期、请求挂载、错误码与登出联动、可复用的「无品牌」小块（若确有）。
- **项目内**：登录页布局、品牌图、多租户文案、跳转 SSO 前的交互。

---

## 十四、表格、表单、弹窗与样式主题

### 14.1 表格页

查询与列表数据分离管理；列定义建议单独抽出；操作列与权限码联动；空状态、加载态、异常态统一处理；导出与批量操作需二次确认与权限校验。

### 14.2 表单页

表单模型使用类型约束；校验规则统一配置；新增与编辑尽量复用同一套表单组件；提交前做基础校验。

### 14.3 弹窗

聚焦单一目标；开关状态由父级控制；提交成功后由父级刷新列表。

### 14.4 Sass 与主题

建议结构示例：

```bash
assets/styles/
├── _variables.scss
├── _mixins.scss
├── _reset.scss
├── _utilities.scss
└── index.scss
```

支持品牌主色、明暗扩展、侧栏配色、覆盖 Element Plus 变量；优先设计变量，避免深层嵌套与大段复制样式。主题变量优先与 **`lg-lib` 主题出口**对齐。

---

## 十五、日志、监控与错误处理

统一处理页面加载异常、接口失败、权限失败、路由异常、JS 运行时异常。建议接入 **Sentry**、**Lighthouse**、**RUM** 等（按环境与预算选型）。禁止提交调试 `console`；敏感信息禁止进入日志系统。

---

## 十六、工程化与质量门禁

必备：ESLint、Prettier、Stylelint、lint-staged、Husky、Commitlint、`vue-tsc` 类型检查。

强制要求示例：禁止提交 `console.log`/`debugger`；CI 不跳过 `lint`、`type-check`、`build`；提交信息符合 Conventional Commits。

推荐 `package.json` 脚本：

```json
{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx,.vue --max-warnings=0",
    "lint:fix": "eslint . --ext .js,.jsx,.ts,.tsx,.vue --fix",
    "lint:style": "stylelint \"src/**/*.{vue,scss,css}\" --allow-empty-input",
    "lint:style:fix": "stylelint \"src/**/*.{vue,scss,css}\" --fix --allow-empty-input",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "type-check": "vue-tsc --noEmit"
  }
}
```

---

## 十七、测试策略

**单元测试**：工具函数、权限判断、数据转换、`composables` 纯逻辑。**组件测试**：搜索表单、表格操作、弹窗、权限组件。**E2E**：登录、菜单访问、列表查询、增删改、权限受限场景（工具与覆盖率按阶段与《开发规范》推进）。

---

## 十八、环境变量

使用 `.env.development` / `.env.test` / `.env.production` 等；仅 `VITE_` 前缀暴露给前端；不提交密钥与真实令牌。常见项：

- `VITE_APP_TITLE`
- `VITE_API_BASE_URL`
- `VITE_UPLOAD_URL`
- `VITE_ENABLE_MOCK`
- `VITE_SENTRY_DSN`

---

## 十九、部署与安全

**部署**：构建产物为静态资源，可部署至 Nginx、CDN+OSS、容器静态站点等；推荐 **PR → CI（lint / type-check / test / build）→ 测试环境 → 预发/生产**；静态资源版本化并保留回滚能力。

**安全**：路由与按钮权限、Token 过期、XSS、上传类型限制、敏感信息脱敏。**原则**：前端为展示级控制，**后端为最终权限边界**。

---

## 二十、性能优化

路由懒加载、大模块按需加载、公共依赖拆包、重型模块延迟加载、字典与配置缓存；列表分页加载、减少无效响应式、避免重复请求、筛选与下拉选项缓存等。

---

## 二十一、实施路线（与总览阶段 + 方案对比落地对齐）

### Phase 1：基础统一（对齐总览 §八 Phase 1）

- 冻结 admin-web 技术栈与模版目录；落地 Vite、TS、Sass、Lint、环境变量与 Git Hooks。
- 初始化布局、登录页骨架、请求封装最小闭环。

### Phase 2：核心沉淀（对齐总览 §八 Phase 2）

- 打通 **API Client、Service、Pinia、动态路由与菜单**。
- 接入 **`lg-lib`**；沉淀设计变量与主题机制。
- **优先**：认证模块与后端 SSO/OIDC 路线对齐（方案对比 §6）。

### Phase 3：规范深化与可选演进（对齐总览 §八 Phase 3）

- 测试、CI/CD、监控与埋点完善。
- **评估微前端**：仅当产品明确「统一门户 URL」等条件满足时启动（总览「可选微前端」+ 方案对比 §5）。

### 存量迁移补充（若适用）

- 按业务域拆工程与部署时：**菜单与路由在各站工程内演进**；组件库统一逻辑层；**不以先建壳为前置**（方案对比 §4.5）。

---

## 二十二、结论

`admin-web` 的建设目标是 **标准化、可复用的管理后台基座**，而不是单一项目的临时搭建。本方案在《前端架构方案总览》的技术栈与分层之上，纳入《前端开发规范》的执行约束，并根据《微前端与组件库方案对比》明确：

- **模版工程 + `lg-lib`（含认证等公共逻辑）为主路径**；
- **各部署实例可保留独立布局与登录 UI**；
- **无界等微前端不作为默认必选项**，仅在门户类需求明确时增量评估。

后续新建或重构的管理后台工程，应在此三份文档一致的口径下初始化与扩展，避免与本方案及方案对比结论相冲突的「默认上微前端」假设。

---

## 附录：文档索引

| 文档 | 路径 |
|------|------|
| 前端架构方案总览 | `./前端架构方案总览.md` |
| 前端开发规范 | `./前端开发规范.md` |
| 微前端与组件库方案对比 | `./test/admin-web/微前端与组件库方案对比.md` |
| 无界共享依赖演示（技术参考，非默认架构） | `./test/admin-web/README.md` |
